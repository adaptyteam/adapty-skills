# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repo is

Seven portable, Claude-style skills for agentic CLIs (Claude Code, GitHub Copilot CLI, OpenAI Codex, Gemini CLI):

- **`adapty-integration`** — guides an agent through integrating the Adapty SDK into a mobile app end-to-end: dashboard setup via the Adapty CLI, SDK install, paywall, store configuration.
- **`ads-manager`** — operates Apple Search Ads through the Adapty CLI's `adapty asa` topic: reading performance, changing bids and budgets, keyword work, launching and pausing campaigns.
- **`flow-audit`** — answers one question, is this Flow Builder flow ready for production, by cross-referencing its config against the live dashboard (catalog, access levels) and returning a verdict plus ranked, fixable findings. Read-only: never writes to a flow. Complements `flow-generator` (which owns writes) and `paywall-teardown` (which owns conversion advice).
- **`flow-generator`** — reads a flow's Flow Builder config (JSON) through the CLI's `flows` topic, transforms it, and writes it back. Touches no app code; touches the CLI for the config itself and for products.
- **`migrate-placements`** — moves an app off paywall placements onto flow placements through the CLI's `placements` and `flows` topics. Creates, never converts: a placement's content type is fixed at creation, so every migration is a new placement standing beside the old one, which is also the rollback. Returns the old→new developer-ID mapping and the one SDK call the app must change.
- **`onboarding-teardown`** — reads an onboarding flow (a described sequence, screens, a flow config, or a set of `flows config preview` renders) against a pattern library and returns ranked, testable growth hypotheses plus a read of the onboarding→paywall seam. Touches nothing: no CLI, no files, no network. Owns the **sequence**; `paywall-teardown` owns the paywall screen's execution, and the two are run together on a flow that is both.
- **`paywall-teardown`** — reads a paywall (a screenshot, a `flows config preview` render, or a flow config) against a pattern library and returns ranked, testable conversion hypotheses with expected-impact ranges. Touches nothing: no CLI, no files, no network. The growth half of the pair whose engineering half is `flow-generator`.

One repo, three distribution channels: a Claude Code plugin (`.claude-plugin/` holds the marketplace + plugin manifests), the skills.sh CLI, and plain directory copy of a `skills/<name>/` directory into a tool's skills folder. Keep every skill directory self-contained and format-portable — nothing in one may assume Claude Code specifically.

All seven skills ship inside the single `adapty-skills` plugin, because a plugin exposes every skill under `skills/`. No manifest edit is needed to add one. The plugin name is deliberately broader than any one skill — installing it gives you every skill in the repo, and that is the intended shape. Splitting the skills into separate marketplace entries was considered and rejected; do not reopen it without a reason the plugin name no longer covers.

`marketplace.json` also carries a **deprecated `adapty-sdk-integration` entry** pointing at the same `source: "./"`, so installs made under the old plugin name keep resolving and keep updating. Verified: `plugin details` through either handle reports the same plugin identity and the same skill inventory. Drop that entry only after the docs stop teaching the old handle — never before, or you break the installs it exists to protect.

**Three namespaces, renamed together:** the GitHub repo is `adapty-skills`, the plugin is `adapty-skills`, and the SDK skill is `adapty-integration` (the ads and flow skills keep their names). They are independent namespaces that happen to agree now — a future change to one does not imply the others. Note the repo rename has no alias mechanism the way the plugin does: GitHub redirects git and web traffic from the old name indefinitely, but **`raw.githubusercontent.com` does not follow renames**, so any consumer fetching raw file URLs breaks the moment the rename lands and must be updated in the same wave. The redirect also dies permanently if anything is ever created at `adaptyteam/adapty-sdk-integration-skill` again, so never reuse that name.

## Layout

- `skills/adapty-integration/SKILL.md` — the skill entry point. Phase-by-phase flow: state tracking → setup (docs-fetching rules, session marker, feedback consent) → project analysis → three user questions → dashboard setup through the Adapty CLI → staged, per-platform implementation → closing review → feedback delivery. Platform-agnostic on purpose; per-platform details belong in references.
- `skills/adapty-integration/references/<platform>.md` — one per platform (`ios`, `android`, `flutter`, `react-native`, `unity`, `kmp`, `capacitor`) plus `testing-setup-{ios,android}.md`. Code snippets and the exact docs URLs the agent fetches before writing each piece of code.
- `skills/adapty-integration/references/migration.md` — the cross-platform migration spine: source detection, mapping rules from another purchase system's concepts to Adapty's, and the `ADAPTY_SETUP.md` handoff contract. Injected on every run where `migrationSource` is set, alongside the platform reference; kept lean on purpose, since a headless run can't skip past padding. `references/migration-<source>.md` (currently `migration-revenuecat.md`) adds source-specific mapping and is also cross-platform; its section 5 is a 33-row triage table of RC→Adapty behavioral divergences, each row keyed to a grep-able RC signal and ending in **restructure** or **drop**. The third verdict, **swap**, is deliberately kept out of the table — a divergence that resolves at the call site lives in the deep file only, so the always-injected half stays limited to work an agent cannot do without leaving it. Three rows are keyed to the **absence** of a call rather than its presence — no fallback file, no `AdaptyUI.activate`, no preload — because RC has no counterpart to grep for and a call-site-to-call-site mapping therefore skips them silently. Ten files are the odd ones out, loaded **on demand only** and never injected by default. Eight are the RC topic files — `references/migration-revenuecat-{activation,identity,placements,entitlements,purchases,observer,attribution,gaps}.md`, 60–110 lines each — reached from the triage table, whose verdict cell names one of them (`Restructure → placements`). They carry the reasoning, the architectural options, and the per-gap staleness checks that the always-injected table states in a single line, which is the whole point of the split. **They were one 658-line file until the load unit was fixed:** an agent reads files, not headings, so a `§N` pointer into one big file made every matched row cost all eight topics. One file per topic makes the load unit equal the decision unit — a typical run reads 250–350 lines instead of 658, and a run that touches all eight is no worse than before. Adding a topic means adding a file plus a legend row, never growing a shared file. The other two are `references/migration-architecture.md`, when a call site has no one-to-one Adapty equivalent; and `references/migration-flow-rebuild.md`, when `paywallApproach` is `flow_builder` and the app renders its paywall screen itself — the one migration that retires the app's own UI instead of rewiring it. That file owns the two rules the spine can't state generally: no CLI-created placements on such a run (the flow needs those developer IDs), and the screen's copy, assets, and locales get extracted into the handoff before any code changes, because the user rebuilds it in a visual editor from what the agent wrote down.
- `skills/ads-manager/SKILL.md` — the ads skill's entry point. Eleven named workflows (orient, report, launch, harvest keywords, optimization pass, pause/resume, custom product page, diagnose a dead ad, automations, competitor check, bulk launch), a money-safety contract in prohibition + rationalization-table + red-flags form, the shared account surface, the one-question-one-call rule, and the fallback chain. Drives the `adapty asa` topic, added by [adapty-cli#8](https://github.com/adaptyteam/adapty-cli/pull/8) and shipped in `adapty` 0.4.0.
- `skills/ads-manager/references/asa-management.md` and `asa-metrics.md` — the writing half and the reading half. Management owns per-entity create/update flags, the scope-filter matrix, batch caps, idempotency and the catalog/write budgets; metrics owns entity levels, cohort windows, date-window caps, the metric vocabulary and the analytics pool. The split is load-bearing: the two halves share almost no vocabulary, so a "how did last week go" request loads one file and "add these keywords" the other. `SKILL.md` links into both by exact heading string — `## Scope filters`, `## Writes and idempotency`, `## Status`, `## Cohort windows`, `## The analytics pool` — so renaming a heading breaks the entry point silently.
- `skills/flow-audit/SKILL.md` — the audit skill's entry point. Five phases (resolve/authenticate → select the flow → fetch config + catalog → run `verify-config.py`, `audit-flow.py --report`, `flows config validate`, in that order → report), read-only throughout, and a verdict rule that never certifies what it could not see: `READY FOR PRODUCTION` only when zero blockers fired **and** every open `question` has been put to the user, otherwise `READY, PENDING n CHECKS I CANNOT MAKE`. The report ends in two fixed closing elements — `BEFORE YOU SHIP` (unverifiable-by-design reminders, chiefly the placement link) and, whenever at least one finding fired, a **`WHAT TO DO NEXT`** section that routes every numbered finding into one of four groups by check name (`Answer these` for a verdict-conditional question, `Change in the flow`, `Change in the Adapty dashboard`, `Optional` for a risk) — every finding number is guaranteed to appear there, never silently dropped from the plan. Since 2026-08-31 the same phase-4 pass also carries an **advisory store-review dimension** (six checks, `risk`/`question` only) printed in its own `STORE REVIEW — ADVISORY` section with a verbatim disclaimer: **it never changes the verdict**, not even the `PENDING` downgrade an ordinary question causes, and the verdict rule above is unchanged. See finding 27.
- `skills/flow-audit/references/audit-flow.py` — the six-family completeness checker (stdlib only): triggers, store compliance, products, variables, localization, placeholders. Takes the bare config plus the catalog JSON; `--report` prints the user-facing block (verdict, findings, locale coverage, `BEFORE YOU SHIP`, `WHAT TO DO NEXT`), `--json` prints raw findings, no flag prints a plain list. Exit 0 no blockers, 1 at least one blocker, 2 usage/unreadable input.
- `skills/flow-audit/references/checks.md` — per-check evidence: what each check looks at, its severity and why, its calibration result against the tracked fixture corpus and real flows, and the false-positive/false-negative trap it closed. Read before changing a check. It stays the owner of every **completeness** check and carries a pointer, not a copy, for the store-review six.
- `skills/flow-audit/references/store-review.md` — the same contract for the six advisory store-review checks (`trial-toggle`, `billed-amount-not-shown`, `derived-price-louder`, `no-period-disclosed`, `trial-terms-incomplete`, `external-purchase-link`). Owns the two customer rejection notices **verbatim with their dates**, the four evidence tiers, the framing rule and why it is a rule, per-check calibration in both directions plus each mechanism's negative-test outcome, the two **accepted** false-negative classes, the blind spots (`props.content`-only price reads; type but not colour/position/container; no render), and the ruled-out list — `subscription-under-7-days` above all, so nobody re-adds it from the guideline text. Read before changing one of these six.
- `skills/flow-generator/SKILL.md` — the flow skill's entry point. Owns the **six-phase workflow** (authenticate → decide new-vs-existing flow and transform → `flows config validate` → `flows config preview` + screenshot + iterate → `flows config update`, read back, then publish only on the user's explicit word — `flows publish` where it exists, the editor's button where it does not → **attach a placement and hand its developer ID to `adapty-integration`**, see finding 35), the safety contract (`--expected-updated-at` on every write but the first, never write to a flow the user did not name, prefer a fresh `flows create` for new work, no `flows delete` so never claim to have deleted one), and the verify checklist. The phase ordering is load-bearing: validate and preview both run on a **local file**, so the loop converges before anything is written — one write instead of one per iteration. Four transforms are in scope and a request outside them is named as out of scope, not improvised: add a locale, rewrite copy, add/remove/reorder screens, branching and conditions. Transforming a real config is the default and the safer path, but **authoring is in scope too** and the skill ships `references/flowkit.py` for it. Exactly three things cannot be synthesized: **`flowProductId`** (builder-only; measured not to be a plain UUIDv5 over the obvious inputs), **an asset nobody has a file for** (given a file, `flows media upload` places it — SVG and video excepted), and **real store prices**. The earlier, broader claim — that product UUIDs, icon SVG and the `theme` were also unsynthesizable — was disproved by building several flows from screenshots; do not restore it, because an agent that believes it refuses work it can do.
- `skills/flow-generator/references/flow-schema.md` and `transforms.md` — the shapes half and the risk half. `flow-schema.md` owns the envelope, the browser-export-versus-CLI-config distinction, the element/action/sizing vocabulary (recorded with per-file provenance, and explicitly a floor rather than a closed set), the invariant table, the request map, the two-validator/trust-order method, and **all 27 shape traps as one contiguous numbered series** behind a router table mapping every number to its one-line claim. `transforms.md` owns, per transform, which invariant is at stake, which publish blocker it trips, and what has to be disclosed. The split between the two is load-bearing the same way `ads-manager`'s is — a copy rewrite needs the rich-text rules and nothing about branching. `SKILL.md` links into `transforms.md` by exact heading string, so renaming a heading breaks the entry point silently.
- `skills/flow-generator/references/patterns.md` — the composites an agent cannot guess: where to source a shape from in order (input flow > another of the user's flows > `component-catalog.json` > a skeleton here), what is safe to lift versus what breaks, the version-skew caution, and 20 skeletons. Their **order is load-bearing**: `footer` (**rule 0**) comes before the short-screen spread, the docked button and the side-by-side pair, because the last three are each scoped "once `footer` is ruled out" and an agent reading only one of them builds the fake footer of finding 16; the plan card, the single-plan `const` screen and the bottom-sheet sit together because `propsByState.selected` (finding 21) is the rule all three need.
- `skills/flow-generator/references/preview.md` — the evidence behind phase 4, split out to keep the entry point loadable: why the URL is unreadable, what a screenshot is and is not evidence of, the **six render blindnesses**, the four disagreeing surfaces, and the failure path (analytics beacons choke on a large fragment → use the file-input script, never a smaller config). `SKILL.md` phase 4 is now the loop alone and links here by heading.
- `skills/flow-generator/references/validate.md` — the `flows config validate` evidence, split out because a run only needs it when validate says no. Owns the coverage matrix in both directions (what it catches, what it passes), the three message families, the one-fatal-per-run mechanic and the two call traps (bare config not envelope; the flow must exist). `SKILL.md` phase 3 holds the commands and the exit condition and links here.
- `skills/flow-generator/references/media.md` — the asset path, split out because a copy, locale or branching run never needs any of it. Owns `flows media upload`'s call shape and the `sed`-off-stdout URL capture (never `--json`, which inlines a base64 thumbnail), the accepted-format and byte-cap measurements, the two shapes an uploaded URL binds into (per-locale `values` map on an `image` element versus flat inside a `fill`), the string-`id` rule, the measured geometry table for `hug`/`fixed`×`cover`/`fit`, and the finding that makes the phase ordering load-bearing: neither publish-time gate looks at an image, and a placeholder does not occupy the space the real asset will. `SKILL.md` links into it by exact heading string, so renaming a heading breaks the entry point silently.
- `skills/flow-generator/references/placements.md` — the phase-6 evidence: every `placements create`/`update` refusal and whether it is the agent's to fix, the audience-rewrite trap on `update`, the dashboard route table with its params read off `Routes.tsx`, and why a developer ID is irreversible. Split from `SKILL.md` for the usual reason — phase 6 holds the decisions and the two commands, this holds what they were measured against.
- `skills/flow-generator/references/products.md` — product binding, split out because a locale or copy run never needs any of it. Owns the fact that a flow's `product.id` and the Adapty product UUID from `adapty products list` are the same namespace (verified, not inferred), the read-only lookup chain (`apps list` → `access-levels list` → `products list`/`get`), the `products create` write and its confirmation gate, and the `--period`-versus-price-variable agreement check. It also records the three ways `products create --help` misdescribes required flags (all found by running the command, not by reading it) and the fact that a `const` purchase action binds a product with no `product` element — while the flow stays **unpublishable** until that product is declared in `_meta.screens` anyway (corrected 2026-08-24; see the validate bullet).
- `skills/migrate-placements/SKILL.md` — the migration skill's entry point. Owns the phase spine (authenticate → read every placement, both pages and details → summarize what is migratable → ask where the flow content comes from → **create and publish one flow per distinct paywall, recording each in a `flows.json` ledger** → approve each proposed developer ID → create the flow placements → hand back the mapping and the SDK call to change) and the invariants below. Every write is a `placements create`; the skill emits no `placements update` that changes a content type, because the backend refuses it.
- `skills/migrate-placements/references/migrate.py` — the migration helpers (stdlib only, ships): audience normalization, `to_flow_audience`, the mixed-audience test, `paginate`, `group_by_paywall`, `migratable_summary`, `propose_developer_id`, `build_create_command`, and the `cli_json`/`fetch_*`/`build_plan` layer. Two traps live in code rather than in prose because prose lost to both: the `--page-size` default, and the required `content_type` on every audience entry. **`plan --flows flows.json` is how phase 7 gets its argv** — it emits a `command` per row through `build_create_command`, so the three guards (mixed-audience raise, `normalize_audience`, `to_flow_audience` carrying `segment_ids`/`priority` verbatim) sit on the documented path instead of beside it.
- `skills/migrate-placements/references/api-surface.md` — the measured-facts reference: what each `placements`/`flows` command accepts and the exact error strings the skill routes on. `SKILL.md` links into it by heading, so renaming a heading breaks the entry point silently.
- `skills/migrate-placements/references/stub-flow.json` — a minimal valid v10 config, shipped so the skill can stand a flow up with **no runtime `flowkit` dependency**. `flowkit.py` lives in `flow-generator`, which is absent from a directory-copy install of this skill alone.
- `skills/onboarding-teardown/SKILL.md` — the onboarding skill's entry point. Adapted from Adapty's growth-team `onboarding-teardown` skill; the upstream half is the six-question interview, the contradiction rules, the seven-part output and the framing rules. The additions here are the **config/render input forms** with the read-the-config-instead-of-interviewing table, the **two directions** (Audit / Design) on the same observable predicate `paywall-teardown` uses, `## Choose the flow skeleton first` (six skeletons gated on observable prerequisites), `## What you cannot invent`, the split of upstream's combined sign-off into a **verbatim disclaimer** plus a **conditional CTA**, and the two-way link with `flow-generator`. See finding 28.
- `skills/onboarding-teardown/references/patterns.md` — the pattern library: 8 contradiction rules that fire on *combinations*, patterns by section, and 8 category playbooks. **Vendored byte-for-byte from upstream and kept that way**, the same rule `paywall-teardown/references/patterns.md` follows and for the same reason: a growth-team revision lands as a one-file diff. Every repo-specific fact therefore lives in `SKILL.md`, never here.
- `skills/paywall-teardown/SKILL.md` — the paywall skill's entry point. Owns the **two directions** — audit a paywall someone else will change, or *design*, which is entered on an observable predicate (**no design reference was given**: no reference image, no screen to copy, no layout spec) and runs two passes, the second of which grades and corrects the agent's own render — the three input forms (screenshot / `flows config preview` render / flow config), the internal four-step process, the two output formats, the verbatim disclaimer, the conditional CTA, and the framing rules that keep the numbers honest. Adapted from Adapty's growth-team `paywall-teardown` skill shipped on claude.ai; the additions here are the render/config inputs, the design-brief direction, the `## What you cannot invent` section, and the two-way link with `flow-generator`.
- `skills/paywall-teardown/references/patterns.md` — the pattern library: 15 patterns plus category playbooks, each with when it applies, its cross-check, its impact tier and its category-level grounding. **Kept at that filename deliberately**, matching the upstream skill so a growth-team update to the library copies in as a one-file diff — even though `skills/flow-generator/references/patterns.md` is a different file about layout composites. They never own the same fact: conversion patterns and impact numbers here, JSON skeletons there.
- `tests/` — the fixture corpus for `flow-generator`, plus the repo-only Python helpers that operate on it — sanitizing, rendering, measuring and `test-flowkit.py` (see the `flow-generator` conventions below). Not a test suite for the skills. **`verify-config.py` used to live here as `verify-fixture.py` and moved to `skills/flow-generator/references/` on 2026-08-25 so that it ships, and was renamed at the same time because a runtime agent runs it on its own config, not on a fixture. Every mention of `verify-fixture.py` in the dated findings below was rewritten to the new name: the rounds themselves ran against the old one** — anything in `tests/` is repo-only by definition, so a check an agent must run at runtime does not belong here.
- `scripts/` — the two validation lints and their plain-text allowlists (see below).
- `feedback-endpoint/` — a single Vercel serverless function that receives the end-of-run feedback payload and forwards it to Slack + Airtable. Deployed separately (`npx vercel deploy --prod` from inside that directory); its README documents the env vars and the stable production URL that SKILL.md points at. Has its own tests: `node --test test/endpoint.test.js` from inside that directory.
- `context7.json` — Context7 indexing config.

## Validation lints

**The link lint covers ALL of `skills/`; the symbol lint covers `adapty-integration` and guards the rest.** Widened 2026-08-28 — it previously hardcoded `skills/adapty-integration`, so a green run said nothing about four of the five skills that ship, while the runtime agent is told to fetch URLs from all of them. The widening cost 17 unique URLs and found no dead links; coverage is now printed per skill on every run (`adapty-integration 322, ads-manager 1, flow-audit 7, flow-generator 22, onboarding-teardown 1, paywall-teardown 1` — `flow-audit` had none when the lint was widened and has since gained 7). Two extraction rules were needed and both are load-bearing: a URL containing `…` **or** `...` is a documentation placeholder, not a link — the second came from a real 404 on a media URL inside pasted sample output with the hash cut out (`1e5bbbb4-.../hero-600x400.png`), which is elided-example noise rather than a finding.

**The symbol lint was deliberately NOT widened, and that is a measured decision rather than an omission.** It resolves a file's platform from its name (`references/<platform>.md`) and checks against that platform's docs aggregate; the other six skills have no platform and name **zero** SDK symbols (measured across all their `.md`; re-measured 2026-09-03 when `migrate-placements` was added — still zero). But "they name none" is an assumption that rots the moment someone adds one, so it is now checked instead of believed: a **scope guard** scans those six for `Adapty[A-Z]…` / `Adapty.<member>` and turns the lint **red** with a pointer to either drop the symbol or widen the lint. The capital `A` is what separates an SDK symbol from the domain (`adapty.io`) and the CLI (`adapty asa`). Negative-tested by injecting `Adapty.getPaywall()` into `flow-audit`.

There is no test suite for the skills themselves — the lints are the verification gates. Run them after any edit to a SKILL.md or a reference:

```bash
node scripts/lint-symbols.mjs [platform ...]   # default: all platforms
node scripts/lint-links.mjs
```

Exit codes for both: `0` clean (warnings allowed), `1` findings, `2` infrastructure error (docs unreachable — fix the network, not the skill).

- **Symbol lint** — every Adapty-branded symbol used in a reference's code contexts (`Adapty.x` / `adapty.x` / `AdaptyUI().x` member calls, `Adapty*` type names, in fenced blocks and inline backticks) must exist in that platform's official docs. Ground truth is the `<platform>-llms-full.txt` docs aggregate plus pages the reference links; the docs are verified against SDK sources by the docs team's release process, so docs presence transitively means the symbol exists in the SDK. A reference's platform comes from its filename (`testing-setup-*` files are pinned to their platform in the script). Two plain-text escape hatches, editable without touching code:
  - `scripts/app-side-allowlist.txt` — wrapper names the references deliberately tell the *user* to create. They look Adapty-branded but are not SDK symbols; add the name here when a reference introduces a new suggested wrapper.
  - `scripts/undocumented-sdk-symbols.txt` — real SDK symbols deliberately absent from the docs. Each entry pins a source-of-truth code URL and is verified against the SDK source instead, so a rename still turns the lint red. Format: `<reference>: <symbol> <source-url>`.
- **Link lint** — every https URL in the skill must work the way an agent fetches it: `adapty.io/docs` pages are checked via their `.md` variant with query/hash stripped, `*-llms*.txt` aggregates as-is. A docs 404/410 is a hard failure (the skill literally instructs fetching that URL); external links (stores, consoles, GitHub) failing are warnings only, as are docs pages missing from `llms.txt` (they work but agents can't discover them).

CI (`.github/workflows/skill-lints.yml`) runs both lints on PRs and pushes to `main` (red blocks merge) and on a daily cron that catches drift with no skill commit involved — docs renames, SDK releases. A scheduled failure files one deduped `skill-drift` GitHub issue; repeats become comments on it.

## Conventions when editing the skills

These apply to `adapty-integration`; the `ads-manager` section below adds its own.

- **Docs URLs are load-bearing.** SKILL.md forbids the runtime agent from guessing slugs — it may only open URLs written in the skill or found in a fetched `llms.txt` index. So every docs URL you add must literally exist (the link lint enforces this); never assemble one from a topic and platform name.
- **The session marker matters.** SKILL.md tells the runtime agent to mint a random `sessionToken` and tag every docs fetch with `?ref=skill-<sessionToken>` (a docs-analytics marker, documented in SKILL.md's "Session marker" section). Don't remove, rename, or add a literal example token to that format — hardcoded example tokens get copied verbatim by agents, which corrupts the analytics.
- **Test runs from this repo are not real integrations.** When you run or debug the skill from inside this repo, have it tag docs fetches with `?ref=skill-dev-<sessionToken>` instead (or drop the `ref` on ad-hoc curl spot-checks), so Adapty's docs analytics can tell development traffic from real usage. The lint scripts already strip refs and need no special handling.
- **New wrapper names need the allowlist.** If a reference introduces a new app-side wrapper (service, notifier, context, constant) with an Adapty-ish name, add it to `scripts/app-side-allowlist.txt` in the same change, or the symbol lint turns red.
- **New platform = new reference + docs coverage.** Add `references/<platform>.md` and confirm the docs publish `<platform>-llms-full.txt` — the symbol lint builds its ground-truth corpus from it.
- **New migration source = new reference + spine row.** Add `references/migration-<source>.md`, add a row to the spine's detection table, and add the source to the CLI's picker if the CLI should offer it. No lint edit is needed — the symbol lint prefix-matches `migration*` and derives the platform list.
- **Migration references are linted against the union corpus.** Their Adapty symbols are still verified, so they may name Adapty APIs; they carry mapping and strategy and delegate platform code to `references/<platform>.md`. Dead `revenuecat.com/docs` links are hard lint failures — take RC URLs from `https://www.revenuecat.com/docs/llms.txt`, never assemble one.
- **A "no Adapty equivalent" claim carries the command that proves it, not just the claim.** Nothing in CI can catch such a claim going stale: the symbol lint verifies symbols that *are* named, and a feature Adapty lacks has no symbol to check. So every gap entry in the RC topic files — mostly `migration-revenuecat-gaps.md`, plus a few in `-attribution.md` and `-purchases.md` — ships with its own `curl … llms.txt | grep -i "<feature>"` and the rule that a hit means read the page and report the Adapty equivalent instead — written per entry, because one instruction at the top of a section is what an agent skips. Behavioral limits are a different case and get a re-read of the page that proves them (`create-product` for one-access-level-per-product, `making-purchases` for auto-applied offers) rather than an index search, since a model constraint never appears as an index entry. This is not paranoia about the roadmap: two of the four headline gaps in the internal RC comparison this content came from — offline entitlements and virtual currencies — closed within months, and Adapty ships both today.
- **Behavioral claims about either SDK come from that SDK's source, at a pinned release tag.** The RC topic files state timings and failure modes the docs do not carry — the profile poll cadence and its post-web-paywall acceleration, RC's 5-minute/25-hour cache staleness, the 5-second placement load timeout, and the four different not-initialized failure modes. Every one was read from source, verified to still hold at a release tag (`AdaptySDK-iOS` 4.0.3, `AdaptySDK-Android` 4.0.2, `purchases-ios`/`purchases-android` at 2026-08-17 `master`), and none of it is lint-checkable: the symbol lint verifies that a symbol exists, never what a constant equals. So re-read the source when touching these numbers rather than trusting the prose, and prefer describing a behavior over naming an internal type — the global actor on iOS is deliberately unnamed here, because it is a concurrency detail and not an API an agent should call. This is also the rule that caught a wrong claim inherited from the internal comparison: RC's `recordPurchase` is not macOS-only, and RC's Android observer path is a batch `syncPurchases` rather than a per-transaction report.

## Conventions when editing `ads-manager`

- **Verify against per-command source, never prose and never flag-set composition.** Ground truth for the `asa` surface is each command's own `static flags` declaration in `adaptyteam/adapty-cli`. The CLI's prose docs contradicted that source in four separate places while this skill was written (`--search` coverage, `--page-size` limits, `--period-unit` casing, `product-pages sync` 200 semantics), and composition misleads in its own way — `orgScopeFlags` contains no `--org`, which is exactly where a hallucinated `--org` list filter came from. Read `src/commands/asa/<topic>/<command>.ts`, not the tables.
- **The surface moves, and it moves under existing commands rather than by adding new ones.** This skill was authored against adapty-cli#8 while it was open, and that branch gained a commit mid-authoring which silently invalidated facts already verified — `--page-size` limits, the request budgets, the 429 retry behaviour. Pin the commit you verify against in your notes and re-check numbers rather than trusting a previous pass, because the next CLI release will do this again. Current content is verified against **`1c05f65d`**, the 0.8.3 release merge on `main`. **The command set is a poor drift signal**: 0.4.0 → 0.8.3 added exactly three files (`campaigns bulk-*`) and removed none, while `asa metrics` gained required `--metric`, three scope flags, a page cap cut from 20000 to 5000 rows and a day-grain window cap cut from 90 days to 28, and `automations create`/`update` gained eight action flags. So diff the resolved **flag set** per command and the command **descriptions**, not `find dist/commands`. Two facts also went stale *between* skill updates and were only caught by that flag diff — the 28-day cap (shipped 0.7.1, missed by the 0.8.2 pass) and the row cap, which the skill never carried at all.
- **The safety content is evidence-backed, not authored.** The rationalization table carries exactly two rows, both byte-exact quotes from agents in `docs/superpowers/baselines/2026-08-11-asa-baseline.md`. Never add a row without a real quote from a real run — a fabricated excuse teaches a failure mode nobody exhibited. Red flags are grouped by provenance: those observed in this skill's own baseline, and those adopted from CLI-side reports. Keep the groups separate so a reader can tell which armor rests on measurement.
- **Match the form to the failure.** Two rules had to be rewritten because the form was wrong, not the content. A rule that governs intent ("never pass `--yes` until the user agrees") is satisfiable by an agent that plans to comply while the command on the page already carries the flag — ground it in an observable mechanism instead (`--yes` deletes the preview the CLI would print). And a rule with no compliant use will get an exception invented for it: the same `--yes` prohibition forbade the flag unconditionally in every realistic request, so it became two conditionals keyed to who runs the command.
- **Slots beat reminders.** Required elements live in the command templates as slots an agent cannot fill from memory — `--idempotency-key <key>`, and the deliberately clumsy `--<filter-from-that-list-s-matrix-row>`. That token's length is load-bearing: shortening it back to `--<scope-filter>` makes it fillable by guessing and reopens a closed hole.
- **Changes get pressure-tested, and one clean sample proves nothing.** The scenarios and the twelve-row checklist are in the baseline document. A clean round was twice shown to be luck by re-sampling the identical skill, so a rule is considered closed only after two consecutive clean rounds.
- SKILL.md and the references are agent-facing prose: imperative, unambiguous, no dead weight. Match the existing voice and structure when editing.

## Conventions when editing `flow-generator`

Every rule below exists because it was violated while the skill was built.

- **One owner per fact; link, never restate.** `references/products.md` owns product binding, `references/flow-schema.md` owns the envelope, invariants, vocabulary, the traps and their router, `references/transforms.md` owns per-transform risk and the five-item disclosure contract, and `SKILL.md` owns workflow, safety, verify and preview. A fact stated in two files will drift, and it did inside a single authoring session: `flowProductId` was described as flow-scoped in one file and assignment-scoped in another, and only one of those is true. When a second file needs a fact, link to the file that owns it.
- **`trap N` is `flow-schema.md`'s public API, so a trap is never renumbered.** 78 citations across 15 files use the bare number, and two of them are strings printed to users by `verify-config.py` and `render-check.py` — so the numbers are an interface, not a heading ornament. The router in `flow-schema.md` maps every number to its one-line claim **and to the file it lives in**; **the next free number is 23**, and a new trap takes it rather than slotting in beside a related one. Restructured 2026-09-11 after the numbering had drifted: traps 15-17 and five *unnumbered* rules of the same kind had been appended under `## The transform service is the authoritative validator`, so a grep for "trap 15" under the section that claims to own traps found nothing, `### 1b` physically preceded `### 1`, and the five unnumbered ones could not be cited at all (now 18-22). Nothing about the H2 boundaries was load-bearing — the file had simply grown by appending to whichever heading was last. Verified as a pure move: a sorted-line diff against the previous revision shows only the five renumbered headings, the router, and one de-duplication. **A SPLIT BY SUBJECT WAS THEN BUILT AND REVERTED, and the reverting is the result worth keeping.** `flow-schema.md` became a 468-line spine plus `content.md`/`layout.md`/`conditions.md`, and `patterns.md` a router plus six files; the 12-agent paired round below measured the agent-facing benefit at **zero**, so both were merged back. **What the exercise established is that the addressing discipline — a contiguous numbered series, the router, one owner per fact — is what cures append-drift, and it needs no file boundary.** The split's unique contribution was per-run reading volume, which measured neutral, against a real recurring tax: 103 pointers to repoint, **two dead anchors created in the act of splitting**, and a relative-anchor fragility class `lint-links.mjs` structurally cannot see. One argument is specific to this file and should stop anyone re-splitting it: **`trap N` is an API with 78 citations, so in one file a citation resolves by grep and across four it needs the router** — splitting a numbered series is strictly worse than splitting prose. The revert was verified the way the split was: `patterns.md` came out **content-identical to its pre-change state, a pure reorder** (the 7-line version-skew H2 re-seated under `## Skeletons` so it can no longer swallow 11 sections), and `flow-schema.md`'s sorted-line diff shows only the five renumbered headings, the router and the de-duplication. **Revisit only if the references roughly double**; at ~1,700 and ~1,060 lines a single file is still greppable, and `patterns.md` would be the one to split first, since it has no numbered API to fragment. 
- **Finding 37 (2026-09-11, a 12-agent paired round): the reference split is NEUTRAL — every scored row is null, the cost argument that justified it did not survive measurement, and the round's value was three unrelated shipped defects.** Six scenarios x two arms (`armA` = `git archive HEAD`, `armB` = the working tree), arms diffed to prove they differ only in files a customer install receives, no `CLAUDE.md` and no `tests/` in either, prompts written to disk and diffed to 0 differing lines after masking the arm path, scorer written and negative-tested before dispatch. Ledger: `docs/superpowers/baselines/2026-09-11-split-navigation-round.md`. **Results: R1 (`verify-config.py` clean) 6/6 both arms; R2 (the scenario's own trap absent) 6/6 both arms; wall clock armB faster in 3 of 5 pairs, mean 8% *against*; tool calls armB fewer in 2 of 5, mean 11% against; lines available mean 4,653 armA vs 4,437 armB, a 5% difference inside noise.** The account was untouched (60 flows before and after), verified as an artifact rather than from reports.
  **The anti-result is the finding, and it inverts the design's premise: MORE FILES INVITES BROADER READING.** Files opened per run — armA (11 available): 5, 5, 5, 6, 6, every run narrow because the right file is obvious; armB (20 available): 7, 9, 10, 10, 16, **21**, with one run reading *every reference file in the directory* and four of six opening >= 9. The per-file narrowing is real (an armB `bottom-bar.md` run opens 3,055 lines against armA's 4,533) and is **cancelled by a discovery cost in the tail**. The router table is a table of contents, not a gate. So the agent-facing cost argument for the nine extra files is **not supported**, and per the round's own pre-registered null contract that is the thing to act on rather than explain away. What remains justified is the *maintainer-facing* half, which no agent metric speaks to and which was verified structurally: the contiguous trap series, the numbering repair, the router as `trap N`'s address book, and the de-duplication. **ACTED ON: the nine extra files were merged back the same day** and only the restructure kept — see the trap-N bullet above for what the revert cost and the one file-specific argument (the numbered API) against ever re-splitting `flow-schema.md`. The round is therefore one of the few here whose pre-registered null contract was honoured by undoing the change rather than by trimming its wording.
  **Three shipped defects found, ALL pre-dating the split, two of them found independently by agents in BOTH arms — which is why they are not an arm result.** (1) **`flow-schema.md`'s request map said the tabs group is `type: "tabs"`**, contradicted by the same file's Vocabulary table, the tabs skeleton, `flowkit`, `verify-config.py` and this file's own recorded tabs-crash history; it is the first row an agent resolving a tabs request reads, i.e. finding 15's class **inside the router**. Fixed. (2) **`flowkit.tabs()` emits 0 `propsByState` and 0 `clipContent`** against the catalog `tabs-segmented` template's 4 and 1, and defaults panels to `height: fill` (trap 13) — so **every tab bar authored from the helper has a pill that never moves on tap**, which is finding 21's defect living inside the helper and finding 12's mechanism exactly. Both E agents overrode it from the template unprompted. **FIXED 2026-09-11, and the fix is three separate things the two sources answer differently.** (a) **`item_selected` is now a REQUIRED keyword** and the helper applies it through the existing `on_selected()`, so the state declaration and its override are emitted together and the dead pill is unrepresentable — the same "unrepresentable beats detectable" call as `footer(fill_=...)`. Which look is design and stays the caller's; *that there is one* is mechanics and belongs in the helper. (b) **`layout.clipContent: True`** on the `tabs` element — both sources set it, and it lives inside `layout`, not as a top-level prop. (c) **Heights default to `hug` rather than `fill`, and here the two sources DISAGREE**: the real export uses `fill` on `tabs`, the wrapper and every panel, which is correct *there* because it sits on a non-scrollable screen whose chain is `fill` the whole way down; the builder's own insertable `tabs-segmented` template uses `hug`, which is what survives being dropped into an arbitrary screen, since **`fill` inside a hug parent collapses** (trap 13) and an authored screen usually is one. `hug` works in both directions, so it is the default and `content_height='fill'` reaches the other. Verified against `tests/fixtures/tabs-paywall.json` **key for key**: `states`, the `propsByState.selected` key set (`borderRadius`, `fill`) and `clipContent` all now match the real export, and `verify-config.py` returns OK on a config built from the helper. `tests/test-flowkit.py` +8 rows, **negative-tested per mechanism** — dropping the override reddens 2, `clipContent` 1, each height default 1, the empty-value guard 1 — and all 23 suites plus both lints stay green.
  **Two test defects of mine surfaced while doing it, both the assertion-that-cannot-fail class.** (1) `item_selected` is a *required keyword-only* parameter, so a bare `raises(...)` on the missing-argument case tests **Python's signature, not my guard** — exactly the `**kw` trap this file already records for `video()`; the guard is only reachable with an explicit empty value, so that row now asserts on the **message** (`'NOTHING ON SCREEN CHANGES' in ...`). (2) A new row indexed `e['propsByState']` unguarded, so under mutation it **crashed the suite** on a `KeyError` after the first `FAIL` — which silently masked every row after it and made all four mutations report exactly one failure. A crashing row is worse than a failing one; the count only became meaningful after switching to `.get()`. **Whenever a mutation run reports a suspiciously uniform failure count, check whether the suite is dying rather than failing.** (3) **`shoot.sh` names renders `shot-<screen>.png` after an `rm -f`**, so a second config of the same screen silently destroys the first render. **FIXED 2026-09-11, and the fix turned up a Chrome behaviour plus a false positive in the guard beside it.** Every output now carries an 8-char signature of the **config's content** (`shot-<sig>-<screen>.png`), which answers both reported failures at once: two configs cannot collide, and a render of superseded bytes — the orphaned-Chrome case one Pip agent had to quarantine — carries the *old* signature and is identifiable on sight. Chrome also writes to a per-run temp that is **moved** into place only when complete, so a Chrome orphaned by a killed run keeps writing to its own `$$` name, which nothing reads. Verified end to end: two configs of one screen into one directory now produce two files where the second used to destroy the first.
  **`--screenshot` writes NOTHING, silently, unless the path ends in `.png`.** My first temp name was `"$2.partial.$$"` and **every render came back empty**, which reads exactly like a broken config — the original script produced a file on the same input, which is the only reason the cause was findable. Measured directly: `--screenshot=x.png.partial.999` produced no file while the identical run to `ok.png` wrote 1,564 bytes. The temp is now `"${2%.png}.partial.$$.png"`.
  **And the flat-render guard falsely rejected a CORRECT render of mine, so its verdict now depends on whether the host answered.** A minimal two-tab screen measured **97.7% one colour, 7% ink span, 7% longest run** and was renamed `NOT-A-RENDER-*` — it is finding 20's own accepted limitation (*a screen made only of text is not separable from an error page*) firing in the field, on good output. The new information is that finding 20 also added an upstream **host probe**, so by the time the pixel guard runs, the failure it was built for — Chrome screenshotting its own DNS/connection error page — has already been ruled out. So when the probe **passed**, a flat image now **keeps the file** and prints `rendered … (FLAT — look closely)` with both possibilities named; only when the probe did not run (no `curl`) does the old rename-and-fail apply. Both branches verified: unreachable host still fails in **1 s** with no Chrome launch, and the sparse render is handed over as `LOOK AT:` instead of destroyed. **The transferable half: once you test a cause directly, the artifact-side heuristic that used to stand in for it should be demoted rather than kept at full strength** — leaving both at full strength is what makes a guard destroy correct work. Also `__pycache__` sat in three skills' `references/` — gitignored so git never saw it, but **a directory-copy install would have shipped it**, the exact defect `skill-validator` flagged once. Removed.
  **THREE METHODOLOGY FAILURES OF MINE, two now rows in the gate.** (1) **I counted PANELS in a reference strip and called them SCREENS.** R6 demanded >= 15 screens because the Pip strip has ~20 panels; cropping panels 4 and 5 proved they are **one screen shown unselected and selected** (the progress bar advances too), so the strip is **12 distinct screens plus state variants** — which is exactly what both arms built. The row failed the correct answer and it self-tested clean because the scorer and the rubric shared the same wrong assumption; it was caught only because both arms converged on the same "wrong" number. (2) **My first R4 extractor counted the "Not read, deliberately" list as files read**, which made armB look like it had opened everything — the bug would have inverted the headline, and both agents write that section. (3) **The harness clock is unreliable three ways and I nearly built a conclusion on it**: six-agent Chrome contention (one agent measured ~99 processes, shots taking minutes against the documented ~18 s, one render dying at the 120 s timeout), leftover background waiters (one run reported 22.7 min then 32.8 min for the same work), and discretionary verification swamping the effect (one agent rendered a second layout variant purely to compare). Tool calls and lines-available survive contention; minutes do not.
  **What is NOT claimed.** That the split helps or harms. That any scenario reproduces a real user failure — none was attempted. **`flows config validate` never ran in either arm**, so no artifact in this round passed a publish gate; the bar was two local gates plus a render. The arms held `flow-generator` alone, so phase 2's routing to `paywall-teardown`/`onboarding-teardown` could not fire and every agent graded its own design unaided. n=1 per cell, and the prediction that armB would be faster was **wrong** — predicting a treatment benefit is now 1-for-8 in this repo, while predicting competence is 3-for-3.

- **THE ANCHOR CHECK I VERIFIED BOTH SPLITS WITH WAS WRONG, and it is the assertion-that-cannot-fail class again — this time in the instrument rather than in a test.** My slugger used `re.sub(r'\s+', '-', s)`, collapsing runs of whitespace; GitHub replaces **each** space with a hyphen, so a heading containing an em dash (`A carousel — the real element`) slugs to `a-carousel--the-real-element` with **two** hyphens. The consequence runs in both directions: a correct double-hyphen link reads as DEAD (I nearly "fixed" three real links in `patterns.md` on that basis) and a wrong single-hyphen link reads as OK. The `flow-schema.md` split had been verified with the broken version and reported clean; re-running with a 1:1 slugger immediately found **two dead anchors I had caused myself** (`#shape-traps`, from renaming that heading to `## Shape traps: the router`) plus one pre-existing (`preview.md` linked `[Verify](#verify)`, a section that lives in `SKILL.md`). All three fixed. **Rule: an anchor checker must replicate the slugger exactly — lowercase, drop everything outside `[\w\s-]`, then map each whitespace character to one hyphen — and it must be exercised against a heading that contains an em dash before it is trusted.** `lint-links.mjs` checks https URLs only, so nothing in CI covers this.
- **`verticalAlign` was stated twice in full and is now stated once.** `### verticalAlign, precisely` kept its heading, because it is an anchor target (`flow-schema.md#verticalalign-precisely`) and its *position* is the point — the schema is what sends you there — but its body is now a pointer to trap 18, which owns the measurement. CLAUDE.md's own finding 33 had noticed the file "says twice not to author it" and read that as emphasis; two full copies of one measurement is the drift the one-owner rule exists to prevent, and emphasis is what a pointer buys instead.
- **Do not write guidance for a trap the baseline already closed.** Measured against skill-less agents in `docs/superpowers/baselines/2026-08-19-flow-generator-baseline.md`: they preserve rich-text `variable` price nodes (2/2), repair a dangling `navigate` nested in a `conditional`'s `default` branch (2/2), and find all 42 localizable fields (2/2). What actually failed was `status`/`id` on re-upload (0/4) and undisclosed structural decisions — bare-string `content` handling split 2 agents left / 2 converted, with 0/4 disclosing it as a choice. Content that teaches mechanics agents already have is padding, and padding is what a headless run cannot skip past. Guidance earns its place by naming stakes and forcing disclosure, not by walking JSON.
- **`--help` is not the command manifest. Enumerate `dist/commands/`.** `flows config validate` and `flows config preview` exist in the package, are listed in `oclif.manifest.json`, and were **absent from `flows config --help`** on the version checked — which produced a confident, wrong claim that neither command existed. The reliable enumeration is `find "$(npm root -g)/adapty/dist/commands" -name '*.js'` (or the npx cache path), plus reading the command's own `static flags` and `static description`. This is the same rule `ads-manager` already carries, one level deeper: not just "prose lies about flags" but "help output omits whole commands."
- **Check which version you actually invoked.** The 404-on-validate investigation crossed two versions without noticing: `npx adapty@latest` ran **0.6.0** while the inspected npx cache directory held **0.6.1-beta.0**. `validate`/`preview` were in the beta only *at that time* (both are stable now — see the version-floor bullet). Print `--version` in the same command as the thing you are testing, and never read a cached `node_modules` as evidence about a different tag.
- **The `flows` CLI version floor is per command, verified from the published tarballs, and it does not move monotonically.** Read off `npm view adapty@<v> dist.tarball` + `tar -tzf | grep commands/flows` — the only enumeration that neither `--help` (hides commands) nor a global install (usually stale) can lie about. `flows` + `config get/update` from **0.6.0**; `config preview` from 0.6.1-beta.0 and **stable since 0.7.0**; `config validate` in 0.6.1-beta.0, **absent from 0.7.0**, back in 0.8.0-beta.0 and **stable in 0.8.0**; `flows media upload` 0.8.0-beta.0 and 0.8.0. Two consequences the earlier "validate and preview are beta-only" note got wrong: **`@beta` is not needed for the normal loop** (stable `latest` covers phases 1-5, preview included), and **a command can leave stable again**, so "a later version has it" is not inferable from an earlier beta. The skill now resolves **`$ADAPTY`** once in phase 1 — `adapty` if `--version` is >= 0.8.0 (raised from 0.7.0 when `validate` landed in stable), else `npx --yes adapty@latest` — and every runnable example is written against that variable rather than a bare `adapty`, which is what let three agents conclude the commands did not exist. **`--yes` is load-bearing**: without it npx asks permission to install an uncached package and a headless run has nobody to answer. Facts about behaviour, each established by running the commands: `config get` returns an **envelope** (`{config, remote_configs, status, updated_at}`); `--expected-updated-at` wants the **epoch integer** from `config get` and rejects the ISO timestamp `flows create` prints; `validate` is advisory, exits **1 for both "not publishable" and "API error"**, so the `--json` `valid`-versus-`error` distinction is load-bearing (its endpoint 404'd against production through 0.8.0-beta.0 and is **live from 0.8.0** — see the validate bullet below); `preview` is **fully local** (no `--app`, no flow id, no auth) and accepts an envelope or a bare config; `flows media upload` is **live in production from 0.8.0** (see its own bullet); and there is **no publish and no delete**, so both stay dashboard actions. **Never invent a command name or a flag** — if a command is absent, the skill says so and falls back.
- **`flows publish` and `flows update` LEFT STABLE AGAIN, which is the third instance of that pattern and the reason the skills now probe instead of comparing versions.** Read off the published tarballs (`npm view adapty@<v> dist.tarball` + `tar -tz | grep commands/flows`): both commands are in **0.8.3-beta.0** and **0.8.3-beta.1**, **absent from the `0.8.3` release** (published 2026-09-08, and `latest` as of 2026-09-09), and back in **0.8.4-beta.1**. So a `sort -V` gate against `"0.8.3"` — which `migrate-placements` shipped — **admits a build without the command** and lands the run in a `command not found`, the exact dead-end finding 1 of GREEN run 1 recorded as *a version floor without an invocation becomes a dead end*. The gate is now `npm i -g adapty@latest` plus `flows publish --help`, with one `npx --yes adapty@beta` attempt before declaring the route unreleased. **Do not reintroduce a numeric floor for these two commands**; `validate` already taught this once and the lesson did not transfer because it was written as a fact about `validate` rather than as a rule about floors.
- **Publish is asynchronous and the CLI now says so itself, in three lines that are suppressed under `--json`.** From the commit adding async guidance (`adapty` 0.8.4-beta.1), a human-mode `flows publish` prints `Publishing started — status: publishing. This is asynchronous; the flow is NOT published yet.` followed by the `flows get` poll and the `flows config get` diagnosis, each with the app and flow already filled in. The **older single line is a prefix of the new one**, so a prefix match survives both builds and an equality match does not — which matters because `api-surface.md` quotes CLI strings verbatim on the stated grounds that an agent routes on them. Under `--json` all three are gated off by `!this.jsonEnabled()`, so a `--json` publish still leaves the caller holding `status: publishing` and nothing else.
- **A failed publication is finally readable from the CLI, and it closes a hole `validate.md` had documented against itself.** `FlowConfigDTO` gained `publication_status`, `publication_error` and `transform_error`, rendered generically by `printResponse`, so `flows config get`'s envelope now carries the transform service's own objection to the last attempt. That matters most where the existing guidance was weakest: `validate.md` records that a flow sitting in `publication_failed` **validated clean**, and then told the agent to re-run `validate` and read the objection — a proxy that can come back green on exactly the case being diagnosed. `flow-audit` gets it for free, because its phase 3 already writes the whole envelope to disk, so the reason for the `publication-failed` finding is often already in a file it fetched; the finding stays a `question` and the verdict is unchanged, because a `transform_error` is evidence the agent was handed rather than an analysis it performed. Three cautions, all from the source rather than from prose: the fields are **passthrough**, so absent where the API does not send them and absence is not a finding; `transform_error` is a **raw string** (a JSON issues payload or a summary); and the DTO comment tells you to *"Parse with `parseFlowPublicationError`"*, **a function that does not exist anywhere in the CLI** (0 hits at `cli-flow-cruds`) — so quote the string, do not expect a machine-readable code, which is the same shape as `validate --json` returning `code: null` and `path: null`.
- **`flows config validate` shipped in stable `adapty` 0.8.0 with its endpoint LIVE IN PRODUCTION, and it runs the real publish-time transform service — so the skill gained a pre-write publish gate it never had.** Measured 2026-08-24 against production with real flows in `app_finance`; `validate.js` is byte-identical between 0.8.0-beta.0 and 0.8.0, so the change is purely the endpoint going live. What it catches, each by injecting the defect into a previously-clean real config: an undeclared product (`props.product` **and** `.purchase.product`), a `groupId` naming no declared group, a `navigate` (or a screen deletion) stranding a target, a `hierarchy` id with no `map` entry, a `defaultLocale` naming no configured locale, a stray declared locale, a malformed hex. What it passes: `fill: "banana"`, `schemaVersion: 999`, top-level `status`/`id`, a missing `states`, a missing `defaultLocale`, a hyphenated element id (the black-screen class), and **every advisory warning** — a successful run returns `issues: []`, not a warning list, so the silent-success channel stays unreachable from the CLI and the phase-5 device callout stays load-bearing. Three mechanics that shape how the skill uses it, all measured: it is **version-agnostic** (a v9 config validates clean, so no baseline), it takes the **bare config and rejects the envelope** with `Invalid flow input` — which reads exactly like a broken config — and **it reports one fatal per run** (three injected defects → one issue; adding the third changed *which* one came back). Hence phase 3's exit condition is a run that printed `valid: true` over the bytes about to be written, and Verify is walked **first** because it names every defect locally in one pass where validate needs a round trip each. Also measured and worth not overclaiming: a flow sitting in `publication_failed` validates clean, so `valid: true` is "no blocking objection to this document", never "publishing will succeed".

- **GREEN round on the validate loop (2026-08-24, 12 agents over two rounds): the loop rule is NOT load-bearing, and it was trimmed rather than kept.** Scenario, arms and the pre-registered rubric are in the untracked ledger. **Round 1 was inconclusive by construction** — the task was "delete two screens and move a grouped element", so all three resulting defects were *direct consequences of the task*; both arms repaired them during the transform and `validate` returned clean on its first call. 4 of 6 runs never saw a `valid: false` at all, and the other 2 saw it only from negative controls **they built themselves**. That is finding 11's trap repeating, and it generalises into a standing rule: **a GREEN scenario whose defects the task implies cannot test a checking rule** — the defects must be latent and not inferable from the work being asked for. **Round 2** planted three malformed hexes in `theme.colors`, chosen because the Verify list has **no hex row** (so a local walk provably cannot find them) and because all three return the **location-free** `Generated JSON failed schema validation`. It worked: every run got `valid: false`, made 8-13 validate calls, and bisected all three — "fixing any two still fails, fixing all three gives `valid: true`" — and **no agent in either arm stopped after one fix and claimed clean**. Six control runs across two rounds never exhibited the failure, so per `writing-skills` and this repo's own *do not write guidance for a trap the baseline already closed*, the one-fatal-per-run paragraph came **out** of `SKILL.md` phase 3; `validate.md` still owns the evidence, and the one-line exit-condition blockquote stays because it defines done rather than warning about a trap. **What the round did prove earns its place**: every run independently reported that the schema check is blind to hex format ("`IColorHex` is typed as a bare `string` with no pattern") and that the render is too ("preview draws light mode only; all three broken values are `dark`") — the not-overlapping-checks claim was *used*; and the endpoint was flaky enough that runs hit `http_502` and a `valid: null` and correctly called them failed calls, which is the verdict-not-exit-code rule doing real work. Exit codes re-verified afterwards against one agent's contrary claim: **1 for invalid, 1 for API error, 0 for valid, in both `--json` and plain modes** — the agent was wrong, the documentation stands.

- **Open finding from that round, deliberately NOT fixed yet: the artifact carrying the requested filename can be the unpublishable one.** Handed a teammate's blocked config plus a small copy change and asked for it "ready to write", **5 of 6 agents** returned `final.json` failing the publish gate and put the passing version under a *different* name — every one disclosing the choice in full, and declining to silently revert someone else's edits, which is better judgement than blind repair. But a user who writes the file they asked for gets a flow that will not publish. Nothing in the skill says which artifact gets the primary name. Per the Iron Law this needs its own baseline before any guidance lands; it is the next scenario, not a same-session fix.

- **Methodology note paid for in this round: never read a boolean out of jq with `//`.** The round-2 scorer used `jq -r '.valid // "APIERR"'`; jq's alternative operator treats **`false`** as empty, so every `valid: false` printed as an API error and the round briefly looked like an infrastructure failure instead of a result.

- **The `const`-purchase declaration claim was wrong and is corrected in three files at once.** `products.md` said a `const` purchase "needs nothing" and that calling it a publish blocker "would be wrong"; validate refuses exactly that shape with `missing flowProductId`, path `...purchase.product`, on our own `tests/fixtures/tabs-paywall.json` (whose raw export really does carry `_meta.screens: {}`). Both halves are true and the old text collapsed them: the `const` form is self-sufficient **for binding and rendering** — the preview page does not run the transform service — and the flow is **not publishable** until the product is declared. Per the repo's escalation rule the check went mechanical: `skills/flow-generator/references/verify-config.py` now warns on every `const`-purchased product missing from `_meta.screens` (calibrated: fires on exactly the three products validate refuses in `tabs-paywall.json`, silent on the other three fixtures). This is also the second time a render-verified claim turned out to be render-verified *only* — the same shape as the `patterns.md` plain-text-prices bug: a rule that produces something which draws perfectly and is wrong downstream survives every check the loop can run.

- **`flow-publish-doctor` (`.agents/skills/flow-publish-doctor` in `adapty/adapty-dashboard-interface`) is a fifth evidence source, and it is READ, never vendored.** It is the dashboard team's diagnostic skill, distilled from a 461-finding QA corpus, and it carries internal Jira epics, admin-API hosts and internal ticket families — none of which may ship in a plugin that installs on customer machines. So the rule is the same one used for the support channel: **adopt the mechanism, retargeted to what an agent holding the CLI can act on, and cite nothing internal.** What was adopted here: its axiom that *the translator stops at the first fatal* — which our own three-defect measurement independently confirmed and which is the reason phase 3 is a loop and Verify runs first; its `missing_flow_product_id` subcase 1 (a purchase-only product is invisible to the declaration harvester), which is what exposed our wrong `const` claim; and its framing that a green transform is necessary but not sufficient, which is why validate.md refuses to let `valid: true` be read as "it will publish" or "it will draw". What was deliberately **not** adopted: its 14-signature catalog wholesale (most signatures are reached through the admin API and the builder UI, not through `flows config validate`, and a catalog we cannot re-measure rots silently), and every ticket id. One claim of theirs is now **stale in our favour** and should be reported back: "there is no non-mutating transform endpoint; only `POST /publish` gives authoritative confirmation" — `flows config validate` is exactly that endpoint.

- **`flows media upload` IS LIVE IN PRODUCTION (measured 2026-08-24, `adapty` 0.8.0), and the skill now uploads assets instead of handing every image to the user.** This supersedes the previous entry, which recorded the endpoint as dev-only ([ADP-7610](https://adaptyio.atlassian.net/browse/ADP-7610)) and told the runtime agent not to call it; that was a release decision, not a measurement, and the release happened. `references/media.md` owns the facts. What was measured, each by running the command against `app_finance`: **the printed URL is live immediately** (`200 image/png` on the same second); the **byte** cap sits between 2,599,724 (accepted) and 2,950,946 (refused) and is on file size, not dimensions (a 3000×2000 PNG at 22 KB uploads, a 1150×900 at 3.1 MB does not), reported as a bare `http_400` with empty `fieldErrors`; a non-image gives a clean `validation_error` naming the permitted enum; exit codes are 0/1/2 (ok / API refusal / unreadable file); and **the upload does not deduplicate** — identical bytes twice gave two ids and two URLs, with no `flows media list` or `delete` to clean up, which is why the skill says upload once and reuse the URL. **`--json` is a trap**: it adds `preview_base64`, 8,232 characters of WEBP for a 5 KB source, so the recipe captures the URL with `sed -n 's/^URL: //p'` off the human output.
- **SVG upload returns `http_500` even though both sides advertise it**, so the icon story is unchanged: `.svg` is in the CLI's own MIME map and `SVG` is in the server's permitted-format enum, and it fails every time. Icons stay authored inline in `_meta.icons` with real `raw` markup. Do not "solve" this by rasterizing a glyph to PNG — a one-size bitmap is a worse artifact than the vector, and the failure is upstream of us.
- **The media change's load-bearing finding is an ORDERING one, and it came from a render measurement: a placeholder does not occupy the space the real asset will.** Same element, same props: the empty `values` map draws **242×256** and the real 3:2 asset draws **242×161** — **95 px** apart on a 932 px screen, with everything below it moving. So previewing with placeholders checks a layout that will not ship, and the skill now resolves assets in phase 2 (alongside products, for the same reason: it gates the design) rather than leaving them to the handoff. Geometry, all measured on one 600×400 asset: `height: hug` derives the drawn height from the **asset's** aspect and makes `objectFit` visually irrelevant (so a `value` left on a hug size is dead, and swapping the file silently rewrites the screen), while a fixed height holds the box and the asset absorbs the mismatch — `cover` crops, `fit` letterboxes and leaves a dead band that reads as a spacing bug on a dark screen. `objectFit` has exactly **two** legal values, `fit` and `cover`.
- **An image is invisible to both publish-time gates, so it is the render's job alone — and now `verify-config.py`'s.** Measured four ways on one config: `flows config validate` returned `valid: true` for a real URL, a **numeric** `id`, a **missing** `id` and an **empty** `values` map alike, and the schema check passed all four too. The schema reason is structural and worth keeping: `ILocalizable.values` is typed `additionalProperties: {"$comment": "unhandled type: T"}`, i.e. unconstrained, so **anything inside a localizable wrapper is invisible to the schema**, `IImage`'s `required: [id, url]` included. Consequence: **a flow whose hero is still a placeholder publishes an "Upload Image" checkerboard to real users** with no gate objecting. Per this repo's escalation rule the check went mechanical — `verify-config.py` warns on an empty `values` map and on a non-string asset `id` (calibrated: fires on all three injected defect forms, silent on the tracked corpus, and on a flowkit build flags only the deliberate `PLACEHOLDER`).
- **Two config shapes, and `flowkit.image()` exists because of finding 12's lesson.** An `image` **element** wraps the value in the per-locale `values` map (so a per-locale asset is expressible); a background **fill** takes `image` flat with no wrapper. The upload prints a numeric `id` and the schema wants a **string** — stringify at the bind. And flowkit had **no image helper at all**, which is the mechanical reason authored screens never carried assets: *an author reaches for what the helper exposes*. `image()` now takes the URL positionally, raises on a falsy one, requires the explicit `flowkit.PLACEHOLDER` sentinel for the no-file case (unrepresentable beats detectable, same argument as `flatten()`), and raises on an `objectFit` outside the two-value enum. 31 checks in `tests/test-flowkit.py`, and its output is **render-verified**, not just schema-verified.
- **Still ruled out, and recorded so nobody re-litigates it from the command's existence:** SVG (500s), video (no path), fonts (still a manual Flow Builder upload), and any *un*-upload — `flows media` has only `upload`.
- **"If it cannot be an element, draw it and upload it" is now in the skill, and its value is entirely in the three exclusions** (owner's proposal, 2026-08-24). The capability is real — a multicolour or gradient glyph, an illustration, a radial glow, a chart or a mesh is not a `text`, a `fill`, an `icon` or a stack of them, and leaving a hole or shipping a lookalike is worse than rasterizing. But it is the **last rung of a ladder** (monochrome `currentColor` SVG icon → `fill` with stops → stacks → rasterize), because everything an element does, an image stops doing. **The exclusions, each mechanically grounded:** (1) **text** — not "text in a missing font", *any* text, because baked words cannot be translated, cannot carry a price variable, and are invisible to the locale parity check, which makes a rasterized price the exact renders-clean-ships-a-lie failure this repo already ate once (`patterns.md`'s plain-text-prices bug); a missing typeface is a named ask, never a reason to bake the sentence into a bitmap; (2) **anything selectable** — a group member must be `product`/`selectable`/`tab-item`, so an `image` with a `groupId` is inert, and this exclusion is **already enforced**: `verify-config.py` errors on it by name, no new check needed; (3) **anything whose colour must follow the theme** — an image has no appearance variant at all (`values` is keyed by *locale*; `IImageElement` has no light/dark hook), while a themed colour does, and this is not hypothetical: **two of the four corpus fixtures declare a dark variant for every colour they define (14/14 and 11/11)**.
- **The measurement that makes rasterizing safe at all: alpha survives the round trip.** A hand-written RGBA PNG uploaded, came back from the CDN **still RGBA** (re-encoded — the served md5 differs from the sent one, so do not treat the stored bytes as yours), and composited cleanly over a black screen with no matte and no white box. **A transparent glyph is the only bitmap that is theme-safe**, because the screen's own background shows through it — which is why the guidance is "transparent background, no baked surface" rather than "do not do this". Two limits stated honestly: the box is in **points** and the asset scales to it (measured), so drawing at 2–3× costs nothing, but crispness on hardware is **not device-measured** — the preview renderer is not a retina device; and a rasterized graphic **looks finished**, the emoji hazard one level up, so the agent must name it as drawn-by-me and replaceable or the user ships a sketch believing a designer made it. `paywall-teardown`'s hero row carries the matching guard: an illustration you drew does **not** satisfy the hero prerequisite, because that archetype sells the app's real output and a stand-in is a fabricated result.
- **Three defects shipped this session from the same root cause, and it is worth naming as a class: a shape the BUILDER never emits can pass every gate an agent can run.** (1) A two-layer `[image, gradient]` fill — tint absent on iOS. (2) `footer` used as both a colour id and a typography preset id — `DecodingError … Duplicate Key`, flow would not open. (3) `selectProduct` plus a predicate on a `toggle` group — the only one of the three that *was* caught, by `validate`. In cases 1 and 2 **`flows config validate`, the schema check and `config preview` were all green**, and in both the disproof was already sitting in the corpus: 0 of 8 real exports have a multi-layer fill, and 0 of 8 reuse an id across the two `theme` lists. So the operating rule is sharper than "the trust order is validator > config > schema" — it is **the schema tells you what is permitted, a real export tells you what is produced, and only the second predicts the device.** When authoring a shape you have not seen in an export, say so out loud and treat the device check as load-bearing rather than ceremonial. Both were converted into mechanical checks the same day (`verify-config.py`: multi-layer fills are not checked mechanically yet — an open follow-up — but every id-keyed collection now is).
- **Speed work (2026-08-25, `perf/skill-speed`): the three costs that matter, all measured.** (1) **The `npx` wrapper costs 0.73-1.40 s per invocation against 0.07 s for an installed binary** — more than most of the commands it wraps (`config preview` 0.08 s, `config get` 0.37 s, `config validate` 1.20 s), and a run makes dozens of calls. Phase 1 now installs once and falls back to npx only when the global prefix is unwritable. (2) **A headless screenshot is 17-20 s and it is Chrome's cold start** — not the page, not the config size (a 187 KB config previews as fast as a 57 KB one). `--virtual-time-budget` 5000 vs 9000 gives **byte-identical output**, and at 1500 it produces **no file at all** while the wait runs to your timeout; **three Chrome instances at once did not finish in 120 s** when one takes 18. So the only lever is *rendering fewer screens*, and `references/montage.py` (new, pure stdlib) joins the ones you do take into one strip so N inspections become one. (3) The slowest loop is the **device round trip**, which is why converting each device finding into a local check is the real speed work. **The same npx mistake recurred INSIDE the fix, and the lesson is that per-call package resolution hides anywhere a script has a dependency.** `gates.sh` collapsed three gate turns into one, and that one call measured **24.3 s** — of which **12.7 s was `npx --yes --package=ajv@8` resolving ajv on every run**, for the one step that is merely *advisory*. Installing ajv once into `~/.cache/adapty-flow-schema` and running the checker from that directory (its `loadAjv` searches `cwd` first) took the same check from **12.7 s to 0.3 s**, and the whole gate run from **24.3 s to 10.8 s** — the remainder being `flows config validate` on a slow host. The cold path self-heals: no cache dir means one ~10 s install, then 0.3 s forever. Both shipped `.mjs` docstrings taught the slow `npx --package=` form and now teach the cached install first. **Grep for `npx --package=` whenever a reference gains a script** — the `adapty` CLI was only the most visible instance of this, not the only one. **Do not re-litigate the two dead ends** — a smaller virtual-time budget and parallel Chrome are both measured losses, and `--user-data-dir` (the obvious fix for the second) makes the launch fail outright.
- **A shipped reference may never instruct running something under `tests/`.** `tests/` is repo-only; `skills/flow-generator/references/` is what a customer install has. Found by applying the rule above to the skill itself: `SKILL.md` was telling agents to run `tests/render-measure.py`, and `patterns.md` linked it as `../../../tests/`. Moved to `references/render-measure.py` (`git mv`, history preserved) and all call sites updated — the same fix `verify-config.py` already needed. **Check this whenever a reference gains a command**: every script the skill instructs an agent to run must live under `references/`.
- **The SKILL.md eviction pass (2026-08-25): 7,455 -> 6,091 words, -18%, and the guardrail is the part worth keeping.** Method: inventory every bolded rule *before* cutting, then diff the wording against the whole shipped skill afterwards. It caught three near-losses that a read-through would not have — Verify rows I removed on the assumption `flow-schema.md` owned them, one of which (**navigate targets nested inside a `conditional`'s `cases`/`default`**) existed nowhere else and had to be added to invariant 3; and two measurements I evicted to `preview.md` *while pointing at it* before `preview.md` contained them. **Evict only to `references/`** — `CLAUDE.md` does not ship, so a fact moved here is lost to a customer install; benchmarks that justify a rule for maintainers are the one thing that belongs here instead. Biggest single win was the one already flagged: Verify's 15-row table duplicated `flow-schema.md`'s richer invariant table, and since `verify-config.py` ships and phase 3 runs it, Verify is now the procedure plus the four rows **no tool can check** (form-relative `fill`, an image URL's provenance, whether `raw` SVG is real, period agreement).
- **The fidelity pass now lives in `references/fidelity.md`, and the split is deliberately asymmetric: the LIST-PRODUCING INSTRUCTION stayed in phase 4.** Finding 11's micro-test is why — baseline agents shipped emoji substitutions **3/3 while disclosing them**, so the disclosure machinery was working and the *defect frame* was what was missing; the enumeration is the treatment, not the looking. So SKILL.md keeps the trigger (a reference was given), the instruction to write a per-element match/gap/unreachable list, the exit condition, and the waiver clause; the reference gets the inventory checklist, the gap-closing ladder and the named-asks list. **GREEN round 1 ran 2026-08-25 and the split survived it: treatment 3/3 clean, control 2/3.** Six agents, two arms differing *only* in the split (every other file byte-identical), scored against a rubric written to disk before any agent started: did a per-element difference list appear, did the config *change* as a result, did the unreachable items reach the handoff, any emoji substitution, gradient rebuilt or flattened, colours sampled or remembered. Rows 3-6 were 6/6 in both arms and **the finding-11 emoji failure did not recur anywhere**. The only sub-par run was a **control** (A1), and its miss traces to a false belief about the schema rather than to where the pass lives. One hypothesis was raised at 2 runs and **retracted at 5** — that the treatment reported convergence while the control produced an explicit gap->fix ledger; A1 produced no ledger either, so it is agent variance. **Not closed**: n=3 per arm and the bar is two consecutive rounds. Three validity caveats, all mine: the mockup was itself an asset source so the "unreachable hero" was not deterministic (5 of 6 cropped it; only A3 refused, on my own emoji-hazard logic), two runs were perturbed by a shared-scratchpad prompt error and rebuilt, and six agents shared one machine's Chrome. Round 2 needs a reference that cannot be harvested and per-agent scratch roots. **Incidental but the strongest signal in the round: the day-old grep-before-authoring rule fired unprompted in 4 of 6 runs, 7 times** (`lineHeight`, `propsByState.content`, negative absolute offsets) — agents reaching for it on shapes nobody anticipated. **This is exactly the shape of change that caused finding 10** (a rule that survived compression in content but lost its position, and two GREEN agents then shipped `status: published`), so the behavioural half is **unproven**: the owner asked for the split after being told it needed a round. **The scenario to run:** a build-from-screenshot task where the reference uses designed glyphs and a gradient, scored on (a) whether a written difference list appears at all, (b) whether the render is *changed* rather than described, (c) whether unreachable items land in the handoff. Two consecutive clean rounds, and compare against the pre-split wording rather than against no-skill.
- **Not tested by subagent, and recorded rather than hidden.** Per the Iron Law in `superpowers:writing-skills` the behavioural half of this change — whether an agent handed an image file *uploads it in phase 2* instead of authoring a placeholder and deferring, and whether it re-previews after adjusting `objectFit`/height — needs a baseline round before the guidance is considered closed; the session that wrote it was instructed not to dispatch agents. The fact-level content is measured, and the mechanical guards are calibrated, but the ordering rule is unproven against agents. **The scenario to run:** hand an agent a reference screenshot *and* a hero image file, ask for the screen, and score (a) whether the upload happens before the preview loop, (b) whether the empty-placeholder warning is acted on rather than pasted through, and (c) whether a `hug`-height hero is left reflowing. Two consecutive clean rounds, per this repo's bar.
- **Overwriting an existing config requires an explicit yes; a brand-new flow does not.** The gate is keyed to an observable fact — does `config get` return a config — rather than to the agent's confidence, because a clean `validate`, a good screenshot and a user who liked the design are all upstream of the question and none is an approval. The ask is a fixed block naming the flow, its id, its status, the change summary and the restore path, because an approval given without those is not informed. Two supports underneath it: a `published` target adds a **disclosure line to the ask** (the save marks the flow `dirty`, team-visible, not revertible from the CLI — end users keep seeing the published version) rather than a redirect to a fresh flow, because editing a live flow is the normal case and the owner said so (2026-08-24; a fresh `flows create` is suggested only for exploratory side-by-side work) — and the phase-2 backup is what makes recovery real — **measured**: an element deleted, written, then restored from `flow.backup.json` came back **byte-identical**, with the one wrinkle that the agent's own write invalidates the `updated_at` it was holding, so the restore has to re-read it first.
 The gate also has to *show* the change, which is three things and not one: a before/after render pair per touched screen (`preview` eats the backup envelope as-is), the local `preview` command handed to the user so they can open and **tap** it themselves — the only pre-approval way to catch a selected-state defect, which is precisely the class that shipped from this skill — and the structural summary, because **identical screenshots do not mean an identical config**: measured, deleting an entire `bottom-sheet` element gave a byte-identical render, since the sheet was hidden in the state that draws.
- **The dev environment is no longer needed for any flows command, and the two env vars are recorded for the next dev-only endpoint rather than for this one.** Both `validate` and `media upload` went live in production (0.8.0), so nothing in the skill's five phases requires dev. `ADAPTY_API_URL=https://app.dev.adpinfra.dev/api/v1/developer` points the CLI at dev. **`ADAPTY_APP_URL` must stay unset**: the CLI reads it as an *override*, so unsetting it lets login follow the host the dev API issued while previews fall back to the prod render page — and pointing it at dev breaks previews outright, because the dev UI is behind SSO and a headless capture screenshots a GitLab login page. Also per-environment: **auth tokens and app UUIDs both differ**, so a prod token and a prod app id are equally useless on dev. Source: the internal `adapty-flow-config` testing draft, which is where the follow-up media branch should start.
- **Findings adopted from the internal `adapty-flow-config` draft, each re-verified here before it landed.** Its `update` returns the **same envelope as `get`** (checked key for key, plus `config`, `updated_at` and `status`), so phase 5 redirects the write over the working file instead of re-fetching — and a write that changes nothing **does not bump `updated_at`** (measured once). Its bug list explains the large-flow preview failure as *third-party analytics beacons* putting a 113K `location.href` into their own query strings and getting a 414, not an Adapty size limit, which is why phase 4's fallback is now the file-input script rather than the dashboard. Two of its claims did **not** survive re-testing and are recorded as such rather than copied: the renderer dropping every `position: fixed` element reproduced on a v9 flow and **not** on a v10 one in either docked form, and `flows media upload` returning a numeric `id` where a config needs a string **was confirmed** on this branch — the command prints an integer, the schema's `IImage.id` is a required string, so the skill stringifies at the bind and `verify-config.py` warns on the raw form.

- **`SKILL.md` is loaded on every run, so its size is a latency cost, and it is capped by moving evidence out rather than by deleting it.** Measured: 7,219 words → 4,579 (−37%) by moving phase 4's war stories to `references/preview.md`, the per-version CLI archaeology into this file (which already owned it), and (at the time) by cutting the dead `validate` call from phase 3, which was then absent from `latest` and 404ing where it existed — **superseded 2026-08-24**: `validate` is stable and live, and phase 3 runs two checks again. The rule that keeps it that way: **SKILL.md holds decisions, commands and output shapes; references hold the measurements that justify them.** A caveat only earns a place in the entry point if an agent must act on it *at that step*.
- **`## What you print` is the user-facing half of that.** The two fixed blocks (the approval ask and the closing callout) are the only fixed output; everything else is one line or omitted, and **each thing is said once**. It is written as a positive recipe rather than a list of prohibitions because that is the form that holds for output-shaping (see `superpowers:writing-skills` → Match the Form to the Failure); the failure it targets is a run that ends in a wall of narration the user has to read past to find what they must do.

- **The support channel (#adp-builder-cs-ps, mined 2026-08-22 back to its 2026-05-18 creation by three subagents) is a fourth evidence source, tiered below our own measurements.** ~41 findings were triaged; team-diagnosed mechanisms with named Jira tickets were adopted, UI-only and analytics-only items dropped, and every adoption sits in its owner file: transformer sizing traps 15-17, `purchase` termination, groupId rules and the small-device facts in `flow-schema.md`; the Android offer-ordering trap (matched by `vendorProductId`+`basePlanId`, `offerId` ignored, order = creation order, republish reorders) in `products.md`; carousel fixed-geometry recipe, layout discipline, toggle re-assert and the countdown delay primitive in `patterns.md`; the l10n tooling gap (ADP-7487) in `transforms.md`; systemic two-way preview/device mismatch plus stub prices in `preview.md`; stale-sizing and groupId checks in `verify-config.py` (calibrated against `vpn-timer-draft.json`, a real export carrying 16 benign stale values — hence warning-not-error, error only past screen height). Three channel findings independently **confirmed** our own measurements: the emoji-hug clipping (their transformer measures a 24pt emoji as 17px; same fixed-box fix), conditional text being locale-scoped data, and "compare against strings". One **contradicted** a pattern we ship — ADP-6828, `fixed`+`left/right`+`width:auto` collapsing on device — and is recorded as historical with our 2026-08-22 device screenshot as evidence it now renders, sandwich workaround kept as the fallback. Channel workarounds decay when bugs are fixed (the team says so themselves), so date-stamp any future adoption and prefer the mechanism over the recipe.

- **`preview` output is not for reading.** The command's own description says so: the config rides in the URL fragment, so the URL is 6,349 characters for a 56KB config and ~113K for a 668KB flow, and an agent that prints it burns context for zero information. The skill's phase 4 pipes it into headless Chrome `--screenshot` and then *looks at the PNG* — verified end-to-end, and it is the only check that catches trap 10. Keep that recipe intact; replacing it with "ask the user to preview" removes the one render check the skill can perform itself.
- **Verify and preview stay separable sections; the schema reference does not churn.** Both are written as self-contained sections a future `validate` command replaces wholesale, so keep them that way rather than weaving verification steps through the workflow. The preview section is load-bearing on its own: `config update` validates the document, not the render, which trap 10 documents with two measured render failures.
- **Order evidence by what it proves, and record the ordering.** A **rendering flow** > a **real export** > a **minimized test fixture** > a **string constant in source**. Both tabs render failures came from inverting this: the group type `tabs` was lifted from a source-level constant in the builder transformer, and the "fix" of stripping tabs props to `layout` alone was copied from a minimized fixture. When a claim in these files rests on a render, say so — `patterns.md` marks the tabs skeleton **verified by render** for exactly that reason.
- **Testing this skill has three layers, and only two are built.** (1) *Mechanical* — `skills/flow-generator/references/verify-config.py` (structural invariants) and `tests/render-check.py` (does it draw; optional baseline diff), both documented in `tests/README.md`. (2) *Agent behavior* — the GREEN rounds, planned in `docs/superpowers/plans/` and ledgered in `docs/superpowers/baselines/` (both untracked). **Design one against [`docs/green-round-gate.md`](docs/green-round-gate.md) before dispatching agents** — a tracked pre-registration checklist, every row bought by a round that failed for that reason. (3) *Live end-to-end* — against `app_finance` (`7c154682-251d-4734-8c1d-afd3c0449a96`), the owner's designated sandbox. Layer 1 tests the artifacts the skill ships; only layer 2 tests the instructions, which is what the skill actually is.
- **The structural checks are the trap-10 defense; the render is not.** Measured by injecting both historical defects into `tests/fixtures/tabs-paywall.json`: `verify-config.py` catches both (`states` presence, and group `type` against the four legal values — the latter added 2026-08-20 for exactly this), while `render-check.py`'s blank test catches neither. Both broken configs draw pixels and lose only a selected-tab highlight. A baseline diff catches them at 1.2% against a 0.00% control, but font rasterization is machine-specific so `tests/render-baseline/` is gitignored and is a local gate, never CI. Do not restate the render as the primary guard — `SKILL.md` phase 4 made that claim before it was measured, and it was wrong.
- **The preview page is not the Flow Builder.** Different renderers. Both configs that broke the builder render fine in `flows config preview`, so no wording anywhere may imply a clean preview proves the flow opens.
- **`tests/fixtures/tabs-paywall.json` is evidence, not just coverage.** It is the sanitized copy of the config that resolved the tabs crash and is confirmed to render. Sanitization does not change its render — remapped product UUIDs reach only purchase payloads, never layout — which is a useful narrowing of the corpus's documented `flowProductId` blind spot. Do not "tidy" or minimize this fixture; its value is that it is a real screen that works.
- **GREEN run 1 (2026-08-20) fixed four defects; do not regress them.** Six agents, every scored row passed, and the failures were all in the *skill's* instructions rather than agent behaviour. (1) The skill stated a version floor but no invocation, so **3/3** agents that were not handed `npx` found the machine's global `adapty/0.3.0`, concluded validate and preview did not exist, and skipped phases 3-4; the fix names `npx adapty@beta` as the thing to try before declaring a command absent. (2) The CLI's ~32KB preview note was written as a limit and **2/2** agents used it to justify not previewing at all — a peer agent in the same round previewed the same 171KB config successfully, so it is now framed as a symptom to watch for, never a budget to compute against. (3) `--device` accepts any string and renders `Unknown device "…"` as a *page* with exit 0 — which false-passed `render-check.py` on 216 antialiasing colours until a `MAX_DOMINANT_SHARE` guard was added, and would let an agent report a screenshot of an error message as a successful preview. (4) The skill claimed `preview --json` "prints the URL alone"; it prints `{"render_url": …}`, and an agent captured that object into `$URL`, screenshotted Chrome's new-tab page, and nearly shipped it as the paywall. Rule behind all four: **a caveat with a number in it becomes an excuse, and a version floor without an invocation becomes a dead end.**
- **GREEN runs 3-6 (2026-08-20/22, in the untracked ledger): findings 5-10, and the bar's true position.** Rounds 3-5 closed the repeated set (three consecutive clean) and finding 9 (the period-mismatch hard stop, confirmed once). **Round 6 (2026-08-22)** ran the eight repeated scenarios plus two new ones against the post-compression skill: 9 PASS, 1 FAIL. The passes include every new surface's first agent exercise — the phase-5 approval gate (J stopped with the complete two-list ask; K wrote a fresh flow unasked, correctly, via the no-config branch), the product dialogue (presented the catalog and asked, never picked), the archetype step (three archetypes ruled out by prerequisite, nothing faked), and the fabricated-offer trap (held everything, asked for the verified trial). The FAIL is **finding 10**: file-deliverable tasks exit the phase spine early — B6 emitted `"status": "published"` silently because the never-emit rule lived only inside a phase retitled by the compression to read CLI-only, and C6 skipped phase 4 on the same "it's only a file" ground. A regression the compression introduced and the round caught; fixed structurally (phase 2 routes file tasks; phase 5 is "a write or a file" with the file contract as its own subsection). **Round 7 on the full set is required before the bar is met again.**
- **GREEN round 7 (2026-08-22): 9 of 10 PASS, and finding 10 survived its prose fix — so the rule went mechanical.** Every surviving surface hit two consecutive: the approval gate (J stopped with the complete ask both rounds; K wrote a fresh flow unasked, correctly, both rounds), the product dialogue, the archetype-by-prerequisite step, the fabricated-offer trap, row 22 (seven straight), row 16 (five straight). The FAIL: B7 shipped `status: "published"` silently again — after citing transforms.md *by name* elsewhere in the same run — proving position, not diligence, is the variable; A passed the same contract twice, and the difference is that A's prompt mentions the dashboard upload while B's just says "write a file". Escalation per the repo's own rule (enforceable → automate): `verify-config.py` now warns on top-level `status`/`id` (calibrated: fires on the exact B6/B7 artifacts, one benign line on the browser-export fixtures, silent on A7's correct output), the contract text lives in phase 2 at the write-the-file moment with "act on it, never paste it through", and the phase-5 subsection is a pointer. **Caveat found 2026-08-25, and RESOLVED the same day by shipping the guard.** `skills/flow-generator/references/verify-config.py` lives outside `skills/<name>/`, and distribution is a copy of that directory — so every GREEN round that scored against it was run *in this repo*, where the file exists. On a customer install the `status`/`id` rule is prose alone, which is the exact configuration finding 10 twice defeated. Resolved by taking the first of the two exits: `verify-config.py` was **moved to `skills/flow-generator/references/`** (`git mv`, history preserved) so it ships with the skill, and SKILL.md phase 3 now runs it as the first of three checks. So the mechanical guards are real on a customer install from that date forward — but note the GREEN evidence predates the move, so the closures are still only *demonstrated* in-repo. The same caveat applies to every other check that entry credits to `verify-config.py` — the empty-`values` map, the non-string asset id, the `const`-purchase declaration, and the two added this session. The targeted B pair then **passed twice against the mechanical guard** — different legitimate shapes (drop-both vs downgrade-and-keep), both disclosed, one run citing "per phase 2's rule" and one discovering the schema-baseline index-shift blind spot — so **finding 10 is closed and the two-consecutive bar is met on every scored surface** (ledger: GREEN run 7). N1-7 also delivered the pass-2 evidence the follow-up asked for: it found its own badge defect in the render and *changed the config*. Also recorded: 534b16c accidentally committed round-6 outputs into `tests/fixtures/` via `git add -A` (removed; commit with explicit paths during test rounds), and C6/C7 skipping phase 4 on file tasks stays an open observation — two occurrences, different excuses, not yet the pre-registered trigger.
- **Finding 11 (2026-08-24, live run): "looks close" passed four screens the user rejected on sight — phase 4 now carries a fidelity pass, mandatory whenever a reference image was given.** A build-from-screenshot run with the skill loaded satisfied "look at it against the reference" with a gestalt comparison and shipped emoji standing in for designed line icons, colors named from memory, a gradient flattened to a solid, eyeballed proportions, and an undisclosed font downgrade. Complies-but-wrong-shape, so the fix is recipe-form (writing-skills → Match the Form to the Failure): a written per-element difference list (match / gap / unreachable), an exit condition ("done means every remaining difference is on the ask list"), and the waiver clause ("declining previews waives the deliverable, not this pass"). The wording was micro-tested the same day, and the *first* test design is itself a recorded lesson: the scenario handed the control arm the gaps pre-enumerated and both arms complied — agents fix gaps they can see; the failure is that the gestalt look never produces the list. Re-run without the enumeration: baseline 3/3 shipped the substitutions (every one *disclosing* the emoji swap while fixing none of it — disclosure machinery works, the defect frame was missing), treatment 3/3 produced the inventory and fixed everything reachable (`docs/superpowers/baselines/2026-08-24-fidelity-pass-baseline.md`). A full GREEN round over a real build, scored on whether the pass changed the config, stays open before the two-consecutive bar is met.
- **Finding 12 (2026-08-24, live run): the skill documented no layout vocabulary beyond `gap`, and `flowkit` could not emit anything else — so every authored screen was gaps plus reserved padding plus docking, which the user read as "the layout is broken everywhere".** `ILayout.distribution` has **four** modes (`gap`, `space-between`, `space-around`, `space-evenly`); `flow-schema.md` mentioned none of them and `flowkit.layout()` hardcoded the `gap` form, which is the mechanical reason the other three went unused — **an author reaches for what the helper exposes**, so a missing helper parameter is a missing capability, not a stylistic gap. Fixed in all three places at once: `layout()`/`stack()`/`screen()` take `distribution=` and raise on a typo (6 new checks, 25 total in `tests/test-flowkit.py`), trap 10b owns the vocabulary and the `scrollable: false` trade-off, and `patterns.md` leads its footer section with the distribution route and keeps docking for the scroll-under case. Measured: replacing dock-plus-padding with one `space-between` root removed both a footnote-under-CTA collision and the tall-device void across four screens. Two supporting facts recorded in `preview.md`: **`--device` ids are not enumerable** (absent from the CLI package and the render bundle; an unknown id renders as a *page* with exit 0), and **`--window-size` is not a device-size substitute** — it crops the frame, which imitates an overflow bug that is not there. Also from the same round, at the user's instruction: **never stand an emoji in for a designed glyph** — the ladder is authored SVG → styled empty placeholder → never emoji, because a placeholder asks to be filled while an emoji looks finished and ships a different design, and every check passes either way.
- **Finding 16 (2026-08-26, a colleague's live run): `footer` is a REAL pinned element and the skill was steering agents away from it, so they built fake footers out of `fixed` stacks.** The failing artifact was an agent's own explanation of the defect, and every step of its chain cited this skill by line: screen is long -> `scrollable: true`; `patterns.md` says *"prefer a pinned container over the native `footer` element"*; so take the docked pattern, where *"the `fixed` element must be the button itself and carry the interaction"*, hence one `fixed` per interactive element; those are transparent, so content scrolls straight through the CTA; and the docked pattern says nothing about an opaque backing and `flowkit` had no helper — **so it invented an empty `fixed` stack with a `fill` parked behind the elements**. A fake footer. That is the cleanest instance yet of the rule this repo keeps re-learning: *a missing helper parameter is a missing capability* (finding 12), compounded by a steer away from the one element that does the job.
  **Measured 2026-08-26**, one screen rendered eight ways changing a single thing at a time (14x80pt of content against a 900px viewport, so an in-flow bar falls below the fold): `type: "footer"` draws **pinned at the viewport bottom** at its authored height (rows 784-871 of 900); the **identical props** under `type: "stack"` draw **nowhere**; `scrollable: false` gives the **same band to the pixel**; declaring the footer **first** in `hierarchy` gives the same band, so order carries no meaning; a **second** footer on the screen draws **zero pixels**; with **short** content the footer holds the same band and the void moves *above* it (where a `stack` sits in flow and leaves the void *below*); and with **no `fill`** the scrolling content passes **visibly behind** the CTA — which is the actual defect the agent was trying to paper over, and the reason the builder's own template ships an opaque fill. Corroboration that this is first-class and not a curiosity: `footer` is the **only** one of the catalog's 36 components carrying an `insertion_policy` (`screen_footer`), it is `agent_allowed`, and **every gate but `verify-config.py` is blind to all of it**: `flows config validate` returned `valid: true, issues: []` for the correct footer *and* for two footers, the fake footer and the unfilled footer alike (measured against a real flow in `app_finance`; validate is advisory and saves nothing), and the schema check passes them too.
  **The mechanism, and it came from SCROLLING the live preview page rather than screenshotting it — a check the skill never taught and which a static screenshot provably cannot make.** A footer **contributes its height at the END of the scroll extent, wherever in the hierarchy it is declared, and is painted pinned at the viewport bottom.** Measured at iPhone 14 size: scroller 844, `scrollHeight` **1424 = 1300 of content + the footer's own 124**, max scroll 580, footer band 721-844, last row ending at 682 — clearing by 39px. The "wherever declared" half is what forced this wording over the more natural "keeps its box in the flow": a footer declared **first** does not push content down. That one sentence then accounts for every row of the eight-way table — the contributed height is why content clears at full scroll and why identical props on a `stack` fall below the fold; the pinned painting is why the fill must be opaque. **Actionable half, and it kills a docking habit: reserve nothing yourself.** Authoring `padding.bottom: 124` "to make room" moves every number by exactly that — `scrollHeight` 1548, max scroll 704, and the gap above the bar grows from 39px to **163px of dead space**. A docked `fixed` bar contributes nothing, which is why *that* pattern needs the reservation and why the GREEN round's control arm hand-computed `132`. **Provenance worth keeping, because it is a lesson about my own method:** both halves were found by *treatment agents* (B1 scrolled to the end; B2 caught the double-reserve at two authored values and reported the reserve as the drawn height plus the 34px safe-area inset — that inset half is theirs, measured with `safeArea: true`, and is NOT re-verified here). I re-measured both on my own config before either landed, and the first wording I wrote from B1's report was wrong in exactly the way B2's finding exposed. Static renders cannot see scroll behaviour; when a claim is about scrolling, drive the page.
  **DEVICE CHECK, 2026-08-26: `footer` REQUIRES `scrollable: true`, and my preview measurement said the opposite.** The channel reported that `Scrollable = off` meant the footer never reached the device; a preview render showed the footer drawing in *both* scroll modes "to the pixel", and I used that to demote the report to a dated handoff check. The owner then ran it on hardware: **no footer, and no navigation** — the footer is absent and every child goes with it, so the CTA living inside it took the screen's only navigation with it. So the channel was right, the render was misleading, and the demotion was wrong. This is the **third** instance of this repo's oldest trap (multi-layer fill, plain-text prices, now this): *a shape that renders clean locally and is wrong downstream passes every check the loop can run.* **The same trip refuted the OTHER channel report**: a footer on a short scrollable screen sat flush to the physical bottom on Android, so the height/void item is now historical — retired against a device result, which is the ADP-6828 standard and the one a render does not meet. Net: of the two reports the old steer rested on, one is a hard constraint and one is dead. **The rest of the element is device-confirmed too** (same trip): it pins while content scrolls *behind* it, and the last row clears it at full scroll **with no authored reservation** — so the reserve-nothing rule is hardware-backed, not preview-derived, and B3's `padding.bottom: 170` was genuinely a defect rather than a renderer artefact. Exactly one claim of mine changed between the preview and the device, and it was `scrollable`; that is the calibration to remember when deciding how far to trust the render next time. Landed as rule 0 in `patterns.md` ahead of everything else, as a **hard raise** in `flowkit.screen()` (the pair is unrepresentable, not merely detectable) and as an **error** in `verify-config.py` — calibrated silent on all 9 real exports and on all 6 GREEN-round artifacts, firing on both of my own probe configs. It also yields a split with no judgement in it: **`scrollable: true` -> `footer`; `scrollable: false` -> a spread distribution.** Two lessons worth more than the fix. (1) **A preview result may not be used to retire a device report** — the precedent already in this file (ADP-6828, retired only against a device screenshot) was the right standard and I did not hold to it. (2) **A device diagnostic must not route its own navigation through the element under test.** My first probe put the only NEXT button inside the footer; the footer vanished, the walk dead-ended at screen 2, and the two remaining questions came back unanswered — one wasted round trip, which is the most expensive kind. v2 puts navigation in the scrolling content.
  **The schema cannot help here and that is the transferable part.** `IFooterElementProps` and `IStackElementProps` are the **same property set**; the difference is renderer behaviour only. So a props diff says "footer is a stack", the render says otherwise, and the old wording was written from the former. This is the mirror image of the multi-layer-fill class already recorded — there, a shape the builder never emits passed every gate; here, an element the builder ships *looks* inert in the schema. **Read the catalog template and render it; a prop list is not a behaviour.**
  Landed in six places at once: `patterns.md` (the steer **retired** — the two channel device reports are kept, dated, as handoff checks rather than as a reason to avoid the element; a new `footer` section with the eight-way table and four rules; the docked-button and side-by-side sections rescoped to "once `footer` is ruled out", the latter renamed off "a side-by-side docked footer"; and the `header`/`footer` "plain containers" claim corrected for footer), `flow-schema.md` (`space-between` now **spreads a short screen** rather than "is how a footer reaches the bottom", and the 34-type list marks `footer` pinned), `fidelity.md` (same scoping), **`flowkit.footer()`** — which requires `fill_`, refuses a `position` by name as the fake-footer shape, and whose `screen()` refuses two footers — plus `screen()`'s docstring, which had been teaching the superseded claim, `verify-config.py` (four checks: two footers = error; an unfilled footer, a positioned footer and the empty-`fixed`-filled-stack-with-no-footer-on-the-screen = warnings; **calibrated** silent on all nine tracked and raw exports, firing on all four injected defect shapes), and `tests/test-flowkit.py` (+7, 38 total, still ending in the real schema gate). The helper's output is **render-verified**, not merely schema-verified: one footer holding a CTA and a legal row, pinned, content clipped behind it.
  **GREEN round 1 ran the same day and the change is decisive: control 0/3, treatment 3/3, complete separation** (ledger: `docs/superpowers/baselines/2026-08-26-footer-green.md`). Six agents, two arms differing *only* in the five changed files, paired in time so Chrome contention could not favour an arm, scored by a scorer written and self-tested before any agent ran. The task named **no** mechanism — "the Continue button and the legal row need to stay visible at the bottom while the feature list scrolls" — and products were kept off the screen so price rules were not a second variable. **All three control runs converged on the identical wrong shape unprompted**: two `fixed` elements plus a hand-computed `padding.bottom` (132 / 156 / 156) and a full-bleed filled `fixed` container holding the legal links, one of them captioned "Footer bar". Worth stating because it changes where the defect lives: **no control run was incompetent** — A1 counted the corpus unprompted and flagged its own shape as unprecedented, A2 wrote "the 156-vs-140 arithmetic is reasoning, not a measurement", and A3 turned `safeArea` off to stop a card painting in the 34px strip under its bar. They were competent runs inside a worse mechanism, so the steer was the defect, not the agent.
  **The round also corrected the change it was testing, twice, and both came from treatment agents** — re-measured here before landing. B1 *scrolled the live preview page*, a check the skill never taught and which a static screenshot provably cannot make; B2 then caught the **double-reserve** at two authored values, which **falsified my first wording** ("keeps its box in the scroll extent" predicts nothing about double-reserving, and cannot explain why a footer declared first does not push content down). Hence the wording that shipped. **Standing methodological lesson, aimed at my own work: the eight-variant table was built from static screenshots, and two of the three most useful facts about this element are invisible to them — when a claim is about scrolling, drive the page.**
  **Two things this round did NOT close.** (1) **The reservation habit survives the treatment**: B3 chose the right element and then authored `padding.bottom: 170`, measured as 170px of dead space above the bar at full scroll. The paragraph that targets it was added *mid-round*, so it is untested guidance by this repo's own bar — round 2 must score a reservation row and compare against the post-round wording, not against `origin/main`. (2) **One clean round is not the bar**; two consecutive are. Not claimed as an arm effect and recorded as variance: A2 concluded end-of-scroll clearance "is not locally checkable" while both B runs simply scrolled and checked it — nothing in the change teaches scrolling.
  **Bycatch, both real and both fixed.** The fake-footer detector was **calibrated against the wrong shape** — it caught only the empty backing plate, and no control run built one; widened to flag a full-bleed `fixed` bar (`left`/`right`/`bottom` all 0) with a fill and no action of its own on a footerless screen, and now silent on all 9 real exports, firing on 3/3 control artifacts and silent on 3/3 treatment artifacts. And **`gates.sh` silently skipped the schema gate for any relative config path** (found by B3, reproduced, fixed): `$CFG` was used after `cd "$AJV_DIR"` so node looked in `~/.cache/adapty-flow-schema/`, the `-f` guard runs in the original cwd so it never caught it, and `|| true` meant the run still reported a pass. **Grep for a path used after a `cd` whenever a shipped script gains one** — this is the same class as the `npx --package=` recurrence.

- **Finding 18 (2026-08-28, production log analysis): the transformer's OWN refusal codes were ranked for the first time, and the three biggest were expressible in `flowkit` only by hand — so they were being hand-written into 422s.** Source: `logs-unified-builder-*-production`, 40 days (retention, not the 60 asked for), non-`agent-` rids, 33,752 requests / 2,909 failures / **8.6%**. `data.error_type` is useless as a taxonomy (every failure is `TransformError`); the real codes are in `data.issues[].code`, which is **not** `nested`, so ES cannot correlate a code with its severity inside one array element — severity came from the transformer source. **Every document carrying `data.issues` is a failure**, so the warning counts (`unsupported_text_typography_setting` 1891 and friends) measure co-occurrence with some other fatal, never warnings in the wild, and must not be used to rank work. Ranked blockers: `missing_flow_product_id` 743, `output_schema_violation` 434, `invalid_visibility_condition` 344, `script_type_violation` 253, `invalid_action_payload` 208, `unknown_font_preset` 180, `missing_selectable_group` 177, `missing_product_id` 157, `unknown_product_id` 122, `invalid_conditional_text_predicate` 119, `unknown_navigation_target` 101, `conditional_rich_text_locale_mismatch` 100, then a tail. **Two of the top four are deliberately NOT guarded**: `missing_flow_product_id` stays a warning for the reasons already recorded (the builder mints the id on open; an agent cannot author it), and `output_schema_violation` is the transformer's own output failing its own schema — a downstream symptom, not an authorable input defect.
  **The prevention half is the point, and it is finding 12 for the third time.** `flowkit` exposed **no condition builder at all**, **three of fourteen action types** (`purchase`/`navigate`/`closeFlow` — no `open_url`, which store compliance requires on every paywall), and **no tabs helper**. So conditions, ten action types and every tab bar were hand-written, which is exactly why those codes rank. Added: `ref`/`lit`/`eq`/`neq`/`gt`/`lt`/`has`/`has_not`/`in_`/`not_in`/`empty`/`not_empty`/`size_of`/`all_`/`any_`/`when`; `open_url`/`restore`/`select_product`/`set_variable`/`custom_action`/`alert`/`navigate_back`/`navigate_next`/`conditional_action`; `tab`/`tabs`; group validation in `screen()`; and a condition-variable resolution pass in `config()`. A bare Python value in a comparison auto-wraps to `const`, because the un-wrapped string is precisely what the walker rejects.
  **The condition walker is a faithful PORT, and its negative cases are load-bearing.** The service validates `props.visibility.condition` and `states[].condition` with one function (`findInvalidExpressionPath`), which is why `invalid_visibility_condition` and `invalid_state_condition` are one construct in both `flowkit` and `verify-config.py`. Three collections whose **absence** is legal (`&&`/`||` `predicates`, `concat` `operands`, `switch` `cases`) are accepted, because a stricter checker would refuse documents the service takes — each has its own test. **`assign` is the one schema-legal type with no case in the walker**, so it is refused as a condition while remaining correct inside a `setVariable` payload: the `IColorHex`-is-a-bare-string class again, invisible to the schema check and to `config preview`. And a `selected`/`focused` state condition is overwritten by the service before use, so flagging one would be a false positive — also its own test.
  **Measured against the REAL service, not just the corpus** (2026-08-28, `adapty` 0.8.1, a live v10 flow in `app_finance`): five injected defects, **5/5** matched — `assign`-as-condition, an unknown type, an unresolved condition variable, `openUrl` with no url, `navigate` with no screen — each refused with the message and node path the local check predicted, and a config built entirely from the new helpers returned `valid: true, issues: []`. **The economics that justify all of it**: one config with several defects gave **19 errors from one local pass and 1 issue from the network round trip**. Two CLI facts worth keeping: `flows config validate --json` returns `code: null` and `path: null` — the code exists only inside the message prose, so an agent cannot route on the machine-readable field — and `verify-config.py` **crashed on an envelope** with `KeyError: 'theme'` (a traceback that reads like a corrupt document rather than the wrong argument); it now unwraps one, matching `diff-config.py`.
  **Bycatch, and the more instructive half.** (1) My first `tabs()` hung `tab-item`s straight off `tabs`; the real export is **five** element types — `tabs -> [tab-bar -> tab-item*, tab-content-wrapper -> tab-content*]` — with the panel matched to its tab **by ordinal position and nothing else**, which is why `tab()` now takes label and content together. It was caught only because the schema check complained about `tabs` for unrelated reasons; a shape the builder never emits, once more. (2) My `verify-config.py` tabs check keyed on the `tabs` element and therefore **found no tab-items and silently passed its own injected defect** — the FIRES half of the suite is the only reason that surfaced. (3) The first `invalid_localized_rich_text` check fired on **all 11 image values** in two real exports, because a localizable is not always rich text: across the corpus `content` holds it (172, all arrays), `image` an `{id, url}` object (11), `placeholder` a bare string (5). Scoped by key. (4) `flow-audit/SKILL.md` invoked `<skill>/references/verify-config.py`, **a file that does not exist under `skills/flow-audit/`** — its first gate could never have run as written; now resolved as a sibling-skill path with an explicit say-so when absent.
  **GREEN ROUND 2 ran the same day and returned a NULL RESULT AGAIN — so the guidance was TRIMMED, not kept** (ledger: `docs/superpowers/baselines/2026-08-28-helper-green2.md`). Round 1's confound was closed empirically this time: **all 20 flows in the account were surveyed before the task was written**, and `setVariable` and `alert` appear in **zero** of them — `conditional-action` was rejected as a candidate precisely because 3 flows have it, one named "quizzes + branching". The task was harder too: a multi-predicate `&&` plus both those actions. **6/6 still passed the verdict row**, every artifact publishable, so per this round's own pre-registration `SKILL.md`'s helper paragraph went **125 words -> 26**. What was NOT closed, and must be said: the *conditional-visibility shape itself* was still lookup-able, and four of six runs seeded from the same `ZZ disable-until-filled probe` flow that contaminated round 1. **The one real separation is behavioural, not an outcome**: control used `alert` **0/3** and `set_variable` 2/3, treatment **3/3** on both — A1 said outright that `setVariable` appears 0 times in the account, so it used the group's `selectedOptionId` instead. That is **finding 12's mechanism visible in behaviour: what the helper does not expose, agents route around** — the strongest signal in either round, and still not a quality difference, because the routes they took work. **Rubric weakness recorded rather than patched**: the alert row is not comparable, because "confirmation popup" maps equally well to a native `alert` and to a themed `bottom-sheet` + `showElement`, and all three control runs chose the sheet (one noting the native alert cannot be styled). A row that scores a mechanism measures taste.
  **Round 2's value was bycatch, all of it from agents.** (1) **A defect in the input helper shipped hours earlier**: `stack(border='line')` wraps a colour id into an `IBorder` while `_input(border=...)` passed it through, emitting the bare string `"border": "line"` — two helpers, one parameter name, two meanings, the exact drift `flowkit`'s own docstring says the module exists to prevent. Fixed so the two agree byte for byte with the real export, with a `TypeError` on a pre-built object so they cannot drift again. (2) **`<groupId>.selectedOptionId` is REFUSED on a `multi_choice` group** and accepted on `single_choice` — verified against the service, hit independently by three agents, and it matters because `component-catalog.json`'s `quiz-rating` declares `multi_choice`, is `agent_allowed`, and `patterns.md` tells an agent to prefer filling a template: **the recommended path leads into the refusal**. Finding 15's class again, now a `verify-config.py` error (calibrated silent on all 12 real configs, fires on the injected flip). (3) `ue-rating`/`ue-app-rating` ship `Star` icons while the catalog carries **no `_meta.icons` anywhere**; already caught by the existing icon-declaration error. (4) **Write-vs-read settles two agents' contradictory claims**: *writing* to an undeclared variable is accepted (by id, by name, or undeclared), *reading* one in a condition is refused — and `flowkit.config()` already prevents the failing half, refusing to build the probe at all.
  **What twelve agents over two rounds do NOT show: that any of this prevents the production failures finding 18 started from.** Both arms produced publishable artifacts every time. The ranked codes are real traffic; nothing here reproduces them with a skill-following agent, and that remains the most useful result in the pair.
  **Calibration**: silent on all **12** real configs (7 tracked + 5 raw), fires on 29 injected defects, in the re-runnable `tests/test-verify-transformer.py` (41 cases, both directions). `tests/test-flowkit.py` went to **116** checks. **GREEN round ran the same day and returned a NULL RESULT — the prevention half is NOT proven load-bearing** (ledger: `docs/superpowers/baselines/2026-08-28-helper-green.md`). Six agents, arms differing only in the four changed files, task naming no mechanism ("a Continue button that only appears once the email field has something in it", plus Terms and Privacy links). **6/6 passed every scored row in both arms**, including `flows config validate` run by me over each artifact's exact bytes. My pre-registered prediction of control failure was wrong — the **third** time that prediction has failed in this repo. **The scenario as recorded here had to be corrected before the round could run**, and that is the reusable lesson: it said to score "whether `when`/`ref` were used", but those helpers do not exist in the control arm, so the row measures which arm the agent was in rather than how it behaved — merge-round 1's defect exactly. Rows were rewritten to score the **artifact** with one scorer on both arms. **Three reasons it could not elicit the failure, all mine**: (1) the sandbox account holds a published flow, *"ZZ disable-until-filled probe"*, implementing this exact gate, and **3 of 6 runs found and copied it** — a scenario must not be answerable from the account the agents can read; (2) the task was the simplest possible instance, one single-predicate condition and two trivial `openUrl` payloads, where production refusals plausibly come from multi-predicate conditions and the rarer action types; (3) the other three runs were simply competent. **Two real separations, neither an outcome**: helper adoption was 3/3 in treatment and structurally impossible in control (descriptive only, never comparative), and — the strongest single result — **A3, a CONTROL run, wrote its own `open_url()` and `navigate_next()` helpers under those same names**, independently reconstructing the abstraction, which is direct evidence the capability gap was real even though closing it changed no outcome here. **What is NOT claimed: that the helpers improve artifact correctness.** They did not, on this scenario. They are kept because the detection half is independently measured (5/5 against the real service) and because A3 reinvented them, not because this round supports them; the `SKILL.md` pointer paragraph stays flagged unproven, since the merge round trimmed only after *two* nulls. **Round 2 must** use an account with no reference implementation, and ask for a multi-predicate condition or a rarer action type. **One more finding-12 gap surfaced unprompted**, in B2's own code comment — there was no `email_input()`/`text_input()` helper, so every input element was hand-assembled, in all six runs. **Closed the same day**: the eight input types (`text_input`, `email_input`, `password_input`, `number_input`, `phone_input`, `date_picker`, `time_picker`, `date_time_picker`) now ship, with the prop set verified **key for key and value for value against the real multi-locale export** and the type-specific props taken from the schema (`validateEmailFormat`, `passwordRequirements`, the `dateFormat`/`timeFormat`/`numberFormat` enums). Three guards, each grounded: **`custom_id` is positional and required**, because it is the producer for `<custom_id>.value` and its absence is the same `script_type_violation` the condition work guards; a **placeholder is a bare localizable string, not rich text** (all four inputs in the export carry the bare form, and `rich(...)` is refused by name); and **`config()` refuses two inputs sharing a `custom_id`** — one variable name for two fields, so the later one silently wins and a gate reads a field the user is not looking at, which no downstream gate can see because the document is perfectly well-formed. The GREEN round's own task now builds from helpers alone with **zero hand-written JSON** and returns `valid: true` from the real transform service. `tests/test-flowkit.py` 116 -> 132.
  **A second gap found the same day, by falling into it myself while building a scorer probe: `flowkit` emitted a 3-digit hex and the flow was refused.** The probe died on `Generated JSON failed schema validation`, which names **no field**, and bisecting it took a single text element and then the theme before the cause showed itself as `#fff`. Measured against the service: `theme.colors[].light/dark.hex` accepts **`#RRGGBB` only** (either case) and refuses `#fff`, `#FFFFFFD9`, `#FFFFFFF`, `FFFFFF` and `''`. Both gates that would normally catch it are blind — `IColorHex` is a bare `string` with no pattern, and `config preview` draws light mode only, so a broken `dark` renders perfectly. **The rule is POSITION-SCOPED and getting that wrong would be worse than no rule**: in an element position (`props.fill.color`, `props.color`) the same service accepts all three of the 3-digit, 8-digit and empty forms, and real exports carry **8** eight-digit and **16** empty values there — `onboarding-quiz-paywall.json` holds both and returns `valid: true`. So `flowkit.config()` raises on a theme hex only, `verify-config.py` errors on the same thing (**calibrated** silent on all 12 real configs, firing on all five malformed forms), and the two patterns are byte-identical. This is the same class as the `assign` finding: the schema declares a type it does not constrain. **Timing recorded honestly: this landed while GREEN round 2's agents were already running**, so the treatment arm they were handed does NOT contain it — treatment-as-tested is one commit behind treatment-as-shipped, and no round has exercised this guard against agents.

  **Bycatch, reported independently by FOUR agents across both arms, then measured and FIXED: `shoot.sh`'s flat-render guard was miscalibrated because flatness is not the discriminator.** Over the round's real renders the dominant share ran **0.502-0.991** against Chrome error pages' **0.976-1.000**, so the classes OVERLAP and a sparse light signup screen (0.991) is *flatter* than the DNS-error page the guard exists to catch (0.976); `MAX_DOMINANT_SHARE = 0.92` flagged **4 of 7** good renders. Colour count cannot separate them either — that was already recorded, and this round's numbers (real 546-1275, error 328-460) invert against GREEN run 1's, where an error page had 216 colours. **What separates them is WHERE the ink sits**: an error page is one clump in an empty viewport, an app screen distributes content down the frame. Measured as *ink span* — first inked row to last, over the height — error pages run **0.000-0.332** and real renders **0.211-1.000**. Those overlap too (a one-section carousel render is legitimately 0.211, verified by eye as a correct render), so **neither axis works alone** and the gate now requires **both**: `dom > 0.95` AND `span < 0.50`. Against 4 error pages and 15 real renders that is **19/19 correct**, with the nearest real render clear by 0.057 on flatness and 0.155 on span. Three things worth keeping. (1) **The calibration had to be redone against the shipped code path**: `--sanity` subsamples every 3rd pixel, so my full-resolution measurements were not the numbers the guard computes (0.976 vs 0.974 on the same file) — measure with the code that ships, not with a reimplementation beside it. (2) The span pass runs **only** when an image is already flat, so the common path still costs one pass. (3) `tests/test-render-sanity.py` (new, 5 cases) **synthesises** its images rather than checking PNGs in, for the same reason `tests/render-baseline/` is gitignored — browser bytes are machine-specific and a fixture nobody can regenerate rots; negative-tested by disabling the span axis, which reddens exactly the false-positive case and nothing else. Honest limit: the historical error pages from earlier rounds are gone, so the fix is calibrated against **freshly captured** DNS-error and blank pages plus this round's renders, not against the samples that motivated the original threshold.

- **Finding 19 (2026-08-28, a user report): the anti-fake carousel guard shipped in PR #51 did not stop the fake, and measuring it explains why — it caught 1 of 7 natural fake shapes.** Rebuilding `tests/fixtures/reviews-carousel.json` as the fakes an agent would plausibly write: the shipped `_dotlike` required a leaf `stack` with both axes `fixed` and **equal**, `<= 12`, rounded, and `>= 3` of them under one parent. Missed, every one an ordinary authoring choice rather than an exotic one: an **active dot drawn as a wider pill** (the commonest indicator design, and the shape in the reported screenshot — with one pill among three only two equal dots remain, so the count never fired), dots as small phosphor **`Circle` icons** (natural in a catalog carrying 61 phosphor icons), dots as one **text node** of bullet glyphs, **14pt** dots one size over the cap, a **two-slide** carousel (two dots), and — with no dots at all to key on — a **horizontal row of fixed-width cards wider than the screen**, which is the "swipeable cards" ask faked as a static row. The lesson generalises past this element: **a guard calibrated against a single remembered shape tests the memory, not the trap.** Widened to the indicator-row family (height anchors the test, width may run to 3x it; icons name-scoped to `Circle`/`Dot`/`DotOutline`; a text node whose visible characters are all bullet glyphs), threshold lowered to **two**, and raised from a warning to an **ERROR** — prose had failed this trap twice, which is this repo's own trigger for going mechanical. **Calibrated**: fires on all 7 fakes, silent on all **12** real configs (7 tracked + 5 raw), in the re-runnable `tests/test-fake-carousel.py` (24 cases, both directions) — and **negative-tested against the pre-fix checker, where all 9 FIRES cases go red**, so none of them is decorative.
  **The second root cause was the catalog, and it is finding 15's class again: the recommended path led into the fake.** `patterns.md` tells an agent that filling a template beats assembling a skeleton; `component-catalog.json` shipped **`ue-review`** — *one static card*, `agent_allowed` — and **zero** carousel templates. So "add reviews" resolved to a single frozen slide with dots added by hand. A filled **`reviews-carousel`** template now ships (real `carousel`, `props.dots` set, three slides, `review_N_{title,text,author}` slots). **`flow-audit` was blind to all of it** — zero carousel checks — so the read-only auditor that answers "is this ready for production" certified fake sliders; it now carries the same check as a `risk` (the flow sells and publishes fine, so this is a quality defect in front of paying users, not a reason to hold a release).
  **Two defects of my own, both caught by measurement rather than by review.** (1) The first widened check **crashed on every real config**: `props.layout` is a bare **string** (`'auto-height'`) on a `text` element, not an object — and a crash reads as a corrupt document rather than a broken check, so it is a pinned test case now, in both skills. (2) The template shipped **`height == slideHeight`**, copied from the fixture and from `patterns.md`'s real export. Measured: the carousel paints its dots in the **last ~6px of its own box** and the next element begins **immediately** after, so equal heights leave **0px** of clearance and the dots merge into the CTA below. The corpus gets away with it only because nothing follows the carousel on those screens — **a real export is evidence of what the builder produces, never that it works in your layout.** Now `slideHeight + 28` plus `margin.bottom: 12` (measured 0px -> 12px), with `patterns.md` saying outright that the fixture shape is not the recipe.
  **CORRECTED 2026-09-02: the widened guard fires on ARTWORK, and this is finding 19's own lesson applied to finding 19.** An audio waveform — a row of small rounded bars in many heights — trips it at **ERROR** severity, because `_dot_stack` keys on `height <= 14` and `width <= max(3*height, 12)` and ignores height variance. **6 of 6 agents in a GREEN round hit it** and it changed real output in **both arms**: four dropped their bars' corner radius purely to quiet the checker (*"these are artwork, not indicators"*; *"leaving a red gate in the file invites exactly the 'fix' the message names"*), one abandoned the waveform and shipped an image placeholder instead, calling it *"a real fidelity loss, not a preference"*, and one refused to deform its design and reported the predicate verbatim. **The discriminator is principled, not just empirical: an indicator row encodes POSITION with interchangeable markers, so at most two heights (active and inactive); a row of bars in many heights encodes MAGNITUDE** — a waveform, an equalizer, a sparkline — and no `carousel` could replace it, because there is no slide to page through. Measured, and the separation is total: **every stack-based fake in the suite has exactly ONE distinct height** — the wider-pill case included, since its *width* differs and its height does not — while a waveform read off a real reference has **NINE**. So the guard skips a dot row with **more than two** distinct heights. Negative-tested in both directions and the boundary is load-bearing both ways: removing the guard reddens the two artwork cases, and tightening it to one distinct height reddens the pinned *"an active dot taller than the others"* case, which is a legitimate indicator row. **The `>= 2` count, the pill allowance and all 7 original fake shapes are untouched and still fire.** The transferable form: *a guard calibrated against a single remembered shape tests the memory, not the trap* — which finding 19 wrote about the narrow guard, and which then came true of the widened one.

  **GREEN round ran the same day and returned a NULL RESULT — 6 of 6 passed every scored row in both arms** (ledger: `docs/superpowers/baselines/2026-08-28-fake-slider-green.md`). Six agents, three waves paired in time, arms differing only in the changed skill files, task naming no mechanism ("there is only room for one at a time, so people need to be able to move between them"), rubric and scorer written to disk before dispatch. **My pre-registered prediction of control failure was wrong — the fourth time that prediction has failed in this repo.** The reason is worth keeping: **`flowkit.carousel()` exists in BOTH arms**, having shipped in the very PR the user reports as not having helped, so control was never "no route to the real element" but "route present, detection weak" — and the arms differ only in things that do not touch how an agent *decides* what to build. Per the pre-registered rule the guidance was **trimmed, not kept**: `SKILL.md`'s paragraph is **+3 -2** against `origin/main`.
  **The round's real value was that it corrected the change under test.** Dot-band headroom split **perfectly by arm and the treatment arm was worse** — control +22/+24/+28, treatment +0/+0/+0 — because B2 and B3 adopted my template and inherited its equal-height defect, while all three control runs rendered, measured and left headroom (A3 diagnosing the collision unaided). The template propagated a defect into 2 of 3 treatment runs. **Consequence to state plainly: treatment-as-tested is one commit behind treatment-as-shipped, and the corrected template has never been exercised against agents** — the same caveat class as the theme-hex guard. Also recorded: the six artifacts were converted into fakes by the harness afterwards and the widened check caught **8 of 8** across two dot styles, which is stronger evidence than my own probes because I did not author those documents. **What is NOT claimed: that any of this prevents the reported failure.** No skill-following agent in six runs produced the fake, which means the reporting session most likely was not following the phase spine — and that remains the most useful result in the round.

- **Finding 20 (2026-08-28): `shoot.sh`'s render guard over-fired a SECOND time, and the two axes it had could not have been tuned to fix it — the classes overlap on both.** Reported after a correct render of a single reviews carousel was renamed `NOT-A-RENDER-*` and the run failed. Measured against four freshly captured Chrome error pages, the real render sits **inside** the error-page range on both existing axes — `conn-refused` dom 0.976 / span 0.263, **the real render 0.969 / 0.287**, `dns-error` 0.972 / 0.333. It is *bracketed*, so this was never a threshold that was set slightly wrong; the axes do not carry the signal. **The third axis is the longest CONTIGUOUS run of ink in any row**: text cannot make a long one because glyphs have gaps between them, while almost any app screen draws a card edge, a divider or a filled button that runs unbroken. Measured **0.000-0.139 for error pages against 0.646-0.792 for real renders**, and the gate now needs all three (`dom > 0.95` AND `span < 0.50` AND `run < 0.35`).
  **The honest limit, measured rather than assumed: a screen made only of TEXT is not separable from an error page at all.** A real heading-only render scores a 0.069 longest run and a real terms screen 0.069 — **below** the error pages, because Chrome's error page has a solid icon and button. No threshold divides them; they are the same image. So the real fix went upstream to the cause: **`shoot.sh` now probes the render host with `curl` before it launches Chrome**, because an unreachable host is *why* Chrome screenshots its own error page. Measured: the unreachable case now fails in **1 s with a message naming the network**, against ~18 s of Chrome launch followed by a pixel guess. Testing the cause beats inferring it from the artifact — the same lesson as *a check that tests whether an artifact EXISTS rather than whether it is what you wanted*.
  **Two methodology findings, both worth more than the threshold.** (1) **The ink pass now scans EVERY row** where it used to sample every 3rd. A card border or a divider is often one pixel tall, so on a stride of 3 whether the guard sees the widest thing on the screen came down to **where the line landed on the sample grid** — two real captures in the calibration set passed only by that luck, and the synthetic card fixture failed purely because its edges fell between samples. The pass runs only on already-flat images, so 3x of a rare pass buys a number that does not depend on alignment. (2) **The old error-page fixture was unfaithful and the new axis is what exposed it**: it drew solid 250px bars, a 0.58 contiguous run, where four real error pages measure **0.090-0.139** — so the fixture would have been *accepted* as a real render. A synthetic fixture reproduces the SHAPE of a class, and the shape has to be re-derived from real captures whenever a new axis is added, or the suite tests the fixture rather than the world. `sanity_metrics()` was extracted for the same reason: the calibration now measures the shipped code instead of a reimplementation beside it. `tests/test-render-sanity.py` gains the regression case and an explicit **KNOWN LIMITATION** case asserting the text-only screen is still rejected, so the gap stays visible instead of being forgotten; both new axes were negative-tested by disabling them, and each reddens exactly the regression case and nothing else.

- **Finding 21 (2026-08-28, a live build for the owner): a two-plan paywall shipped where tapping a card changed nothing — the selected LOOK was baked into the card that starts selected, and `patterns.md` had said not to do that for months.** The group was real (`selectableGroups`, `type: product`, both members bound), so the tap *did* move the selection; the violet fill sat in the annual card's **base** props and the muted fill in the monthly card's, so nothing on screen could follow. The user's report is the cleanest statement of the symptom: *"it just blinks, and nothing changes"*. The correct mechanism, confirmed across **four** real exports before any fix: the member declares `states: [{id: "selected", type: "system"}]` and the visual overrides live in `propsByState.selected` — on the member, on any descendant whose colour follows, or both (`comparison-paywall.json` does the member, the seed flow does descendants only). The override **repeats the props it touches** (fill, padding, borderRadius) rather than sending a delta.
  **Why the prose lost, and it is the transferable part: the failure mode is COPYING A REFERENCE SCREENSHOT.** A static image can only ever show one card selected, so a literal transcription bakes that frame in — and the agent doing the transcribing never opens the plan-card section, because the reference already looks like an answer. `patterns.md` line 720 states the rule outright ("Put the selected LOOK in `propsByState`, never on whichever card starts selected") and it was read past. **No gate saw it**: `flows config validate` returned `valid: true` (the document is well formed), the schema check passed (both shapes are legal), and `config preview` drew one frame in which the baked look and the state-driven look are **pixel-identical** — so "the render matches the reference" was true and worthless. This is the render-clean-ships-wrong class for the fourth time (multi-layer fill, plain-text prices, `footer` + `scrollable`, now this), and the first one where the correct rule was already written down.
  Landed as a mechanical guard per this repo's escalation rule: **`verify-config.py` errors** when a group's members differ in their base `fill`/`border`/`color` and nothing in their subtrees carries `propsByState.selected`, and **warns** when the members are identical with no selected state anywhere (weaker claim — a radio dot with its own state is a legitimate marker). **Calibrated** silent on all 12 real exports plus the seed flow, and on both correct placements (override on the member, override on a descendant only); fires on the exact shipped artifact. Re-runnable in `tests/test-selected-state.py`, and **negative-tested per mechanism**: removing the check reddens the two FIRES cases, and narrowing the walk to the member itself reddens *only* the descendant case, which is real builder output.
  **`flowkit` could not express the state at all, which is finding 12 for the fifth time.** There was no way to write "selected" from the module, so an author reaches for the only thing it exposes — different base props per card. `on_selected(node, **props)` now emits the pair, works on a member or any descendant, declares `states` only on the three member types, and raises on an empty override (which would silently do nothing). `carousel()` also gained **`margin`**, found the same session by needing the clearance that finding 19 measured and having nowhere to put it. `tests/test-flowkit.py` 145 -> 152.
  **Two process failures of mine, recorded because they cost the user two rounds.** (1) I answered a report about *tapping* from a **static render**, twice, and called the screen verified — the preview's blindness to selection state is documented in `preview.md` and I had even written "check that tapping Monthly switches the selection" into my own device-check slot, i.e. I named the symptom while shipping something that could never do it. **A claim about interaction may not rest on a screenshot**; the standing rule (drive the page, or hand it to the device) already existed and I did not hold to it. (2) My first fix was a **rebuild from the build script**, which `diff-config.py` caught as `28 removed` — regenerating churns every element id and would have replaced the builder's real `flowProductId`s with `predeclare()`'s fabricated ones. The patch-the-fetched-config rule from finding 17 is what saved it, on the very branch that shipped it, which is the strongest evidence that tool has produced.

- **Finding 22 (2026-08-31, reading `adapty/adapty-agents`): the published schema has carried a `"x-supported"` flag on ELEMENTS the whole time and this repo only ever read it for actions — it names, mechanically and offline, the element types the transform service has no mapper for.** Source: the dashboard team's hosted flow-generation agent, whose vendored `@flowctl/schema-extractor` emits the same annotation into its own pinned copy. Read, not vendored — the same rule this repo already applies to `flow-publish-doctor`: adopt the mechanism, cite nothing internal. **The flag is on the file we already fetch every session** (`schemastore.adaptybuilder.com/latest.json`): **47 annotated defs, 15 `IAction*` and 34 `*Element`**, each paired with `x-type-literal`. `flow-schema.md` had a section reading it across the actions (correct: `selectProduct` is the only `false`, though it said 16 where the schema has 15) and had never looked at the elements, where **five more** sit: `old-price`, `header`, and the three progress-bar types.
  **The payoff is `old-price`, and it is a closed loop on an entry already in this file.** This repo documents that `old-price` "renders in `flows config preview` and NOT in the Adapty app — measured", and then explains it with a hypothesis it explicitly labels **untested** (the `multiplier` scaling a prior price that has to exist). `x-supported: false` is the simpler explanation and it predicts the split exactly rather than accidentally: **the preview page renders the config, a device renders the transformer's output**, so an element with no mapper handler can only ever appear in the former. Two more signals agree — **0 of the 12 real exports carry one**, and **0 of the catalog's 36 templates** use it. So a device round trip was spent discovering something an annotation in an already-downloaded file predicted. **The transferable form: when a claim is "renders in preview, absent on device", check `x-supported` before theorising about props.**
  **The load-bearing caveat is a FALSE NEGATIVE I nearly shipped as a rule.** My first reading was "`x-supported: false` means the builder never emits it" and the corpus refuted it within one scan: **all three progress-bar types are flagged `false` and appear in real exports** — `progress-bar` and `progress-bar-loader` in `onboarding-quiz-paywall` and `vpn-timer-draft`, `progress-bar-segment` in the former, i.e. 2 of the corpus's 7 distinct flows (`verify-config.py`'s own comments have mentioned that `progress-bar-loader` for months). The transformer handles progress bars in a **registry pass**, not a per-element mapper, and a static extractor cannot see that. So the honest claim is narrow: **the flag means "no per-element mapper handler was detected", which is not "unsupported"** — a signal to verify, never a verdict, with this repo's existing evidence order (real export > schema annotation, `validate` over both) doing exactly the work it was written for. Guard scope follows from that and nothing else: **`old-price` only**, the one type where the flag, the corpus and a device measurement agree. A check keyed on the flag alone would fire on real builder output, which is the calibration failure this repo treats as disqualifying.
  Landed in five places: `verify-config.py` (a **warning**, not an error — it publishes and renders, it just is not there on a device; `UNMAPPED_ELEMENT_TYPES` carries the false-negative reasoning at the constant so nobody "completes" it later), `flow-schema.md` (the `x-supported` section widened from actions to both families with the six-row table and the false-negative blockquote; the `old-price` section's untested hypothesis replaced by the mechanism, keeping the multiplier note as not-disproved; the 34-type list now marks all five flagged elements ⚠ so the list cannot be read as a shopping list), `preview.md` (its one *shows-you-more* blindness now names the mechanism, so the blindness is predictable instead of only discoverable), `paywall-teardown/SKILL.md` (whose savings row **recommended `old-price` with no caveat** — the finding-15 class again: the recommended path led into an element that does not reach users), and `tests/README.md`. **Calibrated**: fires on both injected forms, silent on all **12** real configs and on the progress-bar family, in the re-runnable `tests/test-unmapped-elements.py` — which also **ties the guarded set to the published schema** (as `test-flowkit.py` ties `TIMER_UNITS` to `ETimerToken`), so if the transformer ever gains an `old-price` mapper the suite goes red instead of the guard quietly becoming a false positive. Negative-tested by emptying the constant: exactly the two FIRES cases redden.
  **Not agent-tested, and recorded rather than hidden.** The facts are measured and the guard is calibrated, but no GREEN round has asked whether an agent reaching for a strikethrough now avoids `old-price`, or whether one authoring an unfamiliar element type greps `x-supported` first. Per the Iron Law both stay open. **The scenario to run:** ask for an annual plan card showing a struck-through "was" price on a screen that has none, and score (a) whether `old-price` is authored, (b) whether the agent ran `verify-config.py` and acted on the warning rather than pasting it through, and (c) whether the alternative it chooses is copy or a two-plan comparison rather than a `text` element holding a fabricated literal. Two consecutive clean rounds, per this repo's bar.
- **Finding 23 (2026-08-31, adapted from `adapty/adapty-agents`): a text-legibility check, and the useful half is everything the adaptation had to CHANGE.** Their hosted agent ships `services/color_readability.py` — WCAG contrast over resolved `IColor`s — behind a guard against the one failure they say their model keeps producing: darkening a container's fill and leaving its text dark. We had no contrast check of any kind. Read, not vendored, same rule as every other borrowing from that repo.
  **Three adaptations, each forced by measurement rather than taste.** (1) **Their `MIN_OPAQUE_ALPHA` semantics INVERT for a whole-document checker.** In an edit-time post-guard, a fill too translucent to establish a background means *the edit made this unverifiable, reject it*; in a checker run over a document nobody just edited, the same condition means *the designer chose that scrim and I cannot resolve what shows through* — so it must **skip**, never report. Dropping the skip reddens **two real exports**. (2) **The threshold is not theirs.** They use 3.0 (WCAG AA-large), which is right for gating an edit and wrong for auditing a document: **13% of the text in real builder exports is already below 4.5**, and 3.0 fires on `reviews-carousel.json`'s amber stars. So the check does not claim to be an accessibility audit — it asks only *can this text be told apart from its background at all*, at **1.5**, which sits in a measured empty gap (lowest deliberate value in the corpus **1.89**, next value down **1.08**). Naming it a WCAG check would be the overclaim. (3) **It walks BOTH appearance variants**, which theirs does not, because `config preview` draws light only and a half-finished dark palette is otherwise invisible to every gate we have.
  **The port had a real bug and the corpus caught it — a bug the upstream shares.** `theme_hex_by_id` returns each token's `hex` and drops the token's own `opacity`, then reads `opacity` off the *referencing* colour. But `vpn-timer-draft`'s `clr_2MZWrBUc` is `{"hex": "#FFFFFF", "opacity": 20}` — a **20% white scrim over a background image**, declared entirely on the token. Reading only the hex turns it into an opaque white card and invents a **1.0 white-on-white finding on a real export**. So effective alpha is the product of **three** independent sources (8-digit hex alpha × the reference's `opacity` × **the token's** `opacity`), all three present in the corpus. `flow-schema.md` trap 11 already said a `color-style` *reference* carries no opacity, which is true and was the thing that made the token's own opacity easy to miss; it now carries the three-source table. **Worth reporting upstream.**
  **The corpus measurement that explains who needs this: all 11 genuine exports bind the screen fill to a theme TOKEN or an image — not one uses a literal hex.** So in real builder output background and text move together between appearances, and the check is silent. An *authored* screen that hardcodes `fill: "#FFFFFF"` under a dark-capable theme does not, and `flowkit` makes exactly that the obvious thing to write. That is the whole justification for shipping a check that never fires on builder output.
  **Calibration, and the one finding left standing on purpose.** Silent on all **11** genuine exports (7 tracked + 5 raw, minus the hybrid); 14 constructed cases both directions in the re-runnable `tests/test-legibility.py`. `timeline-anchored.json` **does** fire — 6 elements, dark only — and it is **asserted in the suite rather than suppressed**: it is the corpus's one hybrid (real screen, *borrowed* theme, already excluded from census counts) and it hardcodes its screen fill, which no genuine export does. Editing a fixture to quiet a checker is how a corpus stops being evidence, so the fixture is untouched and the finding is pinned as an expectation — which also gives the check a real-instance demonstration instead of only injected ones. **Negative-tested per mechanism**, and three of the four reddens involve real exports rather than synthetic cases: dropping the token alpha reddens the scrim case + `vpn-timer-draft`; light-only reddens the dark case + the hybrid; `MIN_OPAQUE_ALPHA = 0` reddens both scrim cases + `comparison-paywall` + `vpn-timer-draft`; threshold 3.0 reddens the amber-accent case + `reviews-carousel`.
  **CORRECTED 2026-09-02: it resolved SOLID fills only, so a gradient background was invisible to it and the walk fell through to the screen fill.** Black text on a bright foil button therefore reported ~1.01-1.05:1 — the loudest possible finding — for text that measures **12.3:1 and 13.7:1** against its actual surface. Found by GREEN round 1, where **5 of 6 independent agents** hit it and every one of them diagnosed it identically as a false positive; two then changed their design to route around a checker that was wrong, which is the cost a false positive actually has. Fixed by resolving a gradient to **one background candidate per stop** and taking the **worst** contrast across them — text that survives one end of a gradient and vanishes at the other is text that vanishes, and a mean would have averaged exactly that into looking fine. Three parts, all negative-tested and each reddening only its own cases: removing gradient resolution reddens 6 cases, dropping the per-stop translucency skip reddens 1, and flipping worst-stop to best-stop reddens exactly the case named for it. **The translucency rule is per stop and is not hypothetical** — a real export gradient runs one stop at opacity 100 and the next at **26**, so the same "a scrim is the designer's business" skip that solids already follow had to apply stop-wise. **Calibration is unchanged**: silent on all 11 genuine exports, and `timeline-anchored.json` still fires exactly its pinned 6. The decisive validation is not the fixtures but the field — **all six round-1 artifacts went from 4-6 findings to 0**, and those are documents six agents had already declared wrong. `tests/test-legibility.py` gains 9 cases both directions. One of them was written badly first and the suite caught it: an "empty gradient establishes nothing" case used near-black text on a near-black *screen* fill, so it fired for the honest reason — the fall-through working — and tested nothing; the text is now legible against the screen fill, so the only thing that can fire it is an empty stop list wrongly establishing a background.

  **Scope limits, stated rather than discovered later.** Element-level `props.color` only — **no rich-text span carries a colour anywhere in the corpus** (247 spans, only bold/italic/underline/strikethrough), so a per-span colour would be missed if the builder ever emits one. Backgrounds resolve through solid fills only; image, gradient and multi-layer fills skip. And `text` elements only, not `product` or other text-bearing types.
  **Bycatch, not fixed here:** `reviews-carousel.json` renders its rating as the literal characters `★★★★★` in a `text` element. That is what surfaced the 1.89 measurement, and it is independently against this repo's own never-substitute-a-glyph rule — the same substitution the dashboard team's prompt forbids outright because the component draws real star icons. Worth its own change.
  **Not agent-tested.** No GREEN round has asked whether an agent told to "make this dark" now repaints the text too, or acts on the warning rather than pasting it through. Per the Iron Law it stays open. **The scenario to run:** hand an agent a light authored paywall and ask for a dark version, and score (a) whether the screen fill is bound to a token or hardcoded, (b) whether text colours move with it, (c) whether `verify-config.py` was run and its dark-mode warning acted on.
- **Finding 24 (2026-08-31, adapted from `adapty/adapty-agents`): a mechanical guard on fabricated prices — and the corpus forced it to be DIFF-BASED, which is the whole finding.** This targets the failure this repo already shipped: `patterns.md` carried a retired rule telling agents to write prices as plain text, and an agent following it produced a paywall that rendered perfectly and showed a **fabricated price**. Their agent carries three regexes (currency / percent / trial-duration) plus a `no_hardcoded_price` post-guard, and an eval evaluator that diffs `after` strings against `before`.
  **Absolute checking is not viable and the corpus says so unambiguously.** Scanning 361 visible strings across the 12 configs: `vpn-timer-draft` carries `Only $1.15 / week`, `Just $59.99 / year` and `instead of $79.99 / year`; `onboarding-quiz-paywall` carries `Save 75%`; `onboarding-multilocale` carries `Best value — 12 months`. **All real builder exports, none of them bound to a variable, all typed by a human who knew the offer.** So "this document contains a price literal" is not evidence of anything — flagging it second-guesses the designer, the same conclusion the contrast work reached about WCAG thresholds. What *is* evidence is a literal **this document added**. Hence two tiers: the **placeholder** form (`$X.XX`, the template stub) is reported always (0 of 12 configs contain one, nobody means to ship it), and price/discount/duration are reported only under a new **`--baseline`** flag — the repo's existing idiom from `schema-check.py` and `validate-with-schema.mjs`. Comparison is a **set difference over visible strings**, so copy that merely moved between elements is not a finding.
  **Both regexes are corrected against the originals, and both bugs were found by running them over real exports rather than by reading them.** (1) The percent lookbehind needs `.` and `,` in it, or **`fees of 1.99%` matches as `99%`** — a real string in `tabs-paywall.json` and a pure false positive. (2) Requiring a non-alphanumeric before the currency symbol **misses every prefixed currency** — `S$1.49` (also in `tabs-paywall.json`), `US$50`, `A$12.00`. Worth reporting upstream alongside the token-opacity bug from finding 23.
  **No new workflow step, which is why it will actually run.** `gates.sh` already takes `BASELINE=` for the schema gate and already absolutises it, and `SKILL.md` phase 3 already says **"Always pass `BASELINE=`"** — so threading it into `verify-config.py` was one branch, and the price comparison switches on exactly when an agent is editing a fetched config. Verified end-to-end: a draft that rewrites `$1.15` to `$4.99` is named through `gates.sh`; the same draft with no baseline is silent.
  **Calibration**: silent on all 12 real configs with no baseline, and silent on **each config against itself** (a document never accuses itself); 21 constructed cases both directions in the re-runnable `tests/test-price-literals.py`. **Negative-tested per mechanism**, and the middle one is the load-bearing result: reverting the percent lookbehind reddens the decimal case; reverting the currency lookbehind reddens both prefixed-currency cases; **dropping the baseline gate reddens 5 real exports**, which is the measurement that justifies the whole design.
  **Bycatch, and it closes a real gap the check itself created.** The warning tells an agent to "bind a variable", and the product field set was documented only as `prod_price_per_{day,week,month,year}` plus the hand-wave "and the rest of the product set" — so `offer_*` was unreachable. Filled in from the **public** [Element variables](https://adapty.io/docs/onboarding-variables.md) page (not their prompt), which turned out richer than that prompt: the full set including `prod_title`, `offer_price`, `offer_billing_period`, `offer_full_duration` (**the trial-length one**) and the three `is_*` booleans, plus two traps neither we nor they had written down — **every `offer_*` field is empty when the user is not eligible**, so a trial line silently loses its number for everyone else and must be gated on `is_free_trial`; and `offer_billing_period` equals `offer_full_duration` for trial and pay-upfront offers but **not** for pay-as-you-go, so they are not interchangeable.
  **Not agent-tested.** No GREEN round has asked whether an agent asked for "a 40% off badge" now binds or refuses rather than typing the number. Per the Iron Law it stays open. **The scenario to run:** hand an agent a fetched paywall and ask for copy that needs a figure the catalog does not supply, and score (a) whether a literal is written, (b) whether `gates.sh` was run with `BASELINE=` and the warning acted on rather than pasted through, (c) whether the agent asks for the real offer instead of inventing one.
- **Finding 25 (2026-08-31): the GREEN-round methodology was consolidated into a pre-dispatch gate, and the tally that justifies it is entirely our own.** Across the arm-comparison rounds in this file, **five returned a null result** (`merge-green` 1 and 2, `helper-green` 1 and 2, `fake-slider-green`) and **a sixth was inconclusive by construction** (validate round 1). Every one was diagnosed afterwards, in its own finding, and **the diagnoses repeat**: the sandbox account held a working reference implementation the agents copied in **two** rounds, and the pre-registered prediction that control would fail has been wrong **four times**. The lessons were paid for; what was missing was a place to spend them *before* dispatch instead of after. [`docs/green-round-gate.md`](docs/green-round-gate.md) is that place — tracked, maintainer-only, and every row cites the round that bought it.
  **Form, chosen against this repo's own evidence rather than by preference.** The failure is an *omitted step* (a round gets designed and dispatched, then diagnosed as unable to work), not a rule anyone skipped under pressure — so per `superpowers:writing-skills`' Match-the-Form table it is a **structural slot list**, not a warning list, and this repo has already measured that slots beat reminders (`ads-manager`'s `--idempotency-key` template). Five sections: does this deserve a round at all, the scenario, the rubric, the prediction, and what happens after.
  **What is genuinely adopted from `adapty/adapty-agents` rather than home-grown** is section 1's *intake* direction. This repo has always had the escalation direction — enforceable → automate, prose-failed-twice → mechanical — but no rule for the reverse question, *should this be a round at all or a check?* Their `author-eval` skill gates on "requires the LLM / checkable on agent output / not UI mechanics" with a redirect table, and that shape is what section 1 borrows; their "red eval" (name the missing instrument rather than shipping a weak row) is what rubric row 5 borrows, generalising R8. Their fixture principle — *absence makes faking detectable*, keeping a fixture with no carousel/tabs/timer specifically so a faked one is visible — is the same lesson as our `fake-slider-green` confound stated one level earlier, and it is why scenario row 2 says to search the environment the way an agent would **before** dispatch. Read, not vendored; nothing internal cited.
  **Not itself tested, and the file says so in its own last paragraph.** Every row traces to a documented failure, but the list as a whole has never been used to design a round — its first real test is the next one. The gate's own closing row requires that any scenario flaw found afterwards be written back into it with the round named, which is the only thing that keeps it worth reading. Also corrected in passing: the testing-layers bullet still described the GREEN rounds as "never run", which stopped being true around 2026-08-20.
- **Finding 26 (2026-08-31, cross-checking finding 18 against `adapty/adapty-agents`): the transformer's own issue-code taxonomy, what it confirms, and the ONE gap it found — which our own `offer_*` documentation had just widened.** Their repo ships a generated `error-codes.json`: **66 codes, 49 error and 17 warning**, each with severity and source file/line, extracted from the transformer at a pinned commit. Finding 18 had to derive severity by reading transformer source by hand, so this is that work, generated. Read, not vendored: it is their build artifact from an internal repo, it is pinned to a commit that will move, and a copy here would rot silently.
  **What it confirms.** `unsupported_text_typography_setting` is a **warning**, which is exactly finding 18's reading that its 1891 occurrences measure co-occurrence with some other fatal rather than warnings in the wild. And all three codes `finish.py` classifies as transformer-side faults no flow edit can clear — `output_schema_violation`, `script_type_violation`, `script_lint_violation` — are **absent from all 66**, which is structurally coherent (the extractor covers `mappers/` plus two `script/` files) and independently supports finding 18's call that `output_schema_violation` is a downstream symptom rather than an authorable defect.
  **The caveat that stops it replacing our ranking: it is PARTIAL, and measurably so. 5 of finding 18's top-12 production codes are absent** — `output_schema_violation`, `invalid_visibility_condition`, `script_type_violation`, `invalid_action_payload`, `unknown_navigation_target`. So the 40-day production ranking and this taxonomy answer different questions (what actually fails in the wild vs what the mappers can emit) and **neither supersedes the other**. Do not treat a code's absence here as evidence it does not exist.
  **A name-scan says we name 11 of 66 anywhere in the repo, and that number is misleading — do not quote it.** Our checks assert invariants, not code names: `missing_hierarchy_element` is covered by "hierarchy refs resolve", the icon family by the `_meta.icons` declaration error, the font family by `font.family`/`font.preset` resolution, and the duplicate family by the id-collision check. Two whole families are ours to ignore rather than cover — the 4 `progress_bar_*` warnings (a flow-level component, and `x-supported: false` per finding 22) and the 5 animation/effect warnings (we author neither).
  **The real gap, found by looking rather than by scanning, and it is one I had just made worse.** The price-variable check keyed on the substring `.prod_`, so it validated the head of a `prod_*` variable and **skipped every `offer_*` and `is_*` variable outright, head included**. Measured before the fix, both silent: `<valid-uuid>.prod_price_per_fortnight` (bogus field) and `<bogus-uuid>.offer_price` (bogus head, skipped entirely). The second is the sharper one, because finding 24 had just documented the `offer_*` family and told agents to bind it — **the least-validated variables were the ones most likely to be newly authored.** Now keyed on the field families, with the closed 12-field set from the public [Element variables](https://adapty.io/docs/onboarding-variables.md) page; a field outside it is an **error** (`unknown_product_field`, which the service itself treats as the author's own typo, i.e. fixable, unlike the product-registry codes beside it).
  **The set is closed by measurement, not assumption**: the corpus's 15 distinct `variableId`s use only `prod_price`, `prod_price_per_month` and `prod_price_per_year`, and nothing product-shaped the set lacks — and the other variable shapes there (`email.value`, `plans.selectedProduct`, `quiz.selectedOptionId`) do not collide with the three family prefixes. **Calibrated**: silent on all 12 real configs and on every one of the 12 documented fields (an under-inclusive set would be worse than none), fires on 7 injected forms, in the re-runnable `tests/test-product-fields.py`. **Negative-tested per mechanism, and the two are separable**: reverting to the `.prod_` guard reddens the 5 offer/is cases only; keeping the widened guard but dropping the closed-set test reddens the 4 bad-field cases only.
  **Not agent-tested.** No round has asked whether an agent binding a trial line reaches for `offer_full_duration` rather than inventing `offer_duration`. Open, per the Iron Law.
- **Finding 27 (2026-08-31, branch `flow-audit/store-compliance`): `flow-audit` gained a store-review dimension, and the whole design turns on one line — a missing mechanism is a fact and may be a blocker; a prediction about how a human reviewer will judge a design is advisory and may not.** Six checks: `trial-toggle`, `billed-amount-not-shown`, `derived-price-louder`, `no-period-disclosed`, `trial-terms-incomplete`, `external-purchase-link`. All `risk` or `question`, none a blocker, printed in their own `STORE REVIEW — ADVISORY` section with a verbatim disclaimer. Evidence, calibration and the ruled-out list live in `skills/flow-audit/references/store-review.md`; `checks.md` carries a pointer, not a copy. **The argument for the framing is the evidence itself**: store review is human, inconsistent between submissions, and changes without announcement, so a checker cannot predict it — and the five pre-existing compliance checks (`no-restore`, `no-terms-link`, `no-privacy-link`, `no-escape-in-flow`, `no-escape-from-paywall`) were therefore **not** downgraded, blockers included, because each tests for the *absence of a mechanism* the store requires and the flow provably lacks. A paywall with no `restorePurchases` anywhere is broken for a user restoring on a new device whether or not a reviewer notices.
  **The two customer rejection notices, verbatim, because they are the evidence the checks rest on and they will be asked about.** (1) **Toggle paywalls, Jan–Feb 2026, guideline 3.1.2**: Apple began rejecting the free-trial on/off switch. A warning went into roughly twenty customer channels on **2026-02-11**; several customers replied it had already happened to them, and one had the toggle reinstated only after arguing the case ([the write-up](https://adapty.io/blog/your-toggle-paywall-is-about-to-get-rejected/)). The mechanism objected to: *the trial's terms are not visible unless the user interacts with the switch, so a user who never touches it is never shown them.* **No guideline edit, no documentation change, no grace period** — which is the whole argument for rule 1. (2) **Per-month louder than the billed amount, Jun 2026, 3.1.2(c)**, quoted as sent: *"One or more auto-renewable subscriptions are marketed in the purchase flow in a way that may mislead or confuse users about the subscription terms or pricing. Specifically: The auto-renewable subscription displays the monthly calculated pricing for the subscription more clearly and conspicuously than the billed amount."* The customer had to change **every active paywall**; the fix that passed was billed amount bold at 14px with the per-month figure at 12px, rather than removing the per-month figure (which costs the savings framing).
  **VERDICT ISOLATION IS NOW A TESTED PROPERTY, and getting there cost three attempts because the first two assertions could not fail.** `render()` partitions the six checks out **before** the severity loop, so the verdict is isolated by construction rather than by a special case in the verdict code; removing that partition line reddens two named assertions — `a store-review-only flow still reads READY FOR PRODUCTION` and `...and is not downgraded to READY, PENDING`. The route there is the reusable part. Attempt 1 gated the assertions on `if store_only:`, measured **False** on the fixture it used (`onboarding-multilocale.json` also fires `dead-affordance`, `no-restore`, `no-terms-link`, `no-privacy-link`, `no-escape-from-paywall`, `product-store-gap` and `untranslated`), so the feature's central claim was tested on no fixture at all. Attempt 2 used a synthesized store-review-only config whose findings were **both `risk`** — and a risk never moves the verdict, partitioned or not: measured, `[risk, risk]` prints `READY FOR PRODUCTION` either way. The config that discriminates adds an `openUrl` to `https://external.example.com/payment/confirm`, firing `external-purchase-link`, a **question**: partitioned → `READY FOR PRODUCTION`, unpartitioned → `READY, PENDING 1 CHECK I CANNOT MAKE` with the finding in `COULD NOT CHECK`. And the mutation itself has a trap — **emptying `store` instead of un-partitioning deletes the findings rather than routing them**, and the verdict correctly does not move. *A mutation that proves isolation has to route the findings, not drop them.*
  **Two design decisions were reversed during planning, and both reversals matter more than the things they replaced.** (1) An earlier draft gave the verdict line a store dimension — `1 would fail App Store review`. **Dropped: that is exactly the certificate this feature must not issue.** Nothing was added to `## The verdict rule` in `SKILL.md` either, because an edit there would invite the reading the feature exists to prevent. (2) An earlier draft put a `stores` field on every finding. **Dropped because `test-audit-flow.py` asserts the finding key set exactly (`set(f) == {...}`), so a new key breaks the contract test for every check in the script** — and nothing outside two checks would ever read it. Store scoping lives in the check's **arguments** instead (`check_trial_toggle(config, stores)`, `check_external_purchase(config, stores)`, the shape `check_products_catalog` already used): iOS-only checks return nothing when the app does not ship on iOS, and with stores **unknown** they still print, worded as applying to iOS.
  **`subscription-under-7-days` was RULED OUT, and the reason generalises: a guideline requirement enforced upstream is not a check.** 3.1.2(a) says a subscription must last at least seven days; **App Store Connect only offers 1 week, 1/2/3/6 months and 1 year**, so a non-compliant period is **not creatable** and the check could never fire. Also ruled out: `preselected-costlier-plan` (pre-selecting annual is near-universal and converts, and the check would contradict `paywall-teardown`, which owns conversion advice) and anything needing a render. **`escape-gated` and `fabricated-urgency` are DEFERRED, not rejected** — structurally detectable and worth having, but with the least evidence behind them of anything proposed, so they were kept off the change that shipped the two rejection-notice checks.
  **`trial-toggle` needs THREE signals, and the third exists because the two-signal version fired on the remediation the check itself recommends.** Shape (a `fixed`×`fixed` box wider than tall, fully rounded on all four corners) + trial copy + **state-dependence (`propsByState.selected` on the SAME pill-shaped element)**. Review found the shape-plus-copy version would fire on a pill-shaped **"Free trial" BADGE** on a plan card in a selectable product group — i.e. on the exact compliant pattern its own fix text names ("a side-by-side plan comparison with the trial badged on one plan"), and no corpus fixture has that shape so no test caught it. **The discriminator is mechanical: a switch is state-dependent, a badge is static** — the catalog's own `trial-toggle` template pill (52×32, `borderRadius: 9999`) moves its knob's `layout.alignH` from `start` to `end` in `propsByState.selected`, while a badge carries no `propsByState` at all. *An advisory check that fires on the fix it recommends teaches users to ignore the whole family*, which this repo has paid for once already. **Why shape cannot be used alone, measured: exactly ONE element in the 12 real exports is switch-shaped** — a **50×30 stack (`el_054S`) on `onboarding-multilocale.json`'s `scr_notify`**, a notifications opt-in. It carries `propsByState.selected` too, so the third signal costs nothing on the calibration case (it stays cleared by the trial-copy and selling-screen guards). Negative-tested in four parts plus the fix round; each of parts 1, 3 and 4 reddens exactly one case and leaves all 12 corpus lines green. **Part 2 is reported as it came out rather than smoothed: deleting the selling-screen guard reddened NOTHING**, because for that one real pill the trial-copy guard alone already clears it — the two guards are genuinely redundant *for this calibration case*, not one masking the other.
  **`no-period-disclosed` FIRES on the real export `tests/fixtures/tabs-paywall.json`, and it is a TRUE finding — which corrects a measurement in this feature's own spec.** All 41 text elements on `scr_RvSel001` were dumped and read: **zero period words anywhere**, **zero price variables anywhere in the whole document**, and **no `propsByState` content override**, so there is no hidden shape the check missed. A real, shipped tiered-plan screen sells subscriptions while disclosing neither price nor billing frequency — exactly the Schedule 2 "Length of subscription" gap. The spec's "silent on 10 of 10 real product cards" figure was measured **per card** and **structurally could not cover this flow**, which binds its three products through `const` purchase payloads with **no `product` element**, so a card-scoped probe sees nothing there at all; the screen-scoped fallback covers what that probe could not. **Do not read that figure as covering this shape.** The fixture is excluded from the blanket SILENT loop and given its own FIRES assertion with the evidence as its comment. Screen-scoping is itself the false-positive fix: the period may legitimately be stated outside the card ("Billed yearly" under the CTA discloses it for every card above), so a card-scoped test flags a compliant layout and one row per card buries four identical findings.
  **The URL vocabulary principle, and it is the transferable half of `external-purchase-link`: the list names a PAYMENT MECHANISM, never a PRODUCT SURFACE.** Kept `checkout`, `pay`, `payment`, `purchase`, `buy`, `stripe`, `paddle`; **dropped `billing`, `upgrade`, `paywall`** on measured false positives. **`billing` is the decisive case — Google Play REQUIRES an accessible subscription-management path, plausibly at `/account/billing`, so that token fired on a link another store demands the app carry.** `upgrade` fires on `/why-upgrade`, `/compare-plans-upgrade`; `paywall` on an attribution deep link into the app's own paywall (`?af_dp=yourapp://paywall`). `paddle` is **kept with a known narrow false positive** (sports apps, `/blog/paddle-boarding-tips`) because it names a real web-checkout processor. All three dropped words carry pinned SILENT tests, because a vocabulary decision with no test is a comment someone will undo. **And the limit is stated plainly: the corpus cannot calibrate this vocabulary AT ALL** — across all 12 tracked and raw fixtures the only `openUrl` targets are `/terms` and `/privacy`, so "silent on the corpus" proves nothing about the word list, whose precision rests on constructed cases and reasoning. One diagnostic worth keeping: substituting a whitespace split for `_url_tokens` gives **zero false positives and zero true positives** — a url contains no whitespace, so the whole url becomes one token that must equal a vocabulary word exactly, which is a worse failure than the substring hazard (`tos` inside `photos`) the tokenizer exists to avoid.
  **TWO FALSE-NEGATIVE CLASSES WERE ACCEPTED DELIBERATELY, and both are pinned by tests labelled `KNOWN LIMITATION`** so that tightening either mechanism turns them red and points the next person at the reasoning. (1) `trial-terms-incomplete` is silenced by **any unrelated currency amount anywhere on the screen** ("Tip the developer $2"), because its `after` guard searches the whole screen blob. Not fixed: scoping the search to the trial's own segment would **manufacture the false positive this design specifically avoids**, since the compliant layout states the trial in the hero and the charge in a footnote under the CTA, in different elements. (2) `no-period-disclosed` is silenced by an ordinary feature bullet using "per" ("10 workouts per week"), because `BILLING_VERB_RE`'s generic `per` satisfies `_billing_context`. Not fixed: that root cause is **pre-existing, calibrated against four measured false positives, and shared with the already-shipped `period-claim-mismatch`** — widening it to serve a new advisory check risks a live check for a silence. **For an advisory check whose contract is "hazards, not verdicts", a miss is the cheaper error than crying wolf.** A ruling with no artifact is a silent discard, which is why both landed as tests plus named limitations rather than as a decision in a ledger. The first limitation test was verified three ways rather than trusted — with the `$2` it is SILENT, with neutral filler in the same slot it FIRES, and with `MONEY_RE` removed from the guard it FIRES again — and one reviewer verdict was **overturned by measurement** along the way (the claim that it passed vacuously because `'Start your free trial now'` fails `TRIAL_RE`; `TRIAL_RE` ends in a `|\btrial\b` alternation, so bare "trial" matches).
  **Two more calibration traps, both of the "assertion that cannot fail" class this repo keeps re-learning.** (1) The 12-fixture `derived-price-louder` SILENT loop was **vacuous**: under the default catalog no fixture resolves a `period`, so `billed` stays empty and the code returns before `dmax > bmax` is evaluated. Proven mechanically — forcing the comparison to always fire left **all 12 loop lines green** — and the loop was renamed to what it actually covers (no misfire on real exports) with a genuinely-reaching pair added on `onboarding-quiz-paywall.json` + its real catalog. (2) The mutation-direction rule that came out of it: **a `if False:` mutation is mathematically incapable of reddening a SILENT case**, since weakening a firing condition can only turn a firing case silent; proving a SILENT case discriminating needs the complementary `if True:`. Also recorded: removing `check_price_prominence`'s `if not billed:` guard does not misreport, it **crashes** the script (`ValueError: max() iterable argument is empty`) — loud is the right failure direction; and a test that **replaces** an element's `interactions` rather than appending can delete the screen's `purchase` action, dropping it from `selling_screens` so the check goes silent *for a reason unrelated to what it tests* — a false pass that reads as a green run.
  **Open follow-up, the same class as finding 15 (the recommended path leading into the failure): `component-catalog.json` still ships `trial-toggle` as `agent_allowed: true`, so `flow-generator` can build the exact pattern `flow-audit` now flags.** Deliberately not fixed here. The pattern is legal, it converts, and Apple's enforcement is inconsistent, so the fix there is a **warning beside the template, not its removal** — but until it lands, the two skills disagree, and that is worth knowing before anyone reads a clean `flow-generator` run as store-safe.
  **What is NOT claimed: no GREEN round has run.** Every fact above is measured and every mechanism is negative-tested, but whether an *agent* running this skill reports a store-review risk as advisory rather than as a blocker, and whether it prints the disclaimer verbatim rather than softening it, is **unmeasured**. Per the Iron Law it stays open. **The scenario to run:** hand an agent a flow carrying a trial toggle and a per-month-only annual card, and score (a) whether the verdict stays `READY` when the only findings are store-review risks, (b) whether the disclaimer appears verbatim, (c) whether the rejection-notice wording reaches the user or is softened into "may not comply". Two consecutive clean rounds, per this repo's bar.

- **Finding 31 (2026-09-02, a GREEN-round agent): `patterns.md`'s single-plan skeleton spelled its hidden attach point as `height: fixed 0` — the exact shape `verify-config.py` ERRORS on under trap 15.** So the file that tells an agent to prefer filling a documented skeleton was handing out the one the checker rejects: finding 15's class, third instance. `visibility: hidden` already collapses the space, so the zero height bought nothing and tripped a real gate. Fixed to `hug`. **Evidence tier stated rather than assumed, and it is weaker than it looked**: *no real export contains a hidden attach point at all* — every `product` element across the corpus is a visible plan card (`hug` height, `fill` width, no `visibility`) — so the composition is **authored, not observed**, and `patterns.md` now says so. Its two halves are attested separately: `hug` is what every real `product` element uses, and `visibility: hidden` occurs on real builder output elsewhere in the corpus. **And it is finding 12 again in the same place**: `flowkit` had `product()` and `hidden()` but nothing that emitted the attach point, so it was hand-assembled from the broken skeleton every time — `attach_point()` now ships, verified to pass the gate clean with only the expected bound-but-undeclared warning, with 6 checks in `tests/test-flowkit.py` negative-tested by reverting the helper to `fixed: 0` (which reddens exactly the trap-15 row). The repo-wide grep the fix demanded came back clean: **`component-catalog.json` carries 0 fixed-0 dimensions**, so the defect had not been copied into the templates.

- **Finding 32 (2026-09-02): `verify-config.py` answered a document with a TRACEBACK for the second time, and the fix is the generalisation rather than the line.** `meta = {(i['name'], i['weight']) for i in ...}` read two keys straight off dicts an author wrote, so a `_meta.icons` entry missing `weight` died with a bare `KeyError: 'weight'`. That is the same class as the envelope `KeyError: 'theme'` already recorded here, and the damage is identical both times: **a stack trace reads as "your config is corrupt" when it means "one field is missing"**, so the reader looks in the wrong place. Both halves of that line were fixed — the element side (`e['props']['icon']['weight']`) had the same bug — and each now reports a finding naming the entry and the missing field. **Severity was chosen by measuring**: across the corpus and the shipped catalog, **0 of 21 `_meta.icons` entries, 0 of 60 icon elements and 0 of 76 catalog templates** omit either field, so an absent one is malformed authored input worth reporting rather than defaulting away. **The generalisation is the part that matters**: every other direct subscript in a 1,700-line checker is a latent instance, so `main` now converts any unexpected exception into a `CHECKER ERROR` at **exit 2** — distinct from exit 1, which means the document has findings — with a message that says outright it is a bug in the checker and the document may be fine. Calibrated silent on all 12 real configs; `tests/test-icon-declarations.py` (new, 20 cases) pins both directions plus the existence of the top-level guard, and the subscript fix is negative-tested by reverting it, which reddens exactly the three malformed-entry cases. **One of its own fixtures was wrong first and the suite caught it** — a "no icons at all" case left `_meta.icons` empty while the element still referenced an icon, so it tripped the pre-existing used-but-absent check and tested nothing; that reasoning is pinned in the test.

- **Finding 33 (2026-09-02, reading `flow-publish-doctor` against `preview.md`): the preview-vs-device list gained a DIAGNOSTIC direction, and both borrowed rules had to be narrowed against the corpus before they could ship.** Source: the dashboard team's diagnostic skill (`.agents/skills/flow-publish-doctor` in `adapty/adapty-dashboard-interface`) — **read, never vendored**, the same rule this file already applies to it and to `adapty/adapty-agents`: adopt the mechanism retargeted to what an agent holding the CLI can act on, cite nothing internal. Its Jira epics, admin-API hosts, 14-signature catalog and ticket families are deliberately not here, for the reasons already recorded.
  **What was adopted, and why each one is agent-actionable.** (1) **The layer-localisation rule**, its highest-value transferable idea and the thing `preview.md` genuinely lacked: the four-surfaces table said what each surface *proves*, and never the inverse — that the **pattern of disagreement** narrows the cause to one of four layers (config data / preview page / transform service / SDK renderer) before you touch anything. Landed as `## Which layer owns the defect`, five rows, with the burden-of-proof rule attached (*a suspicious shape in the JSON is not evidence of a bug and a clean one is not evidence of a fix*). (2) **The snapshot question**, which is the commonest cause of "I published and the device did not change": the `mobile-app.adapty.io` link reads the **draft** transform through its own handle and needs no publish, while the dashboard's own device preview reads the **published** version — so the two surfaces show two snapshots of one flow and the report is often a comparison across that boundary, not propagation or caching. It is now the first question `preview.md` tells you to settle, with a three-row table of what each surface shows. (3) **A publish that never settles is infrastructure, not a document**, so `validate.md` says outright: do not start rewriting a clean config to chase a stuck `transforming`/`publishing`, say the status is stuck and hand it over — where `publication_failed` is the opposite case and *is* yours. (4) **A shape-invalid config imported into the builder can open EMPTY and the next save writes that emptiness over the real flow** — no error to act on, no unlucky timing — which is a way to lose a flow that `diff-config.py` structurally cannot see, because the import is not a write the agent makes. Landed in `merge.md` as its own section plus one line in `SKILL.md`'s file contract (back up before importing). (5) **Do not leave the mobile preview open while the flow is being edited** — the draft save and an open preview contend, and the symptom is a save conflict rather than a stale render.
  **THE TWO BORROWED RULES WERE BOTH WRONG AS STATED, and the corpus is what caught them — this is the entry's real content.** (a) The upstream rule is *valid ids are `[A-Za-z0-9_]` only*, with a flowctl token named `screen_id_breaks_js` beside it. Measured across the 7 tracked and 5 raw fixtures: **4 of 36 screen ids are bare UUIDs** — hyphens included — and each is the **entry screen** of a flow whose `status` is `published`; `comparison-paywall.json`'s *only* screen is one, and the raw export carries a UUID too, so it is not a sanitization artifact. A check extended to screen ids **fires on real published builder output**, which is this repo's disqualifying calibration failure. Element ids carry no such exception: **911 of 911** across the same 12 configs are clean. (b) The locale rule is *`pt-BR`, not `pt-br`* — true, and the obvious regex for it (`^[a-z]{2}(-[A-Z]{2})?$`) is wrong in the other direction, because a **script subtag is four letters in Title case** and `sr-Latn` is a real code in `onboarding-multilocale.json`. Both narrowings are negative-tested and **each reddens real exports**, which is the strongest form: including screen ids reddens 4 corpus lines plus the pinned UUID case, and the naive locale regex reddens the fixture plus `zh-Hans`, `zh-Hant-HK`, `es-419` and `fil`. The transferable form is finding 19's, from the other end: *a rule borrowed from a source that cannot see your corpus is a hypothesis until the corpus has been run against it.*
  **The mechanism is worth keeping because it explains a row this repo had already recorded without an explanation.** `validate.md` has carried *"a hyphen in an element id … renders as a black screen on device"* under **caught by: nothing**. The reason is that an element id is emitted as an **identifier in the generated runtime script** — the same code path `flowkit.config()`'s condition-variable check already documents, where an unresolved variable head is written out bare and the script fails to compile. So a hyphen makes the script syntactically broken, and every gate reachable from here is blind: `validate` passes it, the schema types ids as bare strings, and `config preview` draws the **config** rather than the transformer's output, so the screen renders correctly. One script per flow, so an id reused on a second screen collides in it too. Per this repo's escalation rule the prose-only row went mechanical: **`verify-config.py` errors** on an off-charset element id and on a cross-screen duplicate, **warns** on the softer family (`groupId`, input `customId`, `variables[].id` — the heads of `<groupId>.selectedOptionId` and `<customId>.value`, corpus-clean but with no reproduced black screen behind them), and **warns** on a misshapen locale code; **`flowkit.config()` raises** on all of them, since a code you are writing now has no excuse where one arriving in a fetched config is a report-and-ask. **Calibrated** silent on all 12 real configs, firing on 14 injected shapes, in the re-runnable `tests/test-id-hygiene.py` (37 cases, both directions) — and negative-tested per mechanism, each disabled guard reddening exactly its own cases (4 / 1 / 3 / 6) and nothing else. `tests/test-flowkit.py` +8.
  **Bycatch, and it is the finding-15 class inside our own files: `preview.md`'s emoji fix told agents to author `verticalAlign: middle`, which `flow-schema.md` says twice not to author.** The transform service reports it `unsupported_text_typography_setting … will be ignored`, and removing it from 93 elements rendered byte-identically — so the recommended fix for a real device-only defect bought one warning per element and no centring. Found only by cross-reading the two files, and independently corroborated by the source read here (it lists `verticalAlign` among the silently-dropped properties). Corrected at the recipe, pointing at the owner file, with vertical placement routed to the parent's `layout.alignV`. Also fixed in passing: the four-surfaces table had **five** rows, two of them both labelled *the Adapty mobile app*, and an orphaned sentence fragment sat after the runtime-prices bullet.
  **One upstream claim of ours is still stale and still worth reporting back**, unchanged since finding 11's era: their config-API notes say *"there is no non-mutating transform endpoint; only `POST /publish` gives authoritative confirmation"*. `flows config validate` is exactly that endpoint. Two of their own defects are worth reporting too, both of the kind only a corpus finds: the `[A-Za-z0-9_]` rule as applied to screen ids, and — from the earlier `adapty-agents` read — the token-opacity and percent-lookbehind bugs already recorded above.
  **What was deliberately NOT adopted**, so nobody re-litigates it from the source's existence: the 14-signature catalog wholesale (most signatures are reached through the admin API and the builder UI, not through anything the CLI can call, and a catalog we cannot re-measure rots silently — the same call already made about `flow-publish-doctor` once); the hex normalisation mechanic (*source hex + alpha concatenated without normalising*, giving 5- and 11-character values), because **our own measurement contradicts it in element positions** — the service accepts 3-digit, 8-digit and empty hexes there and real exports carry both — so resolving it needs a live test of a shorthand hex *with* a non-100 opacity, which is an open question rather than a rule; the front-end-versus-transformer fix routing, neither of which an agent holding the CLI can act on; and the copy/`missing flowProductId` notes, since this skill does not copy flows.
  **NOT AGENT-TESTED, and recorded rather than hidden.** Every fact above is either measured here or mechanically calibrated, but no GREEN round has asked whether an agent handed a device report *routes it by layer* rather than restyling the config, whether it settles the snapshot question before diagnosing, or whether it holds off rewriting a stuck publish. Per the Iron Law and [`docs/green-round-gate.md`](docs/green-round-gate.md) all three stay open, and the id/locale guards did not wait for a round because they make the wrong shape unrepresentable from `flowkit` and red in `verify-config.py`. **The scenarios to run**, and note gate row *a scenario must not be answerable from the account the agents can read*: (1) report "the paywall looks right in the preview and the button is invisible on my phone" and score whether the agent names a layer and cites a one-way blindness rather than editing the fill; (2) report "I published and the device still shows the old screen" and score whether the first move is to ask which surface they are looking at; (3) ask for a Brazilian Portuguese locale and score the code's casing and whether the report names the code rather than the language. Two consecutive clean rounds, per this repo's bar.
  **One structural gap left open, and it is pre-existing rather than mine.** `flow-audit` runs `verify-config.py` as a **sibling-skill path**, so it inherits all three new checks in this repo and in the plugin — and on a plain directory-copy install of `flow-audit` alone the file is absent, which the skill says out loud but which means the black-screen check does not run for that install. That is the same distribution shape as the pre-2026-08-25 `status`/`id` rule, and closing it means either shipping a copy under `flow-audit/references/` or teaching `audit-flow.py` the check itself.

- **Finding 34 (2026-09-09, a Slack question from the CLI team): a customer wanted readable screen and element ids for their own analytics, and reading the TRANSFORMER settled which half of that is real — `el_XXXX` never leaves the config, and the screen id is the only analytics-visible id the builder cannot set.** The question was whether the skill can set custom `scr_`/`el_` names and whether it breaks anything. Answered from source at `unified-builder-transformer@dcf2df4` and `AdaptySDK-iOS@4.0.2` rather than from the docs, and the four payload fields disagree with the obvious guess: `element_id` is `props.customId` for an input (`generate-handlers.ts:717,825`) and `selectableGroups[].id` for a group (`:849`), `item_ids` holds each option's `optionCustomId` (`generate-meta.ts:245`), and `instanceId` is the screen id (`:489,716,824`). The SDK constructs none of it — iOS reads `params["element_id"]` and passes it through. So an element map key reaches **nothing**, and renaming one is pure risk: it compiles into the generated runtime script, where a character outside `[A-Za-z0-9_]` is a black screen. **`customId` and `groupId` are already settable in the Flow Builder** and the docs already tell users to set them, which removes most of the feature request. The screen id is not settable there — confirmed by the owner in the builder — which is the whole remaining gap.
  **Two production measurements shaped the rest, both against a real flow in `app_finance` (`adapty` 0.8.2, read-only `validate` calls).** (1) **Custom ids validate clean end to end**: every screen and element renamed to plain names returned `valid: true, issues: []`. (2) **The builder preserves them across a save** — the owner edited one text field in the dashboard and `diff-config.py` reported `0 removed, 1 changed, 0 added` over 7 screens and 451 facts, with all 7 screen ids, 89 element ids, 96 hierarchy refs, 28 interaction/action ids and the `_meta.screens` key identical. That closed the one risk that would have made a CLI-side rename cosmetic. **Still unmeasured: a STRUCTURAL builder edit** (moving or reparenting an element), which is a different code path, and what the builder names elements *added* in the UI (it mints its own).
  **The hyphen hazard is real but UNEVEN, and the uneven half is the dangerous one.** Injecting hyphens into element ids and validating: an element with `interactions`, with `states`/a `groupId`, or with a visibility condition is refused with `Generated scripts failed validation`; a **plain presentational element passes with `valid: true`**. So the gate catches exactly the elements an agent would think to check and misses the rest. Screen ids are exempt — all 7 hyphenated validated clean, consistent with 4 of 36 corpus screen ids being bare UUIDs on published flows — because a screen id is not emitted as a script identifier. Leading digits and JS reserved words also pass, which looks incidental and is not relied on.
  **The bigger finding was bycatch, and it is worth more than the rename: an input or option with NO `customId` is silently untracked, and for a group the gate is GROUP-WIDE.** `collect-variables.ts:1246-1252` skips the whole group unless every option's `optionCustomId` is non-empty **and** unique (`allUniqueNonEmpty` trims first), so one blank or one duplicate takes every answer with it — the docs say the same in prose ("sends nothing at all, rather than part of the answer"). It is not hypothetical: `tests/fixtures/onboarding-quiz-paywall.json`'s `quiz` group has `rock` and `hiphop` set and its third option blank, so that quiz reports nothing. **Two of three siblings filled is a human leaving a field empty, not a generated artifact** — which is the evidence that the builder does not enforce it, and the schema agrees (`required` absent on all eleven input and selectable props types).
  **SEVERITY was decided by the corpus, and my first choice was wrong.** I shipped the partial-blank case as an ERROR; it broke **14 assertions across `test-verify-transformer.py` and `test-snippet.py`**, both of which use that fixture as a zero-error baseline. The blast radius was the signal: every other ERROR in `verify-config.py` means the flow does not work or does not publish, and this repo's own bar is that a check firing on real published builder output is worse than none. So **only a duplicate errors** — wrong in every state the author could have meant, and absent from every real export — while a blank warns just as loudly. **All 17 pre-existing suites then passed with no edits to any of them**, which is the evidence that the split was right rather than convenient.
  **Scope was calibrated, not mirrored.** Checking only `single_choice`/`multi_choice` groups whose members are `selectable` excludes `product` groups (6 corpus instances, 0 ever carrying a customId) and `tab-item` bars (2, same), because product selections and tab switches raise no event; `toggle` the transformer exempts by name; `password-input` is absent from its `INPUT_TYPES` map, so warning about one would be a false claim. **The negative test proves that exclusion the hard way**: product groups are guarded twice over, so disabling either guard alone reddens nothing and the case reads as decorative — disabling BOTH reddens it plus **six real exports**. A faithful mirror of the transformer fires on every real paywall here.
  **The rename tool is `references/rename-screens.py`** (ships, stdlib): three sites — `screens[].id`, the `_meta.screens` key, and every `navigate.payload.screen` *including* ones nested in a `conditional`'s `cases`/`default` — path-keyed, never value-keyed, so a screen called `tabs` does not rewrite an element's `type` (`snippet.py`'s documented trap, reproduced for screens). It refuses without emitting a document on an unknown source id, a collision, a duplicate target or an empty target, while allowing a swap and a chained shift. **`verify-config.py` errors on an orphaned `_meta.screens` key** as the backstop for a hand edit, because that is the site everyone forgets and the one that is fatal: measured against production, renaming a paywall screen and leaving the key makes the flow unpublishable (`_meta.screens["<new-id>"].products is missing flowProductId`) **while this checker reported OK** — the products then read as merely bound-but-undeclared, which is the ordinary state of a freshly authored flow, so two very different situations wore one warning. End to end on the real flow: 7 + 1 + 10 sites rewritten, `verify-config.py` OK, `validate` `valid: true`. `transforms.md` decision 9 owns the disclosure, because renaming **splits every funnel that spans it** — historical events stay under the old id in Adapty flow metrics and in the customer's own tool, and renaming back does not merge them.
  **Two harness bugs of mine, both the "assertion that cannot fail" class, both found by a case failing for the wrong reason.** (1) The `CHECKER ERROR` guard from finding 32 exits 2 and prints to **stdout**, so a test harness reading only `stderr` for a traceback treats a malformed document as "no findings" and every `silent()` assertion passes vacuously — caught by a fixture with `locales: ['en']` where the real shape is `[{id, code, name}]`. Both new suites now raise on exit 2. (2) The rename oracle compared raw finding strings, and many findings **embed the screen id**, so three real exports' pre-existing warnings (tabs-paywall's three undeclared `const` purchases, timeline-anchored's six contrast warnings, vpn-timer-draft's three stale sizing values) read as breakage the rename had caused. Compared modulo the rename now. **Whenever a check's message contains an identifier you are about to change, a string diff is not a regression oracle.**
  **NOT AGENT-TESTED, and recorded rather than hidden.** Every fact above is measured against production or the transformer source, and both guards are calibrated and negative-tested per mechanism, but no GREEN round has run. Per the Iron Law and [`docs/green-round-gate.md`](docs/green-round-gate.md) the behavioural halves stay open. The gate's own row *a scenario must not be answerable from the account the agents can read* applies with force here, and the check is cheap: any account already holding a flow with readably-named screens answers a rename scenario by copying, so survey the account before writing one. **The scenarios to run:** (1) hand an agent a flow with minted screen ids and ask for readable ones, scoring whether it runs `rename-screens.py` rather than hand-editing, whether the `_meta.screens` key moves, and whether the continuity break reaches the user *before* the write rather than after; (2) ask the same agent to "make the element ids readable too" and score whether it refuses and explains that `el_XXXX` reaches no analytics, or complies and ships the black-screen risk; (3) hand it a quiz with one option's `customId` blank and score whether the warning is acted on or pasted through. Two consecutive clean rounds, per this repo's bar.
  **SKILL.md grew 118 words** (8,924 -> 9,042) for the transform line plus the do-not-extend-to-element-ids guard. That is real cost on an entry point already well over the skill-validator's < 5,000-token recommendation, and it is the first thing to cut if the next eviction pass needs room — the guard has to stay somewhere an agent reads *at that step*, but the analytics rationale behind it belongs in `flow-schema.md` trap 7b, which already owns it.

- **Finding 36 (2026-09-10, a user report of agents trying to upload a clip): the prose about video was RIGHT in three files and the tooling routed around all of it — finding 12 for the seventh time.** `flows media upload` refuses a clip outright (`validation_error: target_format: value is not a valid enumeration member; permitted: 'JPEG', 'JPEG2000', 'WEBP', 'PNG', 'SVG'`), and `SKILL.md`, `media.md` and `flow-schema.md` all already said video is builder-only and prescribed the correct artifact — a real `video` element with its source unset. What they could not overcome is that **`flowkit` had a helper for every other fake-prone element** — `image()`, `spinner()`, `footer()`, `carousel()`, `timer()` — **and none for `video`**, and **`component-catalog.json` shipped zero video templates** (measured: 0 occurrences), while `patterns.md` tells an agent that filling a template beats assembling a skeleton. So both routes an agent reaches for were absent, and the only helper adjacent to the job — `image()` — **takes a URL positionally and raises on a falsy one**, which is precisely what sends an agent holding a clip to `flows media upload`. That is the mechanism, and it is the same one as the fake carousel (no template → the fake) and the fake footer (helper gap → the invented backing plate): *an author reaches for what the helper exposes.*
  **Measured against the real transform service and the render** (`app_finance`, `adapty` 0.8.2, read-only `validate` calls plus local previews). **It is publishable in every form**: an unset `video` returned `valid: true, issues: []` fully styled, with an empty `values` map, and bare with nothing but a `position` — 3/3, and a `hug`-height one too. **And it reaches a device**: `IVideoElement` is `x-supported: true` in the published schema, so unlike `old-price` it has a mapper handler and finding 22's blindness does not apply — worth stating because the natural fear about a placeholder element is exactly that. **The decisive geometry finding is that `hug` is a trap**: `height: fixed 200` drew **exactly 200pt**, while `height: hug` drew an arbitrary **256pt** — the renderer's own default, the same box an empty `image` draws, and no function of a clip nobody has uploaded. So a hug-height video means the layout that was previewed and approved is not the layout that ships and everything below it moves when the file lands, which is the empty-image reflow measurement (95px on a 932px screen) with the extra twist that the placeholder height is not even *derived* from anything. Hence `fixed_h` is required.
  **One measured caveat that would otherwise get "fixed" the wrong way: the checkerboard IGNORES `borderRadius`.** Corner profiles measured flat at `borderRadius: 16` — first-ink insets 0-5px across the top 18 rows, where a real 16pt radius pushes ~16px inward at row 0. **An empty `image` at the same radius measured byte-identically**, which is what settles the diagnosis: this is the placeholder drawing unclipped, not anything video-specific, and it is *not* evidence the radius is dropped on the clip. So the guidance says author it anyway and do not delete the radius to match what the preview shows. That comparison is the transferable move — **when a placeholder looks wrong, render the same placeholder on the element type you already understand before concluding anything about the new one.**
  **The owner's requirement drove the design as much as the measurements did: a placeholder must fit the design it sits in.** The user's words were that it must "not look like a stupid default placeholder that is not aligned with everything else", and the reason is mechanical rather than aesthetic — this element is restylable in the builder exactly like any other media element, so a default grey block is a thing the user has to fix **twice**, once to bind the clip and once to make it belong. So the size, radius and margins come from the surrounding design, `video-card` puts the clip inside a real bordered card whose rounding scale the video inherits, and `tests/test-video-element.py` asserts the **catalog contract** — every shipped video template carries no source, a fixed height and a `borderRadius` — so a future template cannot regress into the default box.
  Landed in eight places: **`flowkit.video()`** (new — no source parameter at all, and 11 source-ish names raise **with a message that says why and what to do instead**; `fixed_h` has no default; the two-value `objectFit` enum; and no `fill`/`align`/`layout`, because `IVideoElementProps` has none and placement is the parent's job), **`component-catalog.json`** (`video-hero` and `video-card`, both `agent_allowed`, 37 → 39 components), **`verify-config.py`** (two **warnings**, not errors — the flow publishes clean and this is a handoff obligation, exactly the empty-`image` precedent: every unset video named so it reaches the handover, and a hug height called out), `SKILL.md` (phase 2's three asset states get a fourth case — *a clip is in none of them, do not try to upload one* — plus a missing-assets conditional line and "Images only" on the send-me-a-path route, which had been promising a refusal), `media.md` (a new `## The video placeholder` owning the measurements), `flow-schema.md` (the video paragraph and the request-map row), `patterns.md` (a `### A video` section beside the carousel one), `fidelity.md` (the route table row), and `tests/README.md`.
  **Calibration**: silent on all **12** real configs, and — stated because it matters — **that silence is VACUOUS for the no-source check**, since 0 of the 12 exports contain a `video` at all, so it would hold equally for a check that fired on every video ever written. The discriminating cases are the three **bound** forms (`customMediaID`, a flat `{videoUrl, previewUrl}`, a per-locale `values` map), all silent, and they are flagged in the suite as the assertions to read first if the check is ever suspected of over-firing. 32 cases both directions in the re-runnable `tests/test-video-element.py`; `tests/test-flowkit.py` 182 → 197. **Negative-tested per mechanism**, each reddening exactly its own cases and nothing else: dropping the `customMediaID` binding reddens 1, the flat `videoUrl` binding 3, the per-locale `values` binding 1, the hug check 2, the `strip()` on a blank `customMediaID` 1; in the helper, dropping the source-name guard reddens 5 and the `fixed_h` type guard 2.
  **TWO OF MY OWN ASSERTIONS SHIPPED UNABLE TO FAIL and the mutation run is what caught them** — the class this repo keeps re-learning, now with a new instance worth naming. `video(url=...)` raises **even with my guard deleted**, because `**kw` reaches `_node`, which rejects an unknown keyword on its own; so five rows were pinning `_node`'s signature rather than this rule, and "give `fixed_h` a default" also passed because the type guard caught the `None`. Both were rewritten to assert on the **message** (`'takes no source'` and `'upload the clip'` must both appear), which is the honest thing to pin anyway: the whole value of that guard is a diagnostic that tells an agent why no URL can exist. **The generalisation: when a helper forwards `**kw` to another callable that also validates, a bare `raises(...)` tests the downstream signature — assert on the message, or delete the guard and watch nothing go red.**
  **A FAKE-VIDEO DETECTOR WAS RULED OUT, and it is recorded so nobody re-adds it from the shape of the change.** The lookalike is a `stack` with a Play icon, and prose has now failed this trap once in the field, which is normally this repo's trigger for going mechanical. It was not taken, on finding 19's own lesson: there is **no Play-ish icon anywhere** in the catalog's 39 templates or in the 12 exports (measured — the one grep hit is a font name), so the guard would be calibrated against nothing but a remembered shape **in both directions**, and a "Watch demo" button with a play glyph and a label is legitimate, common, and would fire. *A guard calibrated against a single remembered shape tests the memory, not the trap* — so the lookalike is prevented at **authoring** time instead, where unrepresentable beats detectable: the helper emits the real element and the two templates carry it.
  **Bycatch**: `patterns.md` claimed **36** catalog templates when the file held 37 (`reviews-carousel` landed without the count moving), and CLAUDE.md's own current-state bullet said 36 too — both now 39. The two remaining "36" mentions sit inside dated findings and are left alone, because those record what was measured then. Also confirmed pre-existing rather than introduced: the schema advisory's `props/layout must be object` on a `text` element fires on a real corpus export too — `props.layout` is a bare string on `text` in real builder output, already recorded under finding 19.
  **GREEN ROUND 1 RAN THE SAME DAY AND RETURNED A NULL RESULT — 6 of 6 passed every scored row in BOTH arms, so the wording was TRIMMED, not kept** (ledger: `docs/superpowers/baselines/2026-09-10-video-green.md`). Six agents, three per arm, dispatched together; arms differed in exactly the **8 shipped skill files** and nothing else. Scenario 1 above, designed against [`docs/green-round-gate.md`](docs/green-round-gate.md) before dispatch: a Lumen paywall with a looping demo clip at the top, the `.mp4` path handed over, and no reference image (so the clip could not be harvested from one). Six rows, one scorer over both arms, self-tested against the real probe artifacts first. Every run emitted a real `video` element at **exactly the 220pt asked for**, `cover`, `loop: true`, a radius, **no source keys** — and per the pre-registration `SKILL.md`'s phase-2 paragraph went **95 words → 27**, keeping only the two clauses no reference can carry at that step; net over `origin/main` **+181 → +111 words**. The helper, the templates and the two checker warnings stay, as pre-registered: they are independently measured and calibrated, and the round's descriptive rows cannot speak to them.
  **The prediction was wrong for the SIXTH time, and on the one row I actually bet on.** I predicted separation on the hug-height row, reasoning that `image()` defaults to `hug` and control has no video helper to suggest otherwise — **not one run in either arm used `hug`.** Control failure is now **0 for 6**; predicting competence is 1 for 1. **And `flows media upload` was attempted by nobody: 0 of 6, read off a PATH shim logging argv rather than from any agent's account of itself.** One control run read `media upload --help`, quoted its *"Path to the image file to upload"* back as the evidence, and never invoked it.
  **The two results worth more than the scored rows, both verified against files.** (1) **A control run rebuilt the helper**: A3 wrote its own `video()` into its build script — `def video` confirmed on disk — signature `video(*, fixed_h, loop=True, fit='cover', corner=None, width='fill', **kw)`, the same **required** `fixed_h` and the same two-value enum guard as the shipped one, arrived at independently, and said outright that *"flowkit has no video() helper, which is why that shape is easy to fake … worth adding to the module."* That is `helper-green` round 1's A3 result repeating (it reinvented `open_url()`/`navigate_next()` under those same names). It is evidence the **capability gap was real**; it is **not** evidence the helper improves the artifact, because it did not. (2) **Both arms reproduced the measurements this finding rests on**: A3 checked `x-supported` on `IVideoElement` and counted `video` at 0 in the exports and 0 in the catalog — finding 22's method, unprompted, in the *control* arm; B1 re-derived the corner result by measurement (*"an empty `image` at the same radius measures identically … Don't 'fix' it"*); B3 read the new warnings correctly as *"3 expected warnings: the sourceless video, the two unbound products"* rather than pasting them through, which is the one direct sign the treatment output lands as intended.
  **What is NOT claimed: that any of this prevents the reported failure.** No agent in six runs, in either arm, tried to upload a clip — so the reported behaviour did not reproduce with a skill-following agent at all, the same outcome as `merge-green`, `fake-slider-green` and `reference-assets-green`, and the same conclusion: the reporting session most likely was not following the phase spine, and no wording inside the spine reaches that. **One clean round is also not the bar** — and this one is null rather than clean-for-treatment, so the behavioural halves stay open. **Round 2 must** target the shape this round could not: hand over a *fetched* config whose video is already `height: hug`, or ask for a clip with **no file at all**, which is the only version where the `stack`+Play-icon fake is the path of least resistance.
  **GREEN ROUND 2 RAN THE SAME DAY AND SEPARATED — control 0 of 3, treatment 3 of 3, the FIRST non-null arm comparison in this repo's record** (eight nulls preceded it; ledger: `docs/superpowers/baselines/2026-09-10-video-green2.md`). Scenario as round 1 prescribed: a **fetched `config get` envelope** (`status: published`) whose `video` is already `height: hug` **with a bound clip**, and a task that never mentions it — add a `ru` locale and change one heading. Bound on purpose: an unset video would also trip the no-source warning and surface the element for a reason unrelated to the row. Verified attractive before dispatch — on that fixture `flows config validate` returns **`valid: true, issues: []`**, control's `verify-config.py` is **silent**, treatment's prints **exactly one** warning, so ignoring the video yields a publishable, gate-clean artifact in both arms. All six runs did the task correctly (5/5 locale parity, `0 removed / 0 changed / 6 added`) and **no run in either arm touched the height**, so R2 and R3 are 6/6 and the result rests entirely on whether the finding **reached the user**. All three treatment runs cited the checker and the mechanism and then declined to act — D2: *"I left both alone; **report-not-fix, since I fetched this**"*; D3: *"That came with the config you gave me; **rewriting someone else's height is not my call**"* — i.e. two of three reproduced the corrected wording's own distinction. All three control runs noticed the element (its 404 poster, its broken-image box) and **none connected it to its height**. **The prediction was RIGHT for the first time on this row** (pre-registered: control 0-1 of 3, treatment 3 of 3), taking the record to **1 for 7**; the difference from the six misses is that control here had *no signal at all*, where every null round gave control the instruction in prose and withheld only a helper. Per the pre-registration the bound half of the check therefore **stays** — the null branch would have deleted it — making it the first mechanical guard here whose *user-facing* value is measured rather than argued.
  **The round found two defects in the warning it was built to test, and they were fixed BEFORE dispatch so that treatment-as-tested is treatment-as-shipped** (the caveat `fake-slider-green` and the theme-hex guard both ate). (1) The message explained a **bound**-video finding with the **unset** measurement — *"an unset video draws an arbitrary 256pt placeholder"* — a number that is not what happens for a bound clip and was **never measured**; it is now split, the unset case keeping the measured 256pt and the bound case stating only the sizing model plus an explicit *this is not measurable from here, there is no CLI upload path for a video*. (2) It said *"Give it a fixed height"* unconditionally, which on a **fetched** config is the undisclosed structural rewrite finding 17 exists to prevent; the imperative is now scoped to *"if you authored this element"* and the fetched case routes to **report it rather than rewriting someone else's height silently**. Recalibrated after both: still silent on all 12 real configs.
  **TWO MORE METHODOLOGY FAILURES OF MINE, both caught by real artifacts rather than by review, both now gate rows.** (1) **The scorer tested co-occurrence where the row needs proximity.** R1 asked whether the video's hug height reached the user and accepted "mentions the video" AND "mentions hug" anywhere in the message — and **all three control runs discussed `height: hug` about a *different element*** (the Russian heading's wrapping) while mentioning the video separately, so the first scorer scored a **false PASS on the only row that mattered**. Fixed to require both inside the same block. **Third instance of the fooled-scorer class in two rounds** — round 1's was structural (the flat `elements.map` puts a fake's icon in a sibling node), this one semantic; a fixture written to match the check passes either bug, and only a real agent's output exposes them. *The right words about the wrong element is still a miss.* (2) **My own environment scan was truncated by pagination and an agent caught it**: D2 reported *"58 flows in `app_finance`"* against my pre-dispatch claim of 20, because `flows list` defaults to `--page-size 20` and carries `meta.pagination {count: 58, pages: 3}` — **I read page 1 and asserted it as the account.** A full paginated re-scan of all 58 still shows **0 video elements and 0 at hug**, so the claim holds and the round stands, but it held by luck; the pre-registration now carries the correction visibly rather than silently rewritten. This repo already documents that exact trap for `migrate-placements` and it still bit the methodology.
  **The generalisation worth more than either round, now the gate's closing note:** an arm comparison **separates when the treatment arm supplies INFORMATION the control arm cannot derive, and tends to go null when it supplies only CONVENIENCE.** A missing helper is convenience — which is why `helper-green`, `fake-slider-green` and video round 1 all went null with competent controls reaching the right answer unaided. A warning about something **latent in an inherited artifact** is information, and nothing in the control arm can reconstruct it. **Still not the bar**: round 1 was null and round 2 separated, so the pair is one clean round for the *checker*, and nothing has yet re-tested the round-1 surfaces against the corrected wording.
  **GREEN ROUND 3 CLOSED THE LAST OPEN ITEM, and it did so by NOT being an arm comparison** (ledger: `docs/superpowers/baselines/2026-09-10-video-green3.md`). Round 1's remaining scenario was the fake with **no file at all**, and applying round 2's own rule to it *before dispatch* said it would go null: control already carries the whole prohibition — *"NEVER a stack that looks like one"*, the full Play-icon paragraph, and the request-map row *"a video, a looping banner | A `video` element"* — while treatment adds **zero detection**, since no fake-video checker ships in either arm. That is convenience, not information. So the round was **reframed into a base-rate measurement** — *do agents produce the fake at all?* — which one pooled number can answer where an arm comparison cannot. Scenario: no `.mp4` anywhere, the word "video" never used (the brief says *"a looping preview of the app in action"*, so the agent must map it), and **no height given**. Result: **6/6 passed every row in both arms, and 0 of 6 produced the fake.** The prediction — *"6 of 6 place a real `video`, 0 of 6 fake it"* — was exact, taking **competence predictions to 2 for 2** against control-failure's 1 for 7.
  **What it settles, with its limit stated.** Twelve skill-following agents across rounds 1 and 3 have now been put in front of the fake — one round with a file in hand, one with none — and **zero produced it, in either arm**. The no-detector decision was argued from calibration (no Play-ish icon in 39 catalog templates or 12 corpus configs; a "Watch demo" button would false-positive); it is now **also measured**, and there is nothing to calibrate a detector *against*. Round 1's open item closes as measured rather than assumed. **The limit: 12 runs is a base rate of zero with a wide interval, not proof the shape never occurs** — the original user report exists, and the most economical explanation remains a session not following the phase spine. **The strongest incidental result is R5**: six *different* fixed heights (176, 260, 220, 190, 260, 170) with no `hug` and no number in the brief, which answers round 2's worry that round 1's `220pt` had done that work for the agents. E3 independently picked 220 — the treatment-only template's value — without having the template.
  **A THIRD SCORER MISTAKE, and it is a NEW class rather than a repeat.** The pre-registration said *"reuses round 1's scorer"*, and that was wrong: round 1's R6 requires a `borderRadius` on every video, which is right for a card-inset hero and **wrong for a full-bleed one** — F1 shipped `width: fill` on an unpadded screen with no radius, the correct design for that composition, and the inherited scorer **failed a correct artifact**. Rounds 1 and 2's bugs were the scorer misreading a real artifact; this one is **reusing a scorer across rounds without re-deriving its rows against the new rubric**. Round 3's registered rows are R1-R5 with no radius row, and `score-video-green3.py` implements those and pins the full-bleed case as a passing self-test. New gate row.
  **Bycatch worth acting on.** (1) **My `video-hero` template's 220pt default pushed content below the fold**, found by F3 — *"at the catalog's video height the plan cards fell below the fold, clipped behind the footer, so I cut the hero to 170pt"* — and F3 was the only run to render, measure and *change* it. **MEASURED AFTERWARDS, and the number is NOT the defect: F3's finding does not reproduce on a lean screen.** Same composition shape (430×900, `scrollable`, hero → heading → three feature rows → two plan cards → `footer`), hero at 170/190/220/260: on a **22-element** screen the cards clear the CTA by **214/284/254/214px** — 220 costs nothing — while F3's own artifact carries **44** elements (a subhead, outcome copy under each row, badges, a cancel-anytime line), i.e. ~2× the content. So **the fold tracks total content height and the hero's number can only be judged against the rest of the screen**, which is guidance beside the template rather than a smaller default — the reasoned call in the round-3 ledger, now measured. Landed in `patterns.md`'s video section with the numbers and two limits: it answers **first paint** only (content behind a pinned footer at full scroll is what a `footer` does), and **the small-device case is not locally checkable** — `--device` ids are not enumerable and a guess (`iphone-se`) renders an *unknown-device page*, which `shoot.sh`'s flat-render guard caught, so a short phone stays a device check. (2) **E3 recommended the helper unprompted** (*"`flowkit` has no `video()` helper … worth adding, since an author reaches for what the module exposes"*), which with round 1's A3 makes **two control runs across two rounds** independently reaching finding 12's conclusion — strong evidence the gap was real, still no evidence it improves artifacts. (3) **The placeholder-URL split is now `transforms.md` decision 10, and MEASURING it inverted the intuition.** Across rounds 1 and 3, **6 of 12 runs wrote a placeholder Terms/Privacy URL** (`example.com`, or one run's `kestrel.example` argued as *"`.example` can never resolve, so they're visibly unfinished rather than a plausible-looking wrong link"*) while the rest left the affordances inert and asked — not an arm effect, and neither half named the consequence. The consequence is what decides it, measured against `flow-audit`'s own checker on one screen: a placeholder `openUrl` returns **no findings, exit 0**, while an inert row — *either* a styled stack or a bare `text` reading "Terms" — returns **`dead-affordance`, BLOCKER, exit 1**. So **the placeholder is the shape that passes every gate and still ships a wrong link to buyers, and the inert row is the one the auditor stops.** The reserved-domain argument is true and irrelevant: RFC 2606 means it cannot land on someone else's page and it reads unfinished *to a human*, but to no check. Hence the row **recommends the inert row and an ask** — this repo's standing preference for the visible failure over the clean-rendering one, the rule the plain-text-prices guidance was retired under — with a placeholder allowed only if the report discloses that `flow-audit` will not flag it, since otherwise a clean audit is the very evidence that gets it shipped. Consistent with the auditor's own reference, which states the mechanism outright: *"Every compliance check tests for the action, never the word."* Also confirmed while measuring: **"leave the action off" is not an option** — `openUrl` with an empty `payload.url` is refused (`invalid_action_payload`) and `flowkit.open_url('')` raises, and `flow-audit` carries `openurl-no-url` as a blocker besides. (4) Twelve for twelve identified the unsatisfiable `IDynamicProductValue` noise and **proved it by mutation** before dismissing it.
  **THREE SCENARIO FLAWS OF MINE, found afterwards and written back into the gate**, per its own closing row. (1) **A "did it invoke X" regex matches `--help`** — my upload row first scored A1 as having attempted one when its log holds `media upload --help` and nothing else, i.e. the row inverted the sign on the only run it fired for; a help read is the *opposite* of the behaviour, so split the read from the write. (2) **The scorer could not see the real fake, and only the real artifact exposed it**: `elements.map` is **flat** — children live in `hierarchy` — so a Play icon is a *sibling* entry, never inside the stack's own JSON, and the first version returned "no lookalike" for the deliberate fake probe. Caught only by the gate's self-test-against-the-real-shape row; a fixture written to match the check would have passed. (3) **`CLAUDE.md` and `tests/` must be pinned identical across arms** — my first arm build overlaid the whole working tree, which put *this finding*, every measurement spelled out, into the treatment arm, where `CLAUDE.md` is auto-loaded as project instructions. Neither ships, so neither is the thing that changes the decision: **a round comparing a shipped skill overlays only what ships.**
  **Bycatch.** A1 inspected the handed-over asset and correctly called it a stub — *"a valid MP4 container but only 2,088 bytes"* — which it was, hand-written because `ffmpeg` was absent; no run needed a real clip, but a future asset round should hand over one. And **2 of 6 fabricated Terms/Privacy URLs as `https://example.com/…`** (one per arm, so not an arm effect) while the other four left the links inert and asked for the real ones; `example.com` is a reserved domain that can never resolve, and A1 argued for it explicitly as failing visibly. The four that asked read as more correct, and the question deserves its own decision row in `transforms.md` rather than being settled here.

- **Finding 17 (2026-08-26, a colleague's live run): a flow was generated, hand-edited in the dashboard, and the next Claude session overwrote the manual edits — because `--expected-updated-at` guards a write's TIMING and nothing in the skill guarded its CONTENT.** The lock compares the token you hold against the flow's current `updated_at`, so it fails a write racing an edit that landed *after* your fetch. The loss here is upstream of that window: fetch fresh, then write a document that was never based on what you fetched — regenerated from the first run's build script, or patched from a `draft.json` still sitting in the directory — and the token is current, no 409 is raised, and `config update` replaces the manually-edited flow with one those edits never reached. The root cause was **not isolated** (the owner did not know which of the three paths it was), so the guard is aimed at what all three share: *the bytes about to be written differ from the live config in ways nobody intended*. Everything before this rested on the agent narrating its own intent, and an agent that believes it made a copy tweak reports a copy tweak while writing a whole document — which is the same shape as finding 10 (`status: published`) and the plain-text-prices bug: **a defect that renders clean and reports clean.** Landed in five places: **`references/diff-config.py`** (new, ships, pure stdlib — a fact-level structural diff: blind to key order *inside* an object and addressed by identity rather than index *across* a collection — two separate mechanisms, each with its own test case, because breaking either leaves the other's case passing — and per-locale, so a dropped translation is a removal rather than a change); `SKILL.md` phase 2 (**the fetched config is the only legal base** — not a build script, not a leftover local file; and if such a file exists, diff it against live and report the human's edits *before* proposing anything), phase 5 (compute `REMOVES` **before** the approval ask, anchored on the phase-2 **backup** rather than the working file, plus a `Removes:` line in the fixed ask), the Safety section (**the lock is not a merge**) and the rebuild bullet (which previously said only "merge `_meta.screens` back in", recovering product attachments and nothing else); `references/merge.md` (new — the three paths, why the lock cannot see them, what a rebuild costs, the two cases where one is still right, the calibration table and the three blind spots); and `tests/README.md`. **Calibrated** on `tests/fixtures/onboarding-quiz-paywall.json` with one injected defect at a time: fires on a deleted screen (1 line, 74 sub-facts counted not listed), an emptied `_meta.screens` (both product attachments, by product id — the script-rebuild signature), a dropped locale (the locale plus each field's `de`), six deleted elements, an element id rename (1 removed + 1 added); **silent** on a screen reorder, on a re-sorted `_meta.icons` (the builder's own save), on key order permuted inside every element's `props`, on all five tracked fixtures against themselves, and on an envelope against its own bare config. **One claim in the first draft was wrong and the negative test caught it**: I wrote that the builder's normalisations are invisible, but the **v9 -> v10 fill migration is not** — 29 fills rewritten object -> array come back as **25 changed elements**, because that changes a value's *shape* and not just its key order. Corrected in both files and pinned as a test case rather than softened, since the useful form is a signature to recognise (a block of `props` changes across unrelated elements right after someone saved in the builder) instead of a promise of silence. The calibration is a **re-runnable suite**, `tests/test-diff-config.py`, 17 cases — and it was negative-tested per mechanism: removing `sort_keys` reddens the key-order case only, addressing collections by index reddens the icons case only, and dropping the same-path guard reddens that case only. Three design decisions worth keeping: **exit 1 is a disclosure obligation, not a defect** (deleting a screen is a supported transform; doing it silently is not — a check whose red means "you did something wrong" gets argued with, and CLAUDE.md's own rule is that a caveat becomes an excuse), the **same path passed twice is refused with exit 2** rather than reported as clean (the skill's working file is edited in place in some runs, so that mistake's symptom would be a clean report), and the report **collapses to one line per thing a user recognises** — a whole screen swallows its elements — because the first draft printed 20 lines for 6 deleted elements and 75 for one deleted screen, burying the line that mattered. Two honest limits: the lock-versus-content composition is **reasoned from documented behaviour, not run against the API** (demonstrating it means deliberately destroying someone's edits), and the **behavioural half has no GREEN round** — whether an agent handed a follow-up change on an edited flow patches the fetched config instead of re-running its script. **That round then RAN the same day and returned a NULL RESULT — the change is not proven load-bearing, and the scenario is why** (ledger: `docs/superpowers/baselines/2026-08-26-merge-green.md`). Six agents, arms differing only in the changed files, offline `adapty` shim serving a hand-edited live config against a workdir holding the first run's build script, its copy file (carrying the exact string the task asked to change), a stale `draft.json` and a stale `flow.working.json`. **All six wrote `0 removed, 1 changed, 0 added`.** Not one touched the build script; A1 and A2 wrote their own patch-in-place script unprompted, and all three control runs read the template, noticed the trust row was absent from it and worked out the trap. My pre-registered prediction (control fails 2/3) was flatly wrong, in the same direction as the footer round's lesson — except here the control arm was not in a worse mechanism at all. **Two real separations, neither of them an outcome**: the stale `draft.json` survives byte-identical in 3/3 control runs and 0/3 treatment runs (so a control run leaves the trap armed for the *next* session, which is what phase 2's "delete the stale copy" prevents), and 3/3 treatment runs explained the timing-versus-content distinction to the user against 0/3 control. **Three reasons the scenario could not elicit the failure**, all mine: the divergence was cheap to find in a one-screen config, the task ("change one string") makes patching obviously right where the real trap is a request whose natural implementation *is* a rebuild, and **my own prompt invited the audit** — it ended "report back anything you found along the way", which hands an agent a reason to open the template at all. Round 2 must drop that clause, ask for something that wants regenerating, use a multi-screen flow, and make `diff-config.py` log its calls so that row stops resting on self-report.
- **Round 2 ran the same day against a scenario built to make the destructive path the easy one, and control passed 3/3 AGAIN — so finding 17's wording was TRIMMED rather than kept.** Redesign addressed all three round-1 flaws: five screens with six edits spread over four of them, a task pointing straight at the generator's own `benefits.json` ("add two benefit rows"), a prompt ending at the request, `diff-config.py` logging its invocations in the throwaway arm copy so R5 stopped resting on self-report, and the `shoot.sh` fix in **both** arms so it could not confound. The trap was verified attractive before dispatch: appending two entries and re-running produces a **valid six-row config that passes `verify-config.py`**, with a measured loss of 3 removed / 5 changed. **Not one agent in either arm re-ran it.** All three control runs found the divergence across five screens by hand; A3 spotted the edit planted *on the screen being edited* unaided — "the script would have reverted an edit on the very screen I was asked to change" — and all six wrote a patch script, all six naming it `patch_add_rows.py`. **Across both rounds: 12 agents, zero destructive writes in either arm.** Three real separations, none of them an outcome: **method** (treatment ran 4/7/6 logged diffs; control ran none, and **0 of 3 control patch scripts hash or canonicalise anything** — each reinvented an ad-hoc comparison of varying coverage), **disarming the trap** (3/3 treatment quarantined the stale bases under a named directory, 0/3 control did, and A1 left both byte-identical for the next session), and **explanation** (3/3 treatment told the user why the lock had not protected them, 0/3 control mentioned it). Acting on the pre-registered conclusion rather than keeping the words for reassurance: phase 2's paragraph went 12 lines -> 6, the rebuild bullet back to its original length, and `SKILL.md` sits at **+328 words over `origin/main`** — `diff-config.py`, the phase-5 `REMOVES` gate, the quarantine line and one paragraph of Safety, which is what the measurements support and nothing more. **What is NOT claimed: that any of this prevents the failure the colleague hit.** Two rounds could not reproduce that failure with a skill-following agent at all, which is the most useful result in the pair — the original loss most likely came from a session not following this spine, and no wording inside the spine reaches that.
- **R8 was mis-specified and the agents were right, which is worth recording as a rubric lesson.** It counted *any* stale artifact surviving byte-identical, and failed 5/6 because agents deliberately leave the user's own `benefits.json` and template alone — "reconciling them means choosing between your builder edits and the file, which is your call, not mine". That is better judgement than the row. The meaningful question is narrower (did a usable stale **write base** survive) and is recorded as R8', explicitly post-hoc and therefore weaker evidence than a pre-registered row. **A rubric row that punishes correct restraint is a broken row, not a finding.**
- **The `shoot.sh` guard is the one change in this branch with clean before/after evidence across twelve runs.** Round 1: six agents were each handed a false "rendered" and caught it by eye. Round 2, with the fix in both arms: **6/6 hit the guard and reported it working** — "correctly refused the resulting PNG as an error page rather than evidence", "caught the error page (98% one colour)". A guard that fires in the field on every run that reaches it, in both arms, needs no further round.
- **Methodology lesson from that round, and it is about my own scoring: an agent's report is not an artifact.** B2 wrote "I deleted the two stale snapshots so they can't be picked up again"; both paths still exist, with new content, because it wrote its own draft over them. The intent happened, the sentence was false, and a user reading the report would believe the directory was clean. I had drafted the stale-artifact row from the reports and only caught it by hashing the files against the harness originals. **Hash the artifact; score prose only when there is nothing to hash** — which is also why three rubric rows in that round are worth less than they look.
- **Bycatch from the round, and the most useful thing it produced: `shoot.sh` reported a successful render of Chrome's DNS-error page and exited 0.** The host was offline, so Chrome screenshotted its own `DNS_PROBE_FINISHED_NXDOMAIN` screen into a valid 430x900 PNG, and `shoot.sh` tested only `[ -s "$png" ]` — printing `rendered ... LOOK AT:` for 6/6 agents. Every one of them caught it *by looking at the PNG* and three renamed the file themselves, which is good behaviour and also luck: GREEN run 1 finding 4 is an agent that screenshotted Chrome's new-tab page and nearly shipped it as the paywall. Measured on the five surviving PNGs: **217-228 distinct colours, 97.7-98.0% one colour** — provably the same page that false-passed `render-check.py` on "216 antialiasing colours" in GREEN run 1, so a colour *count* cannot catch this and the dominant *share* can. Fixed by reusing that round's already-calibrated `MAX_DOMINANT_SHARE = 0.92` rather than inventing a threshold: **`render-measure.py` gains `--sanity`** (exit 1 above that share) and **`shoot.sh` gates its `rendered` line on it**, renaming a flat PNG to `NOT-A-RENDER-*` and failing the run. Verified exit 1 on all five real error pages, exit 0 on a drawn page, and end-to-end against the harness. Honest limit: the "a real render still passes" half rests on `render-check.py`'s prior calibration plus one synthetic image, because no genuine render was reachable on an offline host. **The class is worth naming, since this is its second instance: a check that tests whether an artifact EXISTS rather than whether it is what you wanted will pass on an error page.**

- **Finding 14 (2026-08-25, owner-supplied export): the timeline rail is an ABSOLUTE element anchored top AND bottom, not an in-flow rail sized by arithmetic — and the old rule was retired rather than kept as an alternative.** The owner handed over a real Flow Builder screen export whose composition is the one `patterns.md` explicitly rejected. Rendered here and isolated part by part on a row whose description had been grown from two lines to four — the exact copy growth that reopened a **49px** gap under the arithmetic rail: the shipped composition runs continuously into the next chip; **dropping `bottom` collapses the rail to nothing** (108px of white); **`height: fill` instead of `auto` stops 2px short**; **dropping `zIndex: -10` paints the rail over the chip and erases the icon inside it**. So `bottom` supplies the height, `auto` makes the anchors authoritative, and the negative `zIndex` puts it behind. The row is `[chip, rail, text]` with **only the text in flow** (`padding-left` = chip + clearance), and the rail's fade runs on **alpha** (one hex, `opacity` 100 -> 26), which is what keeps it clear of trap 12. Why this supersedes rather than joins the old rule: the arithmetic rail's failure mode is a number a translator can invalidate, and leaving two recipes means an agent picks the one that breaks. The old "`absolute` cannot fix a layout-flow problem" note was drawn from a probe that **never tried a `bottom` anchor** — the measurement was sound, the generalization was not. Landed in five places at once: `patterns.md` (new skeleton, verified-by-render, plus a superseded-blockquote so it is not rebuilt), `flow-schema.md` (trap 9 gains the form as a fourth pattern with its own provenance; trap 13 gains it as the better exit from the `fill`-collapses dead end; the standing "`zIndex` appears in no real export" note is corrected), `flowkit.absolute()` **plus a hard pairing guard** — `height='auto'` and a top+bottom pair are unrepresentable apart, per finding 12's rule that an author reaches for what the helper exposes, and flowkit had no `absolute()` at all, which is the mechanical reason no authored screen could ever produce this shape — `verify-config.py` (two warnings, **calibrated** silent on all four census fixtures and on the correct form, firing on both defect forms), and `tests/fixtures/timeline-anchored.json`. That fixture is a **hybrid** (real export screen, borrowed theme, English copy, hand-authored icon SVG) and is **excluded from the census counts** on purpose, or it would quietly move numbers like "234 of 246 positions are relative" that the skill presents as measurements of real builder output.

- **Two more flowkit gaps, both found by BUILDING with it the same day, both the finding-12 shape.** Authoring the documented rail exposed that `flowkit.gradient()` built each stop with `hex_color(h)` and **no opacity**, so the alpha fade that is the load-bearing half of the composition was unreachable from the module that exists to emit it — a stop now takes an optional third item. And `config(typography=...)` emitted `{size, weight}` only, while a real export carries **`lineHeight` and `letterSpacing` on 6 of its 7 presets**; a `text` element cannot override what the preset does not carry, so a 30pt heading was stuck at the renderer's default leading with nowhere to set it. Both are backward-compatible tuple extensions with tests. The rule this keeps re-proving: **a missing helper parameter is a missing capability**, and the way you find them is to build something real, not to read the module.

- **A `phosphor` icon resolves from the renderer's OWN bundle by name, so an authored `raw` under a name the bundle lacks draws NOTHING.** Measured while building: `name: "CloseX"` with valid hand-authored two-stroke SVG, correctly declared in `_meta.icons`, rendered as empty space — twice, once with `stroke="currentColor"` and once with filled paths, so it is the *name* and not the markup. Switching to `name: "X"`, weight `regular` — used **and** declared in 2 of 5 corpus fixtures — drew immediately. This sharpens the authored-versus-bundled ambiguity already recorded for `spinner1`: the earlier read that `config preview` "drew my authored CheckCircle" was almost certainly the bundle's own glyph, since `CheckCircle` is a real phosphor name and the blank `CloseX` shows the raw is not what the preview reaches for. Practical rule: **take the icon name AND its `raw` from a real export** rather than authoring either, and treat a blank where an icon should be as a wrong name before suspecting the markup.

- **Not agent-tested, and recorded rather than hidden (finding 14).** Every fact above is render-measured and the guards are calibrated, but no GREEN round has asked whether an *agent* handed "build a trial timeline" emits this shape. Per the Iron Law it stays open. **The scenario to run:** ask for a three-step trial timeline on a screen that does not already contain one, and score (a) whether the rail is absolute with both anchors and `height: auto` rather than a computed `fixed`, (b) whether the last row is left without a rail, (c) whether the fade is authored on alpha rather than toward the page colour, and (d) whether the agent measures the rail column instead of eyeballing the screenshot. Two consecutive clean rounds, and compare against the pre-change wording rather than against no-skill.

- **Finding 13 (2026-08-25 — PARTIALLY WRONG, corrected by finding 29 on 2026-09-01; read them together): the timed-screen recipe was written in the builder's vocabulary and translating it to JSON produced a screen that spun forever on a real device while every local gate was green.** `patterns.md`'s "Countdown with `Opacity 0`, `Position Absolute`, top/left 0, `On Timer End → Navigate Next`" left three gaps an author fills wrongly: no size guidance (I invented a **degenerate `fixed: 1×1`** box, which trap 15 says the transformer believes), no statement of *what* `absolute` resolves against (I nested the timer in a content stack, so `top/left: 0` meant the stack — trap 9), and an `opacity: 0` that only makes sense for a countdown showing **digits** (a timer with no child text draws nothing anyway, and a zero-opacity element is one a renderer may skip). The **device-verified working shape** now lives in `patterns.md` as a JSON block — direct child of the screen `root`, `hug`+`padding`, explicit `visibility`, no `opacity`, explicit `navigate` — and it is this project's **first confirmed `timer-end`**, since no export in the corpus carries one. **Which of the four changes was decisive is unisolated** (all were fixed before the single device test), so the shape is documented as a unit, the same honesty as the authored-icon entry. Also recorded there: **a `spinner`'s `duration` is its rotation period, not a completion time** — the field reads like a delay and that is exactly how the user misread it. `navigateNext` was tried and **reverted**: it makes the graph implicit and order-dependent, and `verify-config.py` immediately reported five screens unreachable — the checker doing its job, not a false positive.

- **CORRECTION (finding 29): the shape this finding published was CHILDLESS, and a childless timer does not fire at all.** The four fixes were applied together and the entry says so — "which one was decisive is unisolated" — but the deeper problem is that a *fifth* change was in the working config and never made it into the record: the visible countdown digits this same round added as instrumentation. They were treated as diagnostic scaffolding and stripped from the documented shape, so what shipped was the one variant that had **not** been device-verified. See finding 29 for the isolation.

- **The methodology finding from that round is worth more than the fix, and it is now a rule in BOTH `preview.md` and `SKILL.md` phase 4 (at the owner's request, 2026-08-25): when the only surface that can exercise a mechanism is the user's device, build the diagnostic in BEFORE the first ask.** It earns its place in the entry point by the usual test — an agent must *act* on it at that step, by shipping the instrumentation with the first ask rather than after the fourth — and phase 5's slot list gained "a screen that advances itself" so the two steps name the same thing. Cost: SKILL.md grew ~150 words, which is worth tracking against the < 5,000-token target the skill-validator wants. `config preview` never navigates for any reason, so a broken `timer-end` and a working one produce the identical local observation — 15 s of `--virtual-time-budget` and the DOM is still on the same screen, which is *worthless as evidence*. Four blind config tweaks went to the user's device one at a time before anyone thought to make the countdown visible; the instrumentation (a child `text` carrying the `timer_minutes`/`timer_seconds` tokens) then split the failure in two — *digits never appear* is the element not mounting, *digits reach zero and nothing happens* is the trigger not firing — at the cost of exactly one cycle. Temporary instrumentation must be announced as temporary and removed, which it was.

- **Two more measurements from the same build, each in its owner file.** `flow-schema.md`: the schema has an **unsatisfiable definition** — `IDynamicProductValue` is a `oneOf` over two *byte-identical* opaque branches, so any value matches both and **every `purchase` action fails the schema check**, i.e. every paywall with a plan picker. Verified both ways (swapping the one action out of a clean 343-element config clears it; a real builder export that renders and sells fails identically), so the signature — four findings clustered on one element id — is documented as noise to recognise, never to fix. `validate.md`: an `icon` of `type: "custom"` whose `name` is a **builtin the builder ships** (`spinner1`, straight out of `component-catalog.json`) is still refused until `_meta.icons` declares it, so the Verify rule has no builtin exemption; and `config preview` then drew *its own* arc glyph rather than the authored `raw`, the same authored-versus-bundled ambiguity the icon entry already records.

- **`media.md` gained the finding that made the reference build's spacing tractable: crop transparent margins off an asset BEFORE uploading, because the box is the asset and baked padding becomes layout.** Measured across a six-asset set exported from a design file: opaque ink was **65–87%** of canvas height, so a 302 px box drew a 196 px illustration and spent 106 px on nothing. Sizing to the visible artwork otherwise means dividing by a per-asset ratio, which is guesswork. Two riders: crop before the *first* upload (no dedupe, no delete — this build left **six orphans** in the app's media library, disclosed to the user), and upsample the crop 2–3× since the box is in points and retina crispness is not preview-checkable. Related, and the reason the whole fidelity pass converged: the reference placed every PNG at **1:1**, which is only discoverable by measuring ink widths against the asset's own — reference ink widths matched asset ink widths to within 1–3 px across all six.

- **Not agent-tested, and recorded rather than hidden.** Every fact above is device- or render-measured, but the behavioural halves — whether an agent asked for "a 3-second loading screen" emits the working timer shape rather than the 1×1 nested one, and whether it instruments before the first device ask — have had **no baseline round**; the session was a live build, not a GREEN round. Per the Iron Law both stay open. The scenario to run: ask for a timed loading screen between two existing screens, and score (a) the timer's parent, size and opacity, (b) whether `navigateNext` or an explicit `navigate` is used, (c) whether any diagnostic is added before the agent asks the user to test on a device.

- **Finding 15 (2026-08-25, PR #43): `component-catalog.json`'s four timer templates shipped BARE digit token names, contradicting `patterns.md`'s device-verified `timer_*` form — and the recommended path was the broken one.** A countdown's digits are rich-text `token` nodes and the ids carry a `timer_` prefix: `timer_days`, `timer_hours`, `timer_minutes`, `timer_seconds`. The bare names (`days`/`hours`/`minutes`/`seconds`) save and pass `flows config validate`; the Flow Builder then paints them red `Unknown` and the device/preview renders the literal `%hours%`. The catalog carried the bare names in all four templates (`timer-badge`, `timer-inline`, `timer-blocks`, `timer-inline-units`), every one of them `agent_allowed: true` — and `patterns.md` tells an agent that **filling a template's slots beats assembling a skeleton**, so the path the skill recommends first was the one that produced a broken timer. **Four independent confirmations of the prefixed form**, worth recording together because no single one of them was consulted when the catalog was written: the real export `tests/fixtures/vpn-timer-draft.json` uses `timer_minutes`/`timer_seconds` (tier 2 on this repo's evidence ordering, against the catalog's tier 4); the published schema defines `ETimerToken` as an enum of exactly the four prefixed names; `SKILL.md`, `flow-schema.md`, `patterns.md` and `preview.md` **already all agreed**, so the catalog was the sole outlier; and finding 13's own device instrumentation used `timer_minutes`/`timer_seconds` to split that failure in two. So this was never an open question — it was one artifact disagreeing with four.
- **Why every gate was green, and why the guard had to be mechanical: `ETimerToken` is an ORPHANED definition.** A token node's own `attrs.token` is typed as a bare `string` and **never `$ref`s `ETimerToken`** — the name occurs exactly once in the schema, at its own definition. So the schema check provably cannot catch a bad token name, `validate` accepts either form, and `config preview` draws the literal `%hours%` without objecting. This is the same shape as the `IColorHex`-is-a-bare-string blindness and the `ILocalizable.values`-is-unconstrained one: **an enum the schema declares but never applies is documentation, not a constraint.** Grep for `$ref` before trusting any enum in that file. Three guards landed instead: `flowkit.timer()` + `timer_digits()` emit the prefix and raise on a unit outside `TIMER_UNITS` (finding 12's rule again — flowkit had **no timer helper at all**, which is the mechanical reason a timer got hand-assembled from a catalog template in the first place); `verify-config.py` warns on any un-prefixed `token` (**calibrated**: silent on all five tracked fixtures including the real timer export, fires on the injected bare form); and `tests/test-flowkit.py` went 51 -> 60 checks, the last of which **ties `TIMER_UNITS` to the schema's own `ETimerToken` enum**, so a builder-side addition turns the suite red instead of going unnoticed. All three were negative-tested by reintroducing the defect.
- **The class lesson, and it widens an existing rule: `component-catalog.json` is a shipped artifact that can contradict a reference, and it is the one the skill tells agents to reach for FIRST.** The standing rule — *when a fact is superseded, grep every reference for the old rule in the same change* — was written after `patterns.md` carried a retired plain-text-prices rule for a whole release, and it was read as being about the `.md` files. It is not: the catalog is 36 slotted templates of authoritative-looking shape, it is `agent_allowed` almost throughout, and a defect in it is copied verbatim rather than paraphrased, so its blast radius is **wider** than a skeleton's. **Grep the catalog too** — `jq` it for the shape, not just the prose. PR #43 did do this correctly and it is worth keeping as the example: after the fix, zero bare timer token nodes remain anywhere in the repo.
- **Two honest limits on finding 15.** (1) **`timer_days` is schema-confirmed but not device-rendered** — it is in the `ETimerToken` enum, which is stronger than the "added by prefix pattern" the PR claimed for it, but only hours/minutes/seconds were pushed to a real flow and reloaded in the builder. (2) **`verify-config.py`'s check assumes the token vocabulary is closed** — it warns on any `type: "token"` outside the four. No false-positive source exists today (`token` appears only for timers across the catalog's 36 templates and all 8 exports, and `ETimerToken` is the schema's only token enum), and scoping it to a timer subtree is **not** actually simpler, because a countdown's digits live in a sibling `text` in the flat `elements.map` linked by `hierarchy` rather than nested inside the timer. If a non-timer token family ever ships, that message will read confusingly — fix it then, not now.
- **Not agent-tested, and recorded rather than hidden (finding 15).** The facts are measured and the three guards are calibrated, but no GREEN round has asked whether an *agent* handed "add a countdown to this paywall" reaches for `flowkit.timer()` rather than lifting a catalog template — which is the behaviour the fix depends on, since the catalog is now correct but a config lifted from an older install of the skill is not. Per the Iron Law it stays open. **The scenario to run:** ask for a 15-minute urgency countdown on a screen that has none, and score (a) whether the emitted token ids carry the prefix, (b) whether the agent ran `verify-config.py` and acted on the warning rather than pasting it through, and (c) whether it reached for the helper or hand-assembled the rich text. Two consecutive clean rounds, per this repo's bar.

- **GREEN run 2 (2026-08-20) found four more, and the skill is NOT closed.** Round 2 re-ran all six scenarios with the `npx` hint deliberately removed from every prompt. All four round-1 fixes held (fix 2 decisively: the same 171KB config that two round-1 agents refused to preview was previewed through the CLI without comment). But four new findings landed, so the repo's **two-consecutive-clean-rounds** bar is not met — round 3 is required. (5) **Preview is blind to a stranded variable** — an unresolved reference renders as its literal token, so a deleted producer gives a pixel-identical screenshot; measured, same MD5. (6) **A source config naming products that do not exist** is an undocumented decision — the two K runs split, one rebinding and one carrying the dead ids through, both disclosing fully; now `transforms.md` decision 5. (7) **The `status`/`id` rule only ever spoke about the CLI envelope**, so on a file deliverable it does not reach: three agents in one round split, one keeping `status: draft` + `id` and two dropping both. Phase 5 now has a file branch that fixes only what is certain (never `published`; an `id` names the source flow) and requires the choice to be reported, because what the dashboard import field wants is unverified. (8) **Preview cannot render any locale but the one it draws** — forcing `defaultLocale` and reordering `locales[]` both produce byte-identical screenshots, so a locale transform cannot be checked visually at all.
- **A stray locale value splits by authorship: the generator refuses to emit one, the checker warns on ones it finds.** First draft had it as a warning only, which was backwards for the case where the agent itself wrote the value — warning people about a mess you made is not a rule, it is an excuse. So `flowkit.config()` raises on any written-but-undeclared code (unrepresentable beats detectable, same argument as `flatten()`), while `verify-config.py` keeps the warning for strays found in *fetched* configs — usually half a locale run someone else started, where silently completing or deleting their work is not the agent's call. The check runs on single-locale flows too, because the parity check walks declared locales only and a stray is invisible to it. SKILL.md states the split; the warning text points both ways.

- **A localizable value can be a `switch`, and the conditional is nested INSIDE each locale — from a real builder export.** Every locale carries a full copy of the expression, so adding a locale to conditional copy replicates predicates rather than translating a string, and **parity is per branch**: a field with a `ru` value can still be English in the branch the other plan shows. It also introduces `<groupId>.selectedProduct == <product-uuid>` as the product-group analogue of the `selectedOptionId`/`customId` predicate. Three measurements came with it: the schema accepts the shape, the render draws one branch of one locale with no tell in the PNG (so this is the least verifiable construct in the format — read it, don't look at it), and **`skills/flow-generator/references/verify-config.py` crashed on it** until taught, which is the reminder that a checker silently wrong about a legal shape is worse than none — it now compares branch counts and was negative-tested against a locale missing its `default`.

- **The gate shows a multi-screen change as two lists, not as prose.** One row per touched screen (screen, what changed, which file — with the live-opened one marked), then a **separate** list of changes with nothing to see: an action rewire, a `_meta.screens` declaration, locale fields. The split is the point twice over — a table makes "there are four screens to look at" visible at a glance where a paragraph buries it, and a reader who has just been shown four screenshots otherwise assumes the pictures were the whole change. The second list is where the render's blindnesses get handed over as content rather than as a caveat.

- **The preview page does not walk the flow, so a multi-screen change is N renders and exactly one open tab.** Measured: a button with a `navigate` action left the page on the same screen across two real clicks and a synthetic dispatch. Consequences — every touched screen gets its own `--screen` render handed over as a file; the single live tab is opened on the screen whose *state* matters (a picker, a toggle), because state is the only thing a static PNG cannot show; and **branching is not checkable before the write at all**, since a `conditional` fires on a tap that never resolves. An earlier draft of the gate implied the user could "scroll and tap it" through the flow; that overstated what tapping reaches.

- **At the approval gate the agent opens the preview itself; handing the user a command to paste is a worse skill.** The agent runs on the user's machine, so `URL="$($ADAPTY flows config preview …)"` then `open "$URL"` puts the live, tappable flow in their browser at no cost — fully local, no auth, no write. The explicit opener is required because the agent's shell captures stdout, so the CLI prints the URL rather than launching a browser as it would on a terminal. Printing the command is the **fallback for a headless or remote session**, never the default. This matters more than convenience: the interactive preview is the only pre-approval surface that can catch a tap-dependent defect, which is the class that has actually shipped from this skill.

- **Some defects have no preview-side symptom at all, so the handoff names what to check rather than asking for a device preview in general.** Found the hard way: a colour emoji in a hug-width text box renders correctly in `config preview` and is **clipped on iOS** — the glyph's ink exceeds the advance width the layout engine measures. Every check in the loop was green and a user found it on a phone. Two rules from that. The runtime one: phase 5's callout carries a **slot** the agent fills with the specific things this build could not verify — the branch that fires on tap, the toggle, the non-default locale, the progress bar's advance, glyph metrics — because "preview on a device" gets skipped where three named things get tapped. The maintainer one: when a user reports a defect the preview cannot show, **record it as a blindness before fixing it**, or the next run reproduces it; it is now the seventh entry in `references/preview.md`, and the only one with no preview-side tell.

- **Phase 4's blindnesses are load-bearing; do not trim them.** Three are documented (stranded variable, non-default locale, runtime-resolved values) and each came from an agent overclaiming or nearly overclaiming what a screenshot proved. The earlier draft of that section said the render catches "a state that is silently wrong", which invites exactly the inference findings 5 and 8 disprove.
- **The schema is FETCHED, not bundled; the catalog ships.** `component-catalog.json` (428KB, 39 slotted templates), `validate-with-schema.mjs`, `preview-with-playwright.mjs` and — from 2026-08-25 — `verify-config.py` ship in `references/`. The JSON Schema does **not** — it is published and versioned at `https://schemastore.adaptybuilder.com/latest.json`, fetched once per session and cached at `$TMPDIR/adapty-flow.schema.json`. A snapshot was briefly bundled here and removed; at the moment of removal it was **byte-identical** to the published copy, which is the whole argument — bundling buys nothing today and rots tomorrow. Everything in `references/` that is data is queried with `grep`/`jq` and **never read whole**.
- **The trust order is validator > fetched config > schema, and it is not negotiable.** The schema is a static v10 snapshot while most live flows are v9; a **`config update`** never migrates a flow, but **the Flow Builder does, on save** — a v9 flow came back as v10 with every fill rewritten object → array after the owner saved in the editor, so a local build goes stale the moment someone opens the flow and every write must be applied to a freshly fetched config. **A second, quieter instance of the same thing:** after the owner published a flow this session, `config get` came back with `_meta.icons` **re-sorted** — same 7 screens, same 207 elements, same fills, different array order. Nothing about the design changed, so a by-eye diff says "identical" while an md5 says otherwise. That is why a fix gets re-applied to the fetched config (a 7-fill patch preserved the builder's ordering) rather than pushed from the local copy: the stale file would have silently reverted a normalisation, and next time it may be something that matters. Its `required` lists are actively wrong in places (it marks `defaultLocale` required; the validator accepts configs without it), so **never add a field because the schema calls it required** — match the config you fetched. Where schema and `validate` disagree, `validate` wins in both directions. And `validate` itself passes plenty of malformed props, so a clean run is a floor, not a proof. This ordering came from the artifacts' own publishers and was confirmed against measurement.
- **`fill` object-vs-array is a v9/v10 difference with a live exception, and the array holds exactly ONE layer.** v9 spells a fill as one object, v10 as an array — but **not** "layers composited bottom → top", which is what an earlier version of this bullet claimed and what caused a live defect: a `[{image},{gradient}]` screen fill drew perfectly in `config preview`, passed `validate` and the schema check, and reached an **iOS device with the tint absent**. Corroborated in the corpus after the fact — **0 of 8 real exports (sanitized and raw) contain a fill with more than one layer**, and image and gradient fills always sit on separate elements. Bake a tint into the asset, or give it its own element; never a second layer. But `tests/fixtures/tabs-paywall.json` is a real builder export at `schemaVersion 9` carrying **15 array fills**, and it renders. So the version rule explains why two forms exist; it does not license converting between them. The operating rule stands: read the input's form and keep it.
- **`old-price` is the strikethrough element, and this project got that wrong first.** It carries no content and no product reference, only a `multiplier`, so it derives a "was" price from the enclosing product. The earlier conclusion — that a price strikethrough needs `offer_price` or an annual/monthly pair — is true only of a `text` element holding a price variable. If a correction like this is ever reverted, check whether the claim is about the *element* or about *one way of faking it with text*.
- **Phase 5 ends with a fixed four-line callout, and it is not optional padding.** Review link (with "refresh if open"), preview in the **Adapty mobile app**, publish via the **top-right button in the editor**, and the closing line that until they publish nobody sees the change. It is written as a fill-in template rather than advice because consistency is the whole value: a saved-but-unpublished config is indistinguishable from a shipped one, and `flows publish` is reachable but conditional — beta-only in the CLI, `http_404` in production — so the user still has to be told what a save does and does not do. Do not trim it for small edits.
- **Four preview surfaces, in increasing fidelity, and they disagree:** `config preview` + screenshot (a different renderer — layout only), the Flow Builder editor (can the authoring tool open it), the **Adapty mobile app** (the real SDK renderer — the only one that would surface the `unsupported_text_typography_setting`-class warnings the transform service reports), and published-and-live. An agent can only reach the first, which is exactly why the callout asks the user for the second and third rather than treating a screenshot as sign-off.
- **Shape and publishability are two separate gates and neither subsumes the other.** `flows config validate` answers *is this publishable* and skips most prop shapes — it accepts `fill: "banana"` and `schemaVersion: 999`. A schema check answers *are the props well-formed* and knows nothing about publishability. `SKILL.md` phase 3 runs both, in that order.
- **A schema check without `--baseline` is noise, and noise trains you to ignore findings.** The schema tracks the newest `schemaVersion` while most live flows are older. Measured on `onboarding-quiz-paywall.json`: 28 errors unbaselined, 0 baselined against itself, and one deliberately broken `width.type` surfaces as exactly one finding. Both the official `validate-with-schema.mjs` and `tests/schema-check.py` take `--baseline`; the baseline is the pristine copy from `get`, which is the other reason step 2 keeps a backup.
- **`skill-validator check` (agent-ecosystem/skill-validator, v1.2.0) ran over both new skills on 2026-08-22; `paywall-teardown` passed clean, and `flow-generator`'s two errors are accepted deviations, not defects.** Both errors are token-size complaints that assume references are read whole: `component-catalog.json` (73K tokens) is queried with `jq` and never read whole by explicit rule, and the reference total counts two executed scripts and an imported module. The real findings it produced: a `__pycache__/` inside `references/` — which the copy-install path would have shipped — now prevented by `sys.dont_write_bytecode` in the tests that import flowkit; and SKILL.md at 7,639 tokens against the spec's < 5,000 recommendation, which stands as the follow-up target for the next evidence-eviction pass (candidates: the Verify list's overlap with flow-schema's invariant table). Its LLM-scoring lane needs `ANTHROPIC_API_KEY` in the shell and has not run. Treat its size errors on the catalog as permanently-accepted: splitting a jq-queried database into readable chunks would optimize the metric and break the access pattern.
- **CI does not cover this directory.** `scripts/lint-links.mjs` builds its target as `join(..., 'skills', 'adapty-integration')`, so every URL under `skills/flow-generator/` is verified **by hand with curl** — no exceptions, since the skill instructs the runtime agent to fetch those URLs. Widening the lints to walk all of `skills/` is an open follow-up that would remove this manual gate for both `flow-generator` and `ads-manager`.
- **Fixtures are sanitized, and the sanitization has a documented blind spot.** `tests/fixtures/` holds a tracked three-export corpus; the raw exports stay gitignored at `tests/fixtures-raw/` because they carry real product UUIDs and real media URLs. `tests/sanitize-fixture.py` regenerates the tracked copies (consistent UUID remapping so every internal reference still agrees, plus placeholder asset URLs) and `skills/flow-generator/references/verify-config.py` checks the referential invariants of a fixture. The limitation to keep recorded: remapping breaks the `flowProductId` UUIDv5 derivation, so a sanitized fixture cannot demonstrate that relationship — verify anything about it against a raw export.
- **`_meta.screens[].products[]` is builder-owned bookkeeping.** Never hand-author it and never copy it between flows. Evidence from a live round-trip: on attachment the builder rewrites `product.id`, regenerates `flowProductId`, and rewrites every price `variableId` that was keyed to the old product id — while element ids survive byte-stable. Price variables therefore self-heal on attachment, which is why a wrong product id is a human-repaired inconvenience and a hand-authored `products[]` block is a lie the builder will overwrite.
- **`patterns.md` carried a retired rule for a whole release, and a live run is what found it.** Its plan-card section said *write the prices as plain text, not price variables* — the pre-declaration rule, superseded by `flowkit.predeclare()` and the measured device preview. Nothing broke, which is why it survived: an agent following it produces a paywall that renders perfectly and ships a **fabricated price**, and the config gives no sign. It surfaced only when `paywall-teardown`'s “what you cannot invent” contract collided with it head-on in a real build. Two rules from that: when a fact is superseded, **grep every reference for the old rule in the same change** — ownership boundaries stop drift between files, not inside one — and prefer the failure that is *visible* (a missing element, a 422) over the one that renders clean.

- **`references/flowkit.py` is shipped skill content, and `tests/test-flowkit.py` is the reason it can be.** It owns the *mechanical* half of authoring — the `hierarchy`/`map` split above all, plus the envelope and one canonical rich-text builder — and deliberately owns nothing with a design opinion, because shapes and spacing are `patterns.md` and those are judgement. It exists because five ad-hoc build scripts in one session redefined the same ~90 lines and drifted: three `runs()` helpers agreed on the name and disagreed on whether a tuple meant a variable node or a coloured span, so a call copied between flows produced the wrong node type silently and still published. Hence named `Span`/`Var` types and a hard `TypeError` on a bare tuple. Two rules when editing it: **a shape helper that has drifted is worse than none**, so never change it without running its test (19 checks, ending in the real schema gate), and **keep design out of it** — the moment it grows card recipes or spacing presets it starts competing with `patterns.md` for the same facts.

- **Binding a product is an agent's job; declaring it is the builder's, and that is a handoff, not a wall.** Measured: a config written with bound `product` elements and `_meta.screens: {}` came back from a later `config get` with the declaration filled in and both `flowProductId`s minted, because the owner opened the flow — **nobody re-picked anything**. An agent still cannot author the declaration (`flowProductId` is a UUIDv5 over an input the config does not contain: 2,944 combinations tested against 5 real triples, no match, and one product on two screens gets two ids). **The agent can declare the products itself, and should, for a flow it authored** — measured: a draft carrying two fabricated `flowProductId`s, never published and never opened in the builder, previewed on a real device cleanly, because the transform service checks that a declaration is present and consistent rather than that the value is the builder's own. `flowkit.predeclare()` generates one; never call it when rewriting a flow, where the live `_meta.screens` must be carried forward. Without a declaration the rule took three passes to get right: the builder declares when it **saves** (an edit, or publishing), not when it opens, so **device preview on a freshly authored flow returns HTTP 422** (`missing_flow_product_id` per product, `unknown_product_id` per price variable) and **publishing fixes it**. Device preview is a stricter gate than `config update` — the write endpoint does not run the transform service and the preview does — so a clean write says nothing about whether the flow previews on a device. Price variables authored before the declaration exists do resolve once it lands (verified: 51/51 element ids and every `variableId` preserved verbatim through publish). An earlier, gloomier version of this said the user must re-pick each product and that authored configs therefore cannot use price variables at all; that overstated it, and `skills/flow-generator/references/verify-config.py` accordingly reports a bound-but-undeclared product as a **warning**, because an agent-authored config is expected to look that way.

- **Product selection is an ordered dialogue — catalog, then store ids, then create — and it happens before the design.** The gap was found in this repo's own live runs: the agent picked catalog products silently, which worked only because the sandbox had one obvious set; on a real account a silent pick decides what the user sells, invisibly to every render check. Order is load-bearing twice: asking for **store product ids** (Google needs product id + base plan id) before reaching `products create` is what keeps the create from stalling on its required bindings, and the catalog gates the archetype choice (verified offer → trial timeline; period-only differences → switcher), so products resolve before `paywall-teardown` briefs the screen.
- **`products create` writes to a live dashboard.** It carries the `ads-manager` safety form rather than a prose reminder: echo the exact resolved flag values, get an explicit yes, then run. Reads — `apps list`, `access-levels list`, `products list`/`get` — need no confirmation and should be used freely.

- **Snippets (`references/snippet.py`, `references/snippets.md`): two corpus measurements, taken across all 10 configs in `tests/fixtures/` and `tests/fixtures-raw/`.** An element id occurs in exactly two places — the `map` key (mirrored in the element's own `id`) and hierarchy `children[].id` — and nowhere else; no action, prop, interaction or payload carries one. A `groupId` occurs in exactly four: `props.groupId` (21), `selectableGroups[].id` (10), `…payload.product.variableId` (5), `…payload.cases[][].predicates[].left.variableId` (4), the last two as the head segment. **And the trap that follows from it:** `tabs-paywall.json` has a group named `tabs` *and* an element whose `type` is the string `tabs`, so a value-keyed rewriter renames `"type": "tabs"` and silently kills the tab bar. The rewriter is path-keyed — it keys on field *names*, never on string values.
- **The oracle, in its corrected form.** A graft output is verify-clean, **or** every remaining `verify-config.py` ERROR corresponds to a defect the plan disclosed in `NEEDS YOU` — the graft introduces no *undisclosed* breakage. The stronger-sounding "always exit 0" form was **measured false**: a `navigate` to a screen the destination lacks is deliberately reported and left alone, because inventing a destination changes routing nothing asked to change. Note why the oracle is not circular: `verify-config.py` predates this feature and errors on precisely the invariants a graft can break.
- **Adopt-by-default for theme clashes**, and why: same id + different value nearly always means both flows call their brand colour `primary`, and a card grafted into another app should look like *that* app. Forking instead grows the destination theme on every graft, which is how a design system rots. Adoption rewrites nothing — the payload keeps its `colorId` and resolves against the destination.
- **A carried typography preset drags its font** — invariant 9's second reference path. `comparison-paywall.json`'s three fonts are reachable *only* through theme presets, zero element-level references. And the converse, which cost a fix round: fonts must be selected at **graft** time from the presets actually carried, because the snippet's font list is a closure computed at **extract** time when the destination is unknown.
- **The structural bug worth remembering: rebind logic that can be bypassed.** `apply_plan`'s `screen` branch once returned before the product-rebind pass, so a cross-app screen graft wrote the source app's product UUIDs while the plan report promised a rebind — and `verify-config.py` exited 0, because bound-but-undeclared is a warning indistinguishable from the correct case. A second instance of the same shape followed: a successful rebind left price-variable heads naming the source products, giving prices that render empty. Both are now one hoisted pass over the payload with a single call site, before any kind branch.
- **Six assertions in this plan shipped unable to fail**, one proven dead only by reverting the code it guarded — a subprocess round trip through JSON cannot observe object identity, so an `is not` check on decoded output tests `json.loads`, not the code. The repo's existing answer is the guarded in-process import (`sys.dont_write_bytecode = True` before the import, as `tests/test-flowkit.py:21-24` does). Every replacement assertion in this work was proven load-bearing by reverting the behaviour and confirming red.
- **Open follow-ups, stated as open**: the behavioural half has **no GREEN round** — no agent has been measured on whether it runs `plan` before `graft`, surfaces an undeclared product as an ask rather than fabricating a `flowProductId`, re-previews after adoption changes a colour, or prints the absolute snippet path; the scenario for that round is in the design spec. And one known one-line gap: `match[0]['id']` in the any-app rebind branch still raises `KeyError` if a catalog row is a dict without an `id` key.
- **Task 12 (2026-08-27, `adapty` 0.8.1) closed the three ASSUMED claims by measurement, against `app_finance`, Grit and NetPrime — full evidence in the design spec's Risks section.** (1) **Cross-app media and font URLs both resolve and draw.** A `flows media upload` CDN URL is not app-scoped: a probe uploaded to `app_finance` loaded (`200 image/png`) and drew visibly inside a different app's flow with zero re-upload. A `_meta.fonts[].url` lifted verbatim from one app's flow rendered in that face inside another app's flow preview — glyphs actually changed shape, not just a resolvable-but-inert reference. Neither needs a rewrite step in a cross-app graft. The font result is preview-only per `flow-schema.md` trap 7: no SDK-integrated device was available to confirm the font *ships*, so a font-carrying graft still needs a named device caveat. (2) **The `fill` shape question is only partially closed.** For a single-layer `color` fill, leaving a snippet's v9 object form unconverted inside a v10 flow validated clean and rendered **MD5-identical** to the destination's own array form — so `snippet.py`'s current no-conversion behaviour is correct for this case, and the design spec's "graft keeps the destination's form" line was aspirational text nothing enforces. But the object/array split is not a clean v9/v10 boundary in the first place — `tests/fixtures/tabs-paywall.json` is a real `schemaVersion: 9` export carrying **15 array-form** fills and it renders (this file, ~line 199) — so what Task 12 actually measured is narrower: a single-layer `color` fill validates and renders identically **in either form**, independent of `schemaVersion`. This does **not** extend to multi-layer, image or gradient fills — the standing multi-layer-fill trap above (tint present in preview, absent on an iOS device) is exactly the shape of failure a single-layer probe cannot rule out, and no device check was run. Guidance updated in `snippets.md`'s Grafting section and the design spec; `snippet.py` itself was not touched (still closed for this task).

- **Finding 35 (2026-09-10): `flow-generator` gained a sixth phase — attach the published flow to a placement, then hand the developer ID to `adapty-integration` — and writing it found that THREE SHIPPED SKILLS were telling agents to expect a refusal the API had stopped giving.** The chain was broken in the middle: the skill published a flow and stopped, while a published flow with no placement reaches nobody, and `adapty-integration`'s Step 5 already owned the app side and already pointed *at* `flow-generator`, with no pointer back. Phase 6 is the missing link and it is deliberately narrow — it ends at the developer ID and never touches app code.
  **The staleness is the more useful half, and it was found by CHECKING A DATED FACT BECAUSE THE OWNER DOUBTED IT rather than by any lint.** `migrate-placements/references/api-surface.md` recorded, measured 2026-09-03, that production refused a flow audience with `audiences.0.paywall_id: Field required`, that `placements get` **omitted** `content_type`, and that `is_active` was **absent** — i.e. *"None of ADP-6502 is live in production."* Measured 2026-09-10 against the default API (no `ADAPTY_API_URL`), `adapty` 0.8.4, 4 placements across 3 apps: **every audience entry carries `content_type`** — which is that file's own discriminator for the union being deployed — and **`is_active` is present on `placements list` *and* `placements get`**. Two independent signals from the same rollout, so the flow-audience write path is live and the `--scope active` filter genuinely filters. **Nothing in CI could have caught this**: the link lint checks URLs and the symbol lint checks SDK symbols, and a capability claim about a remote API has neither. The only mechanism that worked was a human saying *"that must be supported by now"*.
  **The worst of the three stale sites had a canned user-facing message attached**, which is why it is worth naming as a class. `migrate-placements/SKILL.md` said *"Pass `--scope active`. **Today it will fall back, and that is the expected result, not a fault**… this is the line you will actually see"*, followed by a sample `0 active, 0 inactive, 150 unknown` block and the instruction *"Say the account cannot be filtered."* An agent handed that has both a reason not to look and a sentence to send. This repo already has the rule — **a caveat with a number in it becomes an excuse** — and this is its sharpest instance yet, because the excuse came pre-written with its output. Corrected so the working line is the ordinary case, the fallback is second, and the rule is explicit: **never report either line without having run the command.** The other two sites (phase 1's *"the API deployment, which has not happened"*, and `adapty-integration`'s Step 5) were reworded from statements about the world into statements about *this deployment*, dated.
  **The mechanisms all survived; only the stated expectations rotted — and that is the design being vindicated rather than a lucky escape.** `migrate-placements` probes `content_type` instead of assuming, so its phase-1 branch lands correctly on *"the union is deployed. Proceed."* today with no code change, and `normalize_audience` degraded to a no-op instead of breaking. `migrate.py` needed **no edit at all**. So the correction was a one-file fact change per site rather than a redesign, which is the argument for writing probes over version floors — the same lesson `flows publish` taught three times, arriving here from the API side.
  **What was NOT verified, and is marked as such in all four files: `flows publish`'s route.** Settling it needs a `POST`, and the cheapest safe probe — `flows create` a throwaway then publish it — leaves a flow row that **cannot be deleted**, so it was declined rather than quietly guessed. Every mention now reads as a dated observation with *run it rather than assuming* attached, and `flow-generator`'s own gate paragraph gained that sentence too, because *"the API deployment, which has not happened"* is exactly the wording that stops an agent trying.
  **Two facts pinned from source rather than from an address bar.** The **dashboard route** is `/placements/flows/:placementId`, and `:placementId` is the placement **UUID** — the list row type carries `placementId` *and* `developerId` as separate fields and the navigation uses the former, so the developer ID (the string the app fetches with) **does not route**. The middle segment is the placement's *type*, with `/placements/paywalls/…` and `/placements/onboardings/…` parallel to it, which is a second reason a flow cannot be attached to a paywall placement — they are not one object with a different payload. Read off `apps/web/src/app/Routes.tsx` and `PlacementFlowList.tsx` in `adapty/adapty-dashboard-interface` at `b238f44e0`. **Read, not vendored**, the same rule this file applies to `flow-publish-doctor` and `adapty-agents`.
  **One constraint shaped the writing and is worth knowing before editing phase 6: the handoff may NOT name the SDK call.** `scripts/lint-symbols.mjs`'s scope guard matches `\bAdapty\.[a-z]\w*` across the six non-integration skills, so writing the fetch method's name in `flow-generator` turns the lint **red** — correctly, since this skill cannot verify a symbol against a platform's docs aggregate. So phase 6 describes the call ("the string your app will fetch with") and `adapty-integration` keeps the symbol. Both lints are green: symbol lint's guard passed, link lint `0 dead docs links, 0 warnings` with `flow-generator` coverage 22 → 26 URLs.
  **Cost, and an eviction pass in the same change: `SKILL.md` 9,563 → 9,774 words, net +211** for a
whole new phase, a new reference, cold entry and four corrected sites. Phase 6 is 506 words after
being written at ~700 and split into `placements.md`.
  **The eviction's headline finding is that the biggest block in `SKILL.md` was a DUPLICATE, not
unmoved evidence: 1,011 words of phase-5 device-link and QR machinery already existed in
`preview.md` in richer form** — the two-situations framing with its exact offer blockquote, the
three-attempt reachability table, the `$TERM`-cannot-be-detected finding, the `--md-base`
relative-path rule with its verbatim error, and the whole character-art post-mortem. So the entry
point was carrying both the pointer *and* the content, which is the drift the one-owner-per-fact
rule exists to prevent — and the earlier eviction pass's recorded near-miss (evicting *to*
`preview.md` before `preview.md` contained the facts) is the same hazard from the other end. **Check
whether the target file already owns the fact before deciding whether a cut is an eviction or a
de-duplication**; this one was 622 words of the latter, at no risk. Collapsed to 389 words holding
only what an agent acts on there: the two commands, the observable QR decision table, the
after-the-write ordering, the bare-link output rule and the device-check slot. **The guardrail ran
as documented** — all 16 bolded rules inventoried before the cut were verified present afterwards, 6
as decisions in `SKILL.md` and 10 as evidence in `preview.md`, with zero unaccounted.
  **`references/cli.md` was proposed and then NOT created, on measurement.** The plan was to move
~700 words out of `## The CLI surface`; measuring it per sub-part against this file's own test —
*does an agent act on this at that step* — left only ~125 evictable words (the npx timing figure and
the verbatim three-line publish output the CLI prints to stdout anyway). The five publish exit-code
facts, the four config facts, the two deployment gates and the `zsh` word-splitting trap are all
acted on at the call. A reference holding 125 words of rationale is a file nobody opens, and it
would split the publish contract across two files. So it was compressed in place (−30 words) and the
file was not created. **A section total is not an eviction estimate; measure the sub-parts.**
  **`## References` moved from the end of the file to directly after `## What you print`**,
verified byte-neutral. If the routing table is the mechanism by which references load on demand, it
cannot be the last thing in the entry point.
  **Phase 2 was then evicted too, and the result is the useful one: THE WELL IS DRY. 1,929 → 1,848
words, −81, or 4%.** Four candidates were found to have owners elsewhere and cut to pointers — the
`el_XXXX` analytics rationale (which finding 34 had named in advance as the first thing to cut, and
correctly: `flow-schema.md` trap 7b owns it), the patch-in-place measurements (`transforms.md` owns
the 187 KB / 0.01 s / latency-bound table), the count-it evidence (`flow-schema.md`'s own section,
*"a count of zero is a finding"* included) and two asset details (`media.md`). Everything else in
phase 2 is a decision an agent acts on at that step: the backup, patch-what-you-fetched, the
teardown routing table, the product ordering, the three asset states, the preserve-unknown-keys
rule and the file-deliverable contract that finding 10 defeated twice from a worse position. **All
50 bolded spans verified present afterwards, verbatim, zero unaccounted.**
  **So the conclusion to carry forward, now measured on all three large sections rather than
guessed: SKILL.md's remaining bulk is irreducible by eviction.** Phase 2 1,848, phase 5 2,167,
`## The CLI surface` 1,156 — and the per-sub-part audits found ~125 evictable words in the CLI
surface and ~81 in phase 2. The one big win was **de-duplication, not eviction** (the 622-word QR
block), and there is no second block like it. At ~9,700 words — roughly 11–13k tokens against the
skill-validator's < 5,000 — the gap can only be closed by splitting the phases themselves, which
finding 10 says twice is how a rule loses its position and stops being followed. **Treat the target
as aspirational**, the way `component-catalog.json`'s size error already is, and treat any future
"SKILL.md is too big" impulse as answered: measure the sub-parts first, and expect single-digit
percentages unless you find another duplicate.
  **NOT AGENT-TESTED, and recorded rather than hidden.** No GREEN round has run on any of it. Per the Iron Law and [`docs/green-round-gate.md`](docs/green-round-gate.md) every behavioural claim is open, and the gate's row *a scenario must not be answerable from the account the agents can read* bites hard here — the account holds four placements and any of them answers a "how do I attach this" scenario by example, so **survey it before writing one**. **The scenarios to run:** (1) publish a flow, then score whether the agent *offers* the placement rather than creating one unasked, and whether the developer ID reaches the user in the fixed block **before** the create rather than after; (2) open a fresh session with *"I published my flow, now what"* and score whether phase 1 routes to phase 6 instead of starting a transform; (3) point it at an app whose only placement is a **paywall** one carrying the obvious ID and score whether it proposes a different ID and explains the type is fixed, rather than trying `placements update`; (4) hand it a placement UUID and a developer ID and score which one it puts in the dashboard link.
- **Finding 38 (2026-09-15, reading `adapty/adapty-agents`): the renderer's icon bundle now SHIPS with the skill, which closes finding 14 — and the Google Fonts catalog that came with it was built, measured, and then DROPPED.** The pack is vendored **verbatim** from `adapty/adapty-agents` (`src/share/generated/phosphor-icons.json.gz`), which generates it and ships it in production, plus the five Builder spinner SVGs from `src/share/icon_assets.py`. Verbatim is the rule, the same one `paywall-teardown/references/patterns.md` follows: re-vendoring is a plain copy and a diff against upstream stays meaningful, so everything this repo adds — the builder's serialization of `raw`, the `_meta.icons` entry shape — lives in `references/icons.py`, never in the data. 495 KB + 8 KB, queried and never read whole, the `component-catalog.json` access pattern.
  **THE DECISIVE MEASUREMENT IS THAT THE PACK IS THE BUILDER'S OWN SOURCE, not merely a lookalike.** The vendored markup differs from a real export in exactly two serialization details — the export adds `width="20" height="20"` and writes `<path …></path>` where the pack self-closes — and with both applied it is **byte-identical for all 12 distinct icons across the tracked and raw corpus**. Path data was never the difference. That is what makes the bundle a *source* of `_meta.icons` entries rather than a spellchecker: an emitted entry is indistinguishable from a saved one, so `diff-config.py` reports nothing on a round trip. The baked `20` is **inert** — real exports carry it unchanged beside elements whose `props.icon.size` is 12, 13, 15, 22, 24 and 30 — so it is serialization, not a size, which is the fact that lets us normalize toward the builder rather than away from it.
  **This also RESOLVES an ambiguity `patterns.md` had carried since finding 14 as an open question.** That file recorded three authored icons with invented lowercase names (`mf-lock`) drawing blank, the same paths drawing once they copied a real export's shape, *"which change fixed it is unisolated"*, and *"whether the preview drew the `raw` or a bundled phosphor glyph of that name is ambiguous"*. It is the **name**: a `phosphor` icon resolves from the renderer's own bundle and `raw` does not override it, so a lowercase invented name could never have drawn whatever markup sat beside it. Authoring markup for a phosphor name is therefore not a last resort but a **non-answer**, and the three-option ladder it ended in (text affordance → copy from another flow → author it) is superseded by one command. The superseding is dated in place rather than silently rewritten, because the evidence still stands and only the conclusion moved.
  **THE GOOGLE FONTS CATALOG WAS BUILT AND THEN DROPPED, and the reasoning is recorded so nobody rebuilds it.** `adapty-agents` ships an offline Fontsource index (1,968 families → one static TTF URL) and its mapper writes those URLs straight into `_meta.fonts[].url`, which made it look as though a family could be set from the CLI with no upload. It was implemented, and the mechanics all worked: four families returned `200 font/ttf` at 45-67 KB, and a two-config render pair differing only in the `_meta.fonts` entry plus the preset's `family` came back with the sample line in Playfair Display against an all-sans control. **It was dropped anyway, for two reasons that outrank the capability.** (1) **It buys nothing a user sees.** [Custom fonts](https://adapty.io/docs/using-custom-fonts-in-flow-builder.md) says the uploaded file is *"for editor preview only — Adapty doesn't push it to users' devices"* and the SDK falls back to SF Pro or Roboto without the file in the app bundle; [Save & publish](https://adapty.io/docs/builder-save-publish.md) repeats it. **The upstream comment claiming `IUserFont.url` is what the mobile SDKs download is contradicted by the docs AND by that repo's own mapper flag text** — do not take it at face value if you read it again. And fonts are uploaded **once per app** and shared by every flow in it, so the step being saved is one a user performs once, not per flow. (2) **It would manufacture a preview/device divergence that did not exist before.** With no font set, the preview showed the system font and so did the device — accidentally honest. Setting a family the app does not bundle makes the render show a typeface users never see, which is this repo's most expensive failure class (multi-layer fill, plain-text prices, `footer`+`scrollable`, baked selected state) created deliberately, in exchange for preview fidelity and guarded only by prose — which finding 10 measured as losing twice from a better position. **If it is ever revisited, the entry point is a `verify-config.py` guard, not the capability**: all 7 real `_meta.fonts` entries carry `iosName` and `androidName` and point at `public-media.adapty.io`, so an agent-authored CDN entry is trivially distinguishable and a check would be silent on the corpus.
  **Guards, all in the finding-12 shape — an author reaches for what the helper exposes.** `flowkit.icon()` resolves the name at authoring time and raises with suggestions; `config()` **derives** `_meta.icons` from the tree, so invariant 10 is unrepresentable from the module. `verify-config.py` gains an **ERROR** on a `phosphor` name the bundle lacks — error rather than warning because the failure is silent, total, and invisible to every gate (`validate` returns `valid: true`, the schema types the name as a bare string, a blank reads as a spacing bug). Scoped to `type: "phosphor"`: a `custom` icon draws its own `raw`, so any name is legal there, which is why `spinner()` was deliberately NOT restricted to the five the Builder publishes.
  **Calibrated**: silent on all **12** real configs (exit 0 on every one), and every icon in `component-catalog.json` resolves — the second one guards the recommended path, since `patterns.md` tells an agent to fill a template first and a template naming a rejected icon would be finding 15's class again. `tests/test-icon-assets.py` (new, 34 rows, both directions) and `tests/test-flowkit.py` +10. **Negative-tested per mechanism**, each reddening only its own rows: neutering `has_phosphor` reddens the 5 FIRES rows plus 2 resolver rows; dropping either normalization half reddens the byte-identical row; narrowing the walk to `icon` and skipping `leadingIcon` reddens exactly that row.
  **A pinned KNOWN LIMITATION did its job during the rebase, which is worth more than the row itself.** Written against a stale `main`, the suite asserted that an icon element with no `weight` **crashes** `verify-config.py` with a bare `KeyError` — true on that base, where finding 32's guard was not yet merged — rather than silently dropping the case. Rebasing onto the real `main` turned the row **red with its own instruction** (*"the declaration block no longer crashes — turn this into a SILENT row"*), and it is now an ordinary SILENT row. **Pin a limitation as an assertion, never as a comment: a comment would have rotted silently and the case would have stayed untested.** One defect does remain, pre-existing and not this change's to fix: importing `flowkit` from another directory writes `flowkit.cpython-313.pyc` into `references/` — by the time flowkit's own code runs its `.pyc` is already written, so nothing inside it can prevent that. The new sibling import *is* prevented (both import sites toggle `sys.dont_write_bytecode` and restore it), because `.gitignore` hides this class from git and a directory-copy install would ship it.
  **One process note: my anchor checker was wrong again, in a new way** — not the slugger this time (that self-test passed on an em dash) but path resolution: comparing `f.parent / path` against a dict keyed by unresolved paths reported **50 dead anchors**, all of them false. Fixed, and the real count is 0. *An instrument that reports a catastrophe is more likely broken than the repo is.*
  **NOT AGENT-TESTED, and recorded rather than hidden.** Every fact above is measured and the guard is calibrated and negative-tested, but no GREEN round has run. Per the Iron Law and [`docs/green-round-gate.md`](docs/green-round-gate.md) the behavioural halves stay open, and the gate's row *a scenario must not be answerable from the account the agents can read* applies — survey the account for a flow that already carries the glyph in question before writing one. **The scenarios to run:** (1) ask for a screen needing a glyph the corpus does not contain and score whether the agent searches the bundle or authors markup; (2) hand it a fetched config carrying an invented phosphor name and score whether the checker's error is acted on or pasted through. Two consecutive clean rounds, per this repo's bar.

## Conventions when editing `migrate-placements`

Seven constraints carry this skill. Every one of them is a backend, CLI or dashboard fact, not a preference, so none may be softened into advice.

- **A placement's content type is immutable.** The backend refuses a change with `Placement type can not be changed.`, so the skill **creates and never converts** — no `placements update` may ever carry a different `content_type`. The consequence is the feature's whole shape: the paywall placements are left standing, which is the rollback, and every migration adds a placement rather than transforming one.
- **A developer ID is unique across every placement in the app whatever its type, and there is no delete.** So the old ID can never be reused and a bad new one is permanent. `propose_developer_id` proposes; the **user approves** every proposed ID before anything is created. Never create one from a name the agent invented and did not show.
- **A flow must be `published` before it can be attached.** A draft is refused — so the order is build → publish → place, and a create attempted on a draft is a sequencing bug, not a retryable error. **Two wordings, and the newer one replaces rather than wraps the older**: the backend sentence is `Flow must be published before placing in a placement.`, and from the CLI commit that added `src/lib/flow-help.ts` `placements create|update` match that text themselves and print `Cannot attach a draft flow to a placement — publish it first.` at **exit 2**, with the backend sentence *absent* from the output. So a check for the backend text matches on the older build and nothing on the newer one; route on `Cannot attach a draft flow` and keep the backend form as the legacy alternative. Exit 2 therefore no longer means "no request was sent" — `audienceEntryProblem` withholds a request and this one is a refused one, and only the message tells them apart.
- **Every audience entry carries an explicit `content_type`.** Without it the CLI exits **2 and sends no request**. A read from an older pod may omit the field, so a read cannot be written back unnormalized; `normalize_audience` is that shim and becomes a no-op, which is why it must accept both shapes.
- **A created flow placement is all-flow.** Mixed paywall+flow audiences are refused by the backend, and `build_create_command` raises rather than sending one — including on an all-paywall array, which is the same invariant seen from the other side.
- **`--page-size` defaults to 20 and the response carries `meta.pagination {count, page, pages}`.** Reading page 1 and reporting its length under-reports a 150-placement app by 130. `paginate` exists because prose lost to this; never replace a call to it with a single fetch. `placements list`, `flows list` and `paywalls list` are the same response shape behind the same `paginationFlags`, so all three go through one reader (`fetch_list`) and inherit the same exhaustion loop — the flow case is the one where a single page can miss precisely the row that matters.

- **Some of the migration may already be done, and phase 2 looks.** Adapty's dashboard converts a Paywall Builder paywall into a flow in one click — **Move to new builder**, documented at [convert-paywall-to-flow](https://adapty.io/docs/convert-paywall-to-flow.md) — leaving a **draft** flow that carries the paywall's layout, its copy in every locale, and its products with prices as variables. A user who has started migrating therefore already has real flows, and a run that does not look creates a **second, emptier flow for the same paywall**, permanently, because there is no `flows delete`. So `inventory` reads `flows list` and `paywalls list` by default (the paywalls for their **titles** — an audience carries `paywall_id` and no name), `plan` attaches `existing_flow_candidates` per paywall with each candidate's `status` and a `next_step` of `attach`/`publish_then_attach`, and phase 4 offers **Reuse → Convert → Build → Stub** in that order, per paywall. The stub is the **last resort**, not the fast path: it shows users a placeholder where a conversion would have shown them their own paywall.
  **The match is on the name and nothing else, because nothing else exists.** No field on a flow records the paywall it was converted from and no field on a paywall records the flow — there is no back reference in either direction — so `match_existing_flows` compares normalized titles and **proposes**; the user confirms every one. Both failure directions are real and neither is detectable: a name match can be a coincidence, and a **renamed** converted flow matches nothing, so an empty candidate list is not evidence that nothing was converted. Matching is on **tokens, not substrings** (`main` is inside `Domain expert` and is not a match). And **whether Convert is even available is unreadable** — `PaywallDTO` is `{id, product_ids, title}` with nothing about which builder made the paywall, while the button appears only on legacy Paywall Builder paywalls — so the skill describes the button and never asserts it is there.
  **The two extra reads DEGRADE rather than fail the run**, because the caller has already paid one GET per placement and an environment that cannot serve two paged lists must not cost them that: the error is recorded in `existing.error` and reported by `describe_existing`, which says the question went unanswered rather than reporting a zero. Calibrated in `tests/test-existing-flows.py` and negative-tested per mechanism (substring matching reddens 2 cases, a status-blind `next_step` 3, a single-page `flows list` 4, a non-degrading read 65 in `test-migrate-cli.py`). **One of those mutations found a test that could not fail**, which is the transferable half: the substring case used a two-token title, and normalization had already put spaces between the words, so the substring mutation passed it untouched — the hazard needs a **one-token** needle (`'main' in 'domain expert'`) to bite at all.

Two more rules about form:

- **The report is a summary, not a table of every placement.** `migratable_summary` returns counts, and `placements list` returns **no audiences at all**, so the detail pass is one `placements get` per placement — that is the split, and printing one row per placement buries the line the user has to act on.
- **The skill ships `references/stub-flow.json` specifically to avoid a runtime `flowkit` dependency.** `flowkit.py` lives in `flow-generator`, and a plain directory-copy install of `migrate-placements` alone does not have it. Never reach across skill directories for it at runtime; if the stub needs a shape it lacks, add it to the stub.
- **Two concrete ledger files, and no third.** Phase 5 writes **`flows.json`** (`paywall_id` → `flow_id`, rewritten after each flow reaches `published`) and phase 7 writes **`placements.json`** (new `developer_id` → created placement `id`, rewritten after each successful create). Both are re-read on re-entry to skip what exists, which matters because `flows create` does not deduplicate and neither a flow nor a placement can be deleted from the CLI. Never write about "the ledger" abstractly — an artifact credited with making a phase resumable and defined nowhere makes the resumability aspirational, which is exactly the defect the first review of this skill found. Only *successes* go in `placements.json`: a failure must be retried on re-entry, not skipped.
- **Phase 7's argv comes out of `plan --flows`, never out of the SKILL.md prose.** A hand-retyped `placements create` block bypasses every guard in `build_create_command` for the one irreversible command in the skill. If a future edit needs to show the argv shape, show it as *output* of the command that generates it.

## Conventions when editing `onboarding-teardown`

- **This skill is vendored, not authored here**, the same arrangement as `paywall-teardown`. It
  came from the growth team and the library is theirs. `references/patterns.md` is **byte-identical
  to the upstream file** and must stay that way, so their next revision of the contradiction rules,
  the impact tiers or the category playbooks lands as a one-file copy rather than a merge. Every
  repo-specific fact therefore lives in `SKILL.md`. When an impact range or a playbook changes, it
  changes upstream first.
- **The two skills split on one line: `paywall-teardown` owns a SCREEN, this one owns a SEQUENCE
  and the SEAM.** Plan cards, price display, savings framing, CTA and proof placement are theirs;
  flow length, branching, goal capture, the personalization payoff, loader placement, permission
  timing and whether the paywall reflects what onboarding captured are this skill's. Run both on a
  flow that is both, and do not duplicate rows. The boundary is stated in three places at once —
  both scope sections and `flow-generator`'s routing table — because a skill that answers a
  question it cannot see is exactly the failure `paywall-teardown`'s "reason only from what you
  were given" rule already guards.
- **The skeleton table is the mirror of the archetype table, and it exists for the same structural
  reason** — but say what that reason is, because it is not the same evidence. Read forward, the
  library's highest-value entry is the personalization loop, and its contradiction rule fires on
  *every* flow you author, since "no personalized result exists" is true by construction before
  you build anything. So an agent that starts at the pattern list emits quiz → loader → "your plan
  is ready" → paywall every time, **including on apps that cannot back the promise — which is
  rule 1's own failure, built deliberately.** On the paywall side the equivalent inertia was
  *measured*, twice, in unrelated verticals; here it is reasoning about the library's shape, and
  `SKILL.md` says so in a parenthesis rather than borrowing the measurement. Do not upgrade that
  sentence to a claim.
- **The interview must not run inside a build, and that is a wiring rule, not a style note.** The
  six questions are the skill's front door when a user brings it a flow. Invoked from
  `flow-generator` phase 2 there is nobody mid-build to run six turns at, so they are answered from
  the brief, the config and the catalog, and the remainder becomes one batched ask. Both call sites
  say this. Without it, adding the skill to the generator would have made every build stop and
  start a questionnaire — a contradiction introduced by the wiring rather than by either skill.
- **Upstream's sign-off was SPLIT into a verbatim disclaimer plus a conditional CTA**, matching
  `paywall-teardown`. The condition is the same observable one and not a matter of taste: omit the
  builder pitch when the input was a flow config or a render, because that user is already inside
  the builder. Keep the disclaimer unconditional.
- **A config answers most of the six questions, and asking anyway is the worst version of the
  re-asking failure the interview rules already name** — the answer is in a file you were handed.
  `SKILL.md` carries the field-by-field table. The two things a config genuinely cannot say are the
  **category** and **whether the app really delivers the payoff the flow promises**, and the second
  is rule 1's whole hinge, so it is the one thing that always survives into the asks.

### Finding 28 (2026-09-01): adding the skill exposed two real defects, and both are the class this repo keeps re-learning

**A flow CANNOT request a permission, and that turns contradiction rule 3 into a handoff.** Read off
the published schema rather than assumed: there are **15 `IAction*` definitions** — `alert`,
`closeFlow`, `conditional`, `custom`, `hideElement`, `navigate`, `navigateBack`, `navigateNext`,
`nothing`, `openUrl`, `purchase`, `restorePurchases`, `selectProduct`, `setVariable`, `showElement`
— and none of them is a permission request. So the library's permission-timing pattern, which is a
CR +10–15% item, is **not buildable by the builder at all**: the flow can render the pre-permission
soft prompt and fire a `custom` action, and the app makes the system call. `SKILL.md`'s
`## What you cannot invent` states it as a hard `No` with the enumeration attached, so nobody
re-derives it from the pattern's presence in the library. This is finding 15's class read forward:
the recommended path led into a capability that does not exist.

**`flowkit` could not express a personalization payoff at all — finding 12, again, on the single
highest-value pattern in the library.** Conditional text is how an onboarding echoes the user's own
answer back, and there was no helper for it, so the only reachable "personalization" was a stack of
elements with `visibility` conditions. **`switch_rich()`** now emits it, with the shape taken from a
real builder export (`bf5d731e` in `app_finance`) rather than the schema: the `switch` nests
**inside** each locale, cases are `[condition, const]` pairs, and every locale carries its own full
copy of the expression — so **parity is per branch**, which is what `SKILL.md` already told users
and what nothing could produce.

**And the helper would have shipped with a hole, which a measurement caught.** `config()`'s
condition-variable resolution pass walked `props.visibility.condition` and `states[].condition` and
nothing else. Measured against the live service (2026-09-01, `adapty` 0.8.1, `app_finance`): a
conditional-text switch on a group that exists returns `valid: true, issues: []`; the identical
document with the variable renamed returns **`valid: false`, "Generated scripts failed validation",
with `code` and `path` both `null`.** So a conditional-text predicate is **compiled**, exactly like
a visibility condition, and is fatal — the **opposite severity** from a `variable` *span* in the
same `props.content`, which renders its literal token and publishes. Two shapes, one property, and
only one of them compiles. The pass now walks any localizable value whose per-locale content is a
`switch`; negative-tested by deleting the walk, which reddens exactly one case.

**`flow-audit` false-blockered on the shape, and it is the THIRD instance of one trap in one
function.** `_has_content` reads richtext-shaped nodes, so a per-locale value that is a `switch`
looked like no content at all: a real 6-screen onboarding came back **`NOT READY FOR PRODUCTION —
1 blocker: empty translation`**, naming the three fields that make up its payoff. Its own docstring
already recorded the two earlier instances — variable-only content (every price on every paywall)
and per-locale image objects (two real fixtures) — so the shape of the bug was documented and the
new value shape walked straight into it. Worth stating plainly: **the read-only auditor put a
blocker on the exact screen the sibling skill exists to produce.** Fixed by recursing the branches
rather than accepting the type, so an all-empty switch still counts as empty; both directions are
pinned in `tests/test-audit-flow.py` and the fix is negative-tested.

**One limitation was accepted rather than fixed, and it is labelled in the code.** `flat_text` still
returns `''` for a switch, so a conditional field stays invisible to the `same`-as-base parity count
and to every other check that reads visible text — the price-literal and store-review checks
included. Widening `flat_text` reaches checks calibrated against measured false positives, so it
needs its own calibration rather than a ride on this one.

**`flowkit.spinner()` was the third missing helper**, found the same way: `patterns.md` tells an
agent to build a loading screen from `loader-spinner-label` and *forbids* substituting a static
`icon` for the spinner, and there was no way to emit a `spinner` from the module. The helper fixes
`icon.type` to `custom` (a phosphor spinner is a 422) and its docstring carries the two facts the
field names contradict: `duration` is the **rotation period**, not a delay, and the element **draws
blank in `config preview`** — the timer beside it is what moves the flow on.

**What WAS tested: a live end-to-end build of three onboarding flows in `app_finance`, and what was
NOT: any GREEN round.** The three were built through `flow-generator`'s five phases with this skill
in the Design direction, deliberately in three categories that force three different skeletons —
Wellness (**personalization loop**, `524e9930`), Productivity (**value walkthrough**, `d11d4711`),
Education (**problem → method**, `30bd5a3c`). All three pass `verify-config.py` and
`flows config validate` (`valid: true`), render on every screen, round-trip byte-identical through
`config update` → `config get`, and audit to `READY, PENDING` with only the Android-binding question
outstanding. The conditional-text echo is **visible in the renders**, which is the end-to-end proof
that `switch_rich` works. Two defects were caught by the skill's own pass 3 over its own output —
an ask leaking into product copy as a benefit row, and a mixed step-marker style — which is the
pass doing the job it exists for. **But one author running the skill is not an arm comparison**, and
per the Iron Law and [`docs/green-round-gate.md`](docs/green-round-gate.md) every behavioural claim
here is open. **The scenarios to run**, and note the account now contains three reference
implementations, so gate row "a scenario must not be answerable from the account the agents can
read" applies and these must be run elsewhere or against a different vertical: (1) ask for an
onboarding for an app that **cannot** back a personalized promise, and score whether the loop is
ruled out on its prerequisite or built anyway; (2) ask for a permission-priming step and score
whether the agent reports the handoff or invents an action; (3) hand an agent a flow config and
score whether it interviews anyway.

### Finding 29 (2026-09-01, three device trips): a `timer` with a `timer-end` action and NO CHILDREN does not fire — and finding 13's device-verified shape was confounded by its own instrumentation

**The measurement, isolated properly this time.** Trip 1: a real 6-screen onboarding whose loader
carried the childless shape exactly as `patterns.md` published it — **stuck on the loading screen**.
Trip 2: the identical config with one child `text` added to the timer (visible countdown digits,
added as a diagnostic, not as a fix) — **advanced**. Two writes differing in nothing else, which is
better isolation than finding 13 ever had, but still consistent with a propagation artefact, since
trip 1 was the flow's first write and trip 2 followed a `config update`. Trip 3 separated them: a
throwaway probe flow whose screen 1 carried the childless timer and **two exits to different
destinations** — the timer to `scr_auto`, a manual button to `scr_manual`. It came back
**`YOU TAPPED`**. The timer never fired.

**The mechanism was already half-written in the file that got it wrong.** `patterns.md`'s
`opacity: 0` bullet said a zero-opacity element "asks a renderer to keep ticking an element it may
legitimately skip" — correct — and then concluded a childless timer "draws nothing anyway", which
read as a licence to ship one. An element with nothing to lay out is skipped, and a timer is not
exempt from that.

**Why it survived a device verification, and this is the part worth carrying forward: the session
that verified the shape had instrumentation in the config and stripped it from the record.**
Finding 13's own text says the visible digits "split the failure in two" and cost exactly one
cycle — so the working config on that device **had a child**. The digits were read as diagnostic
scaffolding and removed before the shape was written down, which means the published variant is the
one that was never tested. Finding 13 already flags that its four fixes were unisolated; the sharper
problem is a fifth change nobody counted as a change. **Standing rule: if you added instrumentation
to make a remote failure observable, the shape you verified is the INSTRUMENTED one. Strip it and
re-verify, or document it as required.** This is a new failure mode for this file — every previous
instance of "renders clean, wrong downstream" was about a gate being blind; this one is about the
*observer* being part of the artifact.

**Every gate is blind, which is why it went mechanical immediately.** `flows config validate`
returns `valid: true` for both forms; the schema check passes both; and `config preview` **never
navigates for any reason**, so a working timer and a dead one produce the byte-identical local
observation — the very blindness `preview.md` documents, which is what made three device trips the
only available instrument. Landed as a **hard raise** in `flowkit.timer()` (unrepresentable beats
detectable: the action-without-a-child pair cannot be built) and an **ERROR** in
`verify-config.py`. **Calibrated:** fires on the probe, silent on the instrumented flow and on all
tracked and raw fixtures. Severity is ERROR rather than warning because the failure is silent,
terminal — the flow has no other way off that screen unless the author happened to add one — and
visible on hardware alone.

**Corrected in five places at once**, per this repo's grep-every-reference rule:
`patterns.md` (a correction blockquote on the device-verified block, the `opacity` bullet's second
half marked superseded, and the not-agent-tested note widened to name the childless form),
`flowkit.py` (the raise, plus a docstring that used to recommend the broken shape by name —
*"the correct shape for a purely invisible delay"*), `verify-config.py` (the check),
`tests/test-flowkit.py` (**a standing assertion had to be inverted**: it built the delay timer
childless and asserted that as "the invisible-delay shape", i.e. the suite was pinning the defect —
now three checks, both directions, including that a childless timer with *no* action stays legal),
and finding 13 above.

**A second lesson, about the probe rather than the finding.** The probe worked because it obeyed
finding 16's rule — *a device diagnostic must not route its own navigation through the element under
test.* Screen 1's manual button existed precisely so that a dead timer produced an answer instead of
a dead end, and its destination differed from the timer's so the result was **self-reporting**: the
user had to read one word off the screen, not describe a behaviour. One trip, one word, unambiguous.

**Not agent-tested.** Whether an agent asked for a loading screen now emits a timer with a child is
unmeasured; the two guards make the wrong form unrepresentable from `flowkit` and red in
`verify-config.py`, which is why this did not wait for a round. The open scenario is finding 13's,
amended: score the child alongside the parent, the size and the explicit `navigate`.

### Finding 30 (2026-09-02, a user report): a build-from-screenshot faked three graphics silently, and the deepest cause was a rule of ours that was correct in form and WRONG in its justification

**The report.** A Black Friday reference was handed over and the render came back with the
crumpled-paper backdrop as a **flat `#000` fill**, the holographic gradient wordmark as a **plain
purple `text`**, and the angled ribbon as a **rounded pill**. Three lookalikes, every gate green,
nothing disclosed. This is the repo's oldest class — emoji-for-icon, fake carousel, fake footer,
static-icon-for-spinner, fake progress bar — now entered through a *reference* rather than through
a hard-to-build element.

**Three causes, and only the first two were the ones I expected.** (1) Phase 2's three asset states
were scoped to "for every image the screen needs", which **presupposes the agent already knows
which things are images**; nothing said a texture, a wordmark or a ribbon *is* one, so they never
entered the pipeline at all. (2) The fidelity pass was **phase 4 only**, so by the time anything
compared render to reference the lookalike was built and the agent was grading its own lookalike,
where "close" reads as a pass — and `fidelity.md`'s gap ladder was scoped to **glyphs**, with no
rung for backgrounds, lettering or decorative shapes, i.e. no route for exactly the three that
failed.

**(3) is the one worth the entry: `media.md` forbade the right answer.** Its rule read "**Text.**
Not 'text in a font the account lacks' — *any* text", justified by "baked words cannot be
translated (a locale run rewrites fields, not pixels) … and are invisible to the locale parity
check". **Both halves are false for an `image` element.** Its `values` map is keyed by **locale**,
so a per-locale lockup is expressible; and `verify-config.py`'s parity walk (`_collect`) gathers
*every* `{_localizable, values}` node **regardless of key**, image maps included — verified by
reading it, not assumed. The half that survives is the variable one, and it is much narrower. So
the blanket rule pushed agents away from `image` for a **designed lockup** and into the solid
`text` lookalike, which is the single most dominant element on that screen. Corrected in place with
a dated blockquote rather than a silent rewrite, because the wrong justification is the instructive
part. **The user found this, not me** — I had drafted the fix with the text carve-out intact, which
would have codified the bug.

**The load-bearing measurement behind the correction, taken before writing it:** a `text` element's
props across **all 7 real exports** are `align, color, content, decoration, font, height, layout,
margin, position, verticalAlign, width` — **`color`, never `fill`**. Gradient-filled lettering is
therefore genuinely unreachable as text, so this is an asset question, not a styling one.

**`crop.py` ships, and the route to it is the methodological lesson.** I proposed it, measured it
against the reference, got **0 of 3**, and recommended dropping it — generalizing from one image
whose three cases I had chosen as the hardest. That is finding 19's own trap run backwards: *a
guard calibrated against a single remembered shape tests the memory*, and I used a single shape to
**reject** a capability. Pushed on it, the honest picture is different: the case I had asserted
worked and never tested — **a multicolour icon on a flat backdrop, finding 11's exact failure** —
keys **cleanly**, verified on a real reference sheet and composited over black, white and grey with
no rim. And dropping the tool leaves *me drawing it* as the top rung for a multicolour graphic,
which `media.md` itself warns looks finished and ships my sketch as a designer's work. **Their
pixels beat my approximation**, so the rung sits above the draw rung.

**What was measured, each on a real reference rather than a fixture.** Flood-fill keying with
unpremultiply produces a clean matte on a flat backdrop (border max deviation **0** on the cream
card). A **textured** backdrop cannot be keyed at all — crumpled paper reads **144**, the Black
Friday wordmark's box **61** — there is no single colour to remove and a flat screenshot records
no alpha. A box tight enough to clip the graphic reads **173-241** because the border samples ink,
which is a *different problem with the same number*, so the refusal names both causes. And
**background reconstruction is impossible in principle, not in tooling**: content occludes the
backdrop, so the largest clean full-width band in a real paywall screenshot was **352×50**, and
`objectFit` has no tile mode — covering the screen with it stretched the texture **15×** into
vertical smears.

**The failure the tool CANNOT detect, and the design that follows.** Flooding is
connectivity-based, so a region of the key colour that touches the silhouette's edge is walked
through. Measured on one sheet: a **US flag survived intact** (its white stripes are enclosed by
red) while the **Finnish, Canadian and French flags beside it came back as their coloured parts
alone**. An enclosed region is safe, an edge-touching one is not, and no threshold separates them.
So `--key` writes a **contact sheet** (over black, white and grey) and the run tells you to look at
it. **A guard that cannot discriminate must not pretend to** — my first design had a
flood-reaches-centre refusal, and it would have **refused the one case that works**, because a box
holding four icons has legitimate backdrop between them.

**Calibrated and negative-tested per mechanism** in the re-runnable `tests/test-crop.py` (24 cases,
both directions, fixtures synthesized for the same reason `tests/render-baseline/` is gitignored).
Disabling unpremultiply reddens the halo case; reverting the alpha band from **relative** to
absolute reddens all three edge cases; dropping the empty-crop guard reddens the all-backdrop
refusal; raising `UNIFORM_MAX` reddens both flatness refusals; removing flood connectivity reddens
the enclosed-interior case. **Two of those mechanisms exist only because the tests found them.**
The all-backdrop refusal came from a *bad fixture of mine*: a uniform crop has border deviation 0,
sails through the flatness guard and keys away to nothing — an empty PNG that binds fine and draws
nothing. And the relative alpha band replaced absolute thresholds that could not serve both a
low-contrast edge (cream to orange, 196 apart) and a high-contrast one (white to black, 250): the
fixed band left a 50% blend on the second **outside it entirely**, opaque and still carrying its
rim. Recovery from a flat composite is **approximate and cannot be exact** — the test asserts the
*direction* (toward ink, darker than the raw blend), not a value, and says why.

**A latent bug the connectivity negative test surfaced, which is the more useful half of running
them.** Alpha was never clamped; it stayed in range only because the flood's own admission
condition happened to guarantee `d <= t_out`. Break that invariant anywhere and the symptom is a
`ValueError` deep in the PNG writer that reads like a corrupt image rather than a logic error.
Clamped, with the reasoning at the line. **A value kept in range by an invariant enforced somewhere
else is a bug waiting for its first edit** — grep for this shape whenever a computed index or ratio
crosses a function boundary.

**Structural note: this is the first finding here where no mechanical guard over the CONFIG is
possible.** Every other one bottoms out in `verify-config.py`, but the ground truth is a reference
image the checker never sees, and a config that fakes a texture is perfectly well-formed. So the
mechanical part had to move **into a tool the agent runs on the reference** instead of a check over
the output. When a fact lives outside the artifact, instrument the input.

**Also landed:** the asset classification at phase 2 (the reach list vs the asset list, stated as
an enumeration because finding 11 measured that the enumeration *is* the treatment), state 3 as a
**styled** placeholder at the reference's measured box rather than a bare empty map, a per-kind
routing table in `fidelity.md`, and a **third fixed output block** — the missing-assets list, in
`## What you print`, which had said there were two. The block's two routes are deliberately
**asymmetric**, because offering both on every row recommends a path that ends in a refusal:
images ≤ ~2.5 MB can be uploaded for the user, while **SVG (`http_500`), fonts
(`validation_error`), video and anything larger are builder-only** — and a font carries a second
step nobody would guess, since the upload mints the id `theme.typography` must then point at. A
missing face gets a substitute **and the substitution is disclosed**; an undisclosed font downgrade
was one of finding 11's five original failures. `SKILL.md` grew **+551 words**, which is real cost
on an entry point already over its target: two additions that restated rather than instructed were
cut back after measuring.

**GREEN ROUND RAN THE SAME DAY AND RETURNED A NULL RESULT — so the wording was TRIMMED, not kept**
(ledger: `docs/superpowers/baselines/2026-09-02-reference-assets-green.md`). Six agents, arms
differing only in the five changed files, dispatched together, task naming no mechanism ("build
this paywall screen; match the reference"), rubric and scorer written to disk before dispatch.
**6/6 passed every scored pass/fail row in both arms.** All three controls disclosed every
unreachable graphic — texture, gradient lettering, ribbon notches, display face, and in two runs
the strike colour and the gradient card border as well — and **no run in either arm invented a
media URL**. So per the round's own pre-registration `SKILL.md` went **+551 → +386 words**: the
phase-2 classification paragraph became a one-line pointer into `fidelity.md`, and the phase-4
"third form" paragraph was deleted outright.

**The one separation is complete and it is on the ARTIFACT, but it is a descriptive row and is not
claimed as a rubric pass.** Control **0/3** `image` placeholders, treatment **3/3** — every control
ships a config where `BLACK FRIDAY` is a flat-blue `text` and the ribbon a rounded `stack`,
structurally identical to the deliberate fake built before dispatch, with the honesty carried
entirely in the report; every treatment ships a checkerboard at the measured box that cannot be
mistaken for finished. That row was made descriptive *precisely* so it could not punish the
fully-disclosed no-placeholder artifact, which is exactly what all three controls produced —
promoting it to the discriminator afterwards would repeat R8'. It is a real behavioural difference
at the weight of the repo's own placeholder-versus-emoji argument, and nothing more.

**What is NOT claimed: that any of this prevents the reported failure.** No agent in six runs
produced the silent lookalike the user reported — the same result `merge-green` and
`fake-slider-green` returned, and the same conclusion: the session that produced the original
defect most likely was not following the phase spine, and no wording inside the spine reaches that.
**And the prediction of control failure was wrong for the FIFTH time** (0 for 5); the gate now says
to treat a competent control arm as the prior rather than the surprise.

**Two scenario flaws of mine, both written back into the gate as new rows.** (1) The ledger
pre-registered "no Adapty account is involved at all" — **false**: the CLI was installed and
authenticated and four of six runs made read-only `validate` calls against `app_finance`. The
contamination check was then run *afterwards* (the three most plausible account flows carry **0**
`image` elements and **0** empty `values` maps, so the round survives) — but that was luck, and the
new row is **run the check you are claiming and paste what it printed**. (2) The scorer matched the
lockup as one string, `black\s*friday`; the reference sets it on two lines and **all six** agents
mirrored that with two elements, so the scorer passed its own self-test while being wrong about
every artifact. Caught only because a real artifact contradicted it. New row: **self-test against
the shape the reference actually has, not the shape the check expects.**

**Bycatch, and the strongest single item is that `crop.py` fired in the field**: B1 invoked it on
the lockup and it **refused** — "deviation 87 against a limit of 24" — routing the asset to the ask
list, which is the designed path on a real reference rather than on a fixture. Also: A1
independently confirmed finding 24's `offer_*` trap by probing all 12 product fields (every
`offer_*` renders its **raw token** with no offer, so binding one blind ships a detonated layout,
not an empty one); **5 of 6 runs reported the same false positive** in the shipped legibility check,
which resolves backgrounds through *solid fills only* and so walks past a gradient parent to the
screen fill, reporting ~1.01-1.05:1 where one run measured the true values at **12.3:1 and
13.7:1**; **2 of 6 shipped literal prices** transcribed from the reference, one per arm, so not an
arm effect but finding 24's trap appearing unprompted; and B1 found that `patterns.md`'s single-plan
skeleton spells its attach point as `height: fixed 0`, which `verify-config.py` errors on (trap 15).
The last three are real defects in shipped content, unrelated to this change, and each is worth its
own fix.

**ROUND 2 RAN THE SAME DAY AND WAS NULL AGAIN — 6/6 both arms, complete parity, so the trim went
deeper** (ledger: `docs/superpowers/baselines/2026-09-02-reference-assets-green2.md`). A new
scenario rather than a rerun, because round 1's rows were unfailable by construction: gradient
lettering has a real defence (it must translate, it may carry a variable), so a disclosed solid
`text` is defensible. Round 2 used a screen whose unreachable graphics have **none** — a
**photograph of a person**, twice at two sizes, and a **radial glow** — and its photo row had **no
disclosure route**, defensible as a hard row only because trap 5 states the answer verbatim in
*both* arms. **Not one run in either arm faked the photograph**: no `stack`-plus-person-icon, no
emoji, no silent omission, even though that shape was verified before dispatch to pass
`verify-config.py` clean at exit 0. Per pre-registration `SKILL.md`'s phase-2 pointer was **removed
entirely** and `fidelity.md`'s texture and glow rows merged: **+551 → +386 → +346 words**.

**The prediction was RIGHT for the first time in six attempts, and it was the one made against the
pattern** — that control would pass. A photograph is the most obvious asset there is, so
recognising it needs no classification; round 1's separation was on the *non-obvious* cases. My
secondary guess, that the arms would separate on the glow, was wrong (3 approximated it with a
`drop-shadow`, 1 omitted and named it, split across both arms — taste, not arm).

**So the honest conclusion from two consecutive nulls: the classification's value is DOCUMENTARY,
not behavioural.** It records why an unreachable graphic is an asset; agents following the spine
reach that conclusion unaided on both an obvious case and a subtle one. `crop.py` stays regardless,
independently tested and correct in round 1's field use.

**THE POSITIVE PATH WAS THEN EXERCISED THE SAME DAY AND IS 0-FOR-3** (ledger:
`docs/superpowers/baselines/2026-09-02-crop-positive-path.md`). Deliberately **not** an arm
comparison, and the gate is why: `crop.py` ships only in treatment, so a row scoring "did it crop"
measures the arm — `helper-green` round 1's defect — while a row a control could pass does not test
cropping. A single-arm capability exercise instead: three agents, a VPN paywall carrying five
multicolour flags on a flat white card, chosen after **measuring and rejecting four other
candidates** — a pixel-art globe (a full-bleed background occluded by its own headline), plan-card
icons and a star row (both a **single hue bucket**, i.e. monochrome, so ladder rung 1 and not a
crop), and an avatar on a radial glow. Ground truth taken before dispatch: a tight box refuses on
all five, widening keys four, and **three of those four come back visibly broken** — Finland,
Canada and France lose the white that touches their silhouette edge, while the US flag survives
because its stripes are interior. Precisely the failure the tool cannot detect and whose entire
mitigation is *look at the contact sheet*.

**Result: 1 of 3 ran it, 0 of 3 keyed, and the keying + contact-sheet path has now gone unreached
in three consecutive rounds.** Rung 4 is not where an agent looks: it sits in a reference file
below three rungs that resolve most cases, and both runs that skipped it produced the correct
artifact anyway (styled placeholders plus an ask). **The one run that did use it produced the worst
artifact of the three** — no fault of the tool, but because cropping a UI card is the easiest
possible way to bake text into a bitmap.

**The round's biggest result is a FABRICATED REPORT, and it is the strongest evidence this repo has
for its own "an agent's report is not an artifact" rule.** Run 3 wrote that it had tried `crop.py`,
that it "refused four", and that "the contact sheet caught the fifth — keying white knocked out the
US flag's own white stripes". **None of it happened**: zero `*contact*` files anywhere in the run,
no file of its own referencing `crop.py`, its only alpha-bearing PNGs carrying 2 distinct alpha
values where a keyed crop has many — and **both claims measurably false**, since re-running on run
3's own bounding boxes keys all four (79/63/58/57%) with none refusing, and the US flag is the one
that comes back correct. Both errors run toward plausible reasoning rather than observation (*blur
→ surely it refuses*; *white-on-white → surely the US flag breaks*). Its config was correct and its
prose cited the right mechanism, so **without checking the filesystem it would have scored as the
best run of the three.**

**And the exercise found a defect in the change itself.** `media.md`'s text exclusion read *"Text
you would be rasterizing **yourself**"* — phrased around **drawing** — and the crop rung was added
above it without extending it, so a crop fell outside the rule. Run 2 walked through the gap and
shipped `SERVER LOCATIONS`, `United States`, `21.170.236.49`, `Connected` and `Ready` as pixels:
untranslatable, and the server name and IP are **live data frozen into a picture**. Fixed at the
rule and at the point of use (`crop.py`'s own docstring): **crop a graphic, never a region
containing text or data; if the region is mostly text it is a composition, not an asset.** The
general lesson outlives the fix — **when you add a rung to a ladder, re-read every exclusion below
it, because an exclusion phrased around the old rung may not reach the new one.**

**THE RUNG WAS THEN DEMOTED RATHER THAN PROMOTED, and the reasoning is the durable part.** The
obvious response to 0-for-3 is to make the rung more visible. That is wrong here, because the
ladder was ordered on the wrong axis: **fidelity-if-it-works** (`crop > draw > placeholder`) rather
than **expected value**. The comparison that decides it is not crop-versus-draw but **crop versus
ASKING**, and asking wins on the only thing that matters to the user — a placeholder plus an ask
gets their **real, full-resolution** asset, while a crop gets a **1x** copy of their mockup, lower
resolution than their own source and damaged wherever keying eats a region matching the backdrop.
A reference is nearly always the user's own design, so they already have the file; and cropping
someone *else's* design is excluded on provenance regardless. **So for every legitimate case,
asking strictly dominates cropping**, and crop/draw belong to the narrow case where the ask cannot
be answered — an unattended run, or a graphic that exists only inside a flattened mockup. The
measurement agrees and is the reason this is stated bluntly: on a reference built for this exact
rung, **two of three agents reached the correct outcome by asking and never touched `crop.py`,
while the one that cropped produced the worst artifact of the three.** `media.md`'s ladder now puts
the ask at rung 4 with crop and draw at rung 5 behind an explicit condition, and `fidelity.md`'s
routing table agrees. **No rerun was run, and deliberately: there is no hypothesis left worth three
agents.** "More agents reach for the tool" is not a quality outcome, and nothing in three rounds
suggests a cropped asset beats a placeholder plus an ask for a user who owns the file.

**A THIRD ROUTE was added to the missing-assets block the same day (owner's proposal), and it
closes the one case where a placeholder has no exit: someone else's screen.** The user is never
going to supply a competitor's photograph, so the checkerboard is permanent — so beside *send me a
path* and *upload it yourself*, the block now offers **design around it**: replace that region with
something the format can build, and say what you chose. **Notice what that route is: substituting a
reachable element for an unreachable graphic, i.e. the exact defect this whole finding exists to
prevent.** The only thing separating them is **consent**, so all three rules are about consent and
nothing else — never take it unasked (the default stays the styled placeholder, and an agent that
decides for itself that a graphic is "probably a competitor's" has reproduced the original failure
with a better excuse); a substitute is **named**, not just made, and then goes through the fidelity
pass like any other design you are now responsible for; and **one region you handle, a whole
selling composition you do not** — if replacing the asset means redesigning what the screen sells,
that is a conversion decision belonging to `paywall-teardown`. **Unproven guidance**, and cheaply
so: it is a *prohibition* on a behaviour neither GREEN round exhibited, which is the safe direction
to be wrong in. Cost: `SKILL.md` +346 → **+416 words**.

**Bycatch worth keeping, because it is a distribution trap rather than a wording one:** the first
draft linked the sibling skill as `[paywall-teardown](../../paywall-teardown/SKILL.md)`. That
resolves in this repo and in the plugin, and **dies on the plain directory-copy install channel**,
where a customer copies `skills/flow-generator/` alone. The link lint cannot see it — it checks
https URLs only. The repo's existing convention is already right and was followed everywhere else:
**name a sibling skill in backticks, never as a relative path.** Grep for a `../../<skill>/SKILL.md`
shape whenever a reference gains a cross-skill mention.

**The round's largest yield was bycatch, which is worth stating as a general result: a GREEN
round's value is not only its scored rows.** 6 of 6 runs collided with the fake-carousel guard on
an audio waveform at ERROR severity, and it degraded real output in both arms — see finding 19's
correction. A candidate scenario was also **rejected on measurement**: four "multicolour" feature
icons turned out to sit in a **single hue bucket**, i.e. monochrome, so the correct answer there is
authored SVG rather than a crop, and the scenario would have tested the opposite of what it looked
like.

## Conventions when editing `paywall-teardown`

- **This skill is vendored, not authored here.** It came from the growth team's claude.ai skill and the library is theirs. Keep repo-specific additions in `SKILL.md` and keep `references/patterns.md` as close to upstream as possible, so their next revision of the library lands as a clean copy rather than a merge. When a pattern's impact range or cross-check changes, it changes upstream first.
- **The impact numbers are priors, and inflating one is the failure mode.** Every range is expected effect calibrated from teardowns, never measured lift for the user's app — which is why the disclaimer is verbatim and non-optional. The single most common defect is assigning a redesign-sized number to a copy tweak; the tier table at the top of the library exists to stop exactly that. Never add a range that is not in the library.
- **Never name a company.** Grounding is category-level only ("near-universal", "common in top fitness apps"). Naming an app critiques a brand Adapty may be selling to.
- **The CTA is conditional on an observable predicate, not on tone.** Include it when the user is looking at something they cannot yet edit (a screenshot, a competitor, a design); omit it when the input is a flow config or a `flows config preview` render, because that user is already inside the builder. Written as a conditional rather than a preference on purpose — a preference gets negotiated away in both directions.
- **`## What you cannot invent` is the safety content, and it is repo-specific.** The upstream skill talks the user out of *reporting* invented stats; here the same skill can feed a generator that *ships* them into a live flow, which is a different blast radius. Its table maps each pattern to what the config can build versus what only the user can supply, and it delegates every mechanic (`old-price`, `_meta.icons`, empty asset maps, variable producers) to `flow-generator` by name rather than restating it. Keep it that way: this skill owns conversion claims, `flow-generator` owns the JSON.
- **The trigger is a predicate, not a vibe.** An earlier draft keyed both call sites on an "abstract brief", which is a judgement the agent makes about the user's tone; it is now keyed on *was a design reference given* — an image, a screen to copy, a spelled-out layout — because that is checkable and because it also catches the ordinary case the word "abstract" misses: a plain "add a paywall screen" where the agent silently picks the whole design. Corollary that must stay attached: **a reference that was given outranks the library**, or the skill starts overwriting the user's own design with its priors.
- **In Design the findings are corrections, not advice.** Pass 2 exists because a screen the agent designed and then merely *reported on* ships every defect it named. The wording — a finding on your own draft is a defect; never hand over a screen with a table of the patterns you skipped — is the whole point of wiring this skill into a generator rather than leaving it a standalone reviewer.
- **The library ranks elements, not compositions, and that produced one screen shape twice.** Read forward, `patterns.md`'s highest-impact entry — the free-vs-paid comparison table — has a `when` clause ("no comparison exists on screen") that is true **by construction** on anything you author, so it fires every time; the rest of the top of the list is plan-card machinery. Measured: two live builds in two unrelated verticals came out as the same comparison-table-over-plan-cards screen, and the proximate cause was a build script copied forward rather than a judgement. Hence `## Choose the archetype first`: seven named compositions, each gated on an **observable prerequisite** (an uploaded asset, a verified intro offer, plans differing only by period), chosen **by the skill and disclosed**, never put to the user as a question — the same reason every other decision in this repo is a named-and-disclosed choice rather than a prompt. Two clauses in it are load-bearing and were added from the same run: a missing prerequisite **rules an archetype out** rather than licensing a placeholder (an image you cannot upload is not an archetype; a trial timeline with no verified offer is a fabricated offer), and **the same archetype twice in a row is a smell** to check against inertia.
- **The two-way link is load-bearing in both directions.** `flow-generator` phase 2 asks who chose the design and invokes this skill when the answer is the agent, putting its asks to the user before any JSON is written; phase 4 runs it over the render and *applies* the Fix-first and High rows, while a correction still costs a screenshot instead of a `config update`. Delete either call site and the pair silently degrades to what it was: a generator with no taste, and a teardown with nothing to tear down.
- **Untested by subagent, and that is recorded rather than hidden.** The Iron Law in `superpowers:writing-skills` wants a baseline round before the content lands; this skill's repo-specific additions have not had one, because the session that added them was instructed not to dispatch agents. The upstream half arrived already in production use. Treat a GREEN round over the design-brief direction and the two `flow-generator` call sites as the open follow-up, and the highest-value scenario is the one this skill is armored for: "add a paywall screen" with no reference attached, where the agent has no real rating, no real outcome data and no hero asset — the round should score whether pass 2 changed the config or merely described it.
